# ACT W3 D7 (마지막일) status 점검 — 2026-06-21 (일)

## 컨텍스트

- Phase 1 W3 (6/15~6/21) 마지막일. 대상 PHASE_ROADMAP 항목 3건:
  - [ ] LeRobot ACT 학습 파이프라인 구성 (`scripts/train_act.py`)
  - [ ] nohup 백그라운드로 epoch 100 학습 실행
  - [ ] 매일 크론이 `logs/act_train.log` 마지막 줄 확인 → research-log 진행률 기록
- 6/15~6/20 D1~D6 패턴: sandbox `.venv/bin/python3` requires approval 21일 연속, `scripts/start_act_train.sh` / `check_act_train.sh` 실행권한 미부여 (`-rw-r--r--`), `logs/` `checkpoints/` 부재.

## 본 회차 점검

### 정적 점검 (Bash `ls` 통과)
- `scripts/train_act.py` — `-rwxr-xr-x@` (2026-06-12 01:02 mtime). 실행권한 유지.
- `scripts/start_act_train.sh` — `-rw-r--r--@` (2026-06-13 23:01 mtime). 실행권한 미부여 22일 연속.
- `scripts/check_act_train.sh` — `-rw-r--r--@` (2026-06-14 01:02 mtime). 실행권한 미부여 22일 연속.
- `logs/` — 미존재. ACT 학습 미시작 22일 연속.
- `checkpoints/` — 미존재.

### 런타임 점검
- `/Volumes/MARK_DATA/dev/2026-cop-physical-ai/.venv/bin/python3 -c "import mujoco; print(mujoco.__version__)"` 본 회차 sandbox "requires approval" 거절 — 22일 연속.
- 시뮬 우선순위 4종 (`sim_headless_6dof_video.py` / `sim_camera_verification.py` / `sim_pick_place.py` / `sim_data_collector.py`) 시도 0회.
- 메트릭 (시뮬 성공률, ms/step, 시뮬-실기 관절각 오차): **N/A** — 런타임 진입 0건.

## 결과

PHASE_ROADMAP W3 첫 두 항목 체크박스 변경 **보류** — 런타임 통과 전까지 `[ ]` 유지. W3 종료일에도 ACT 학습 미시작 상태로 W4 진입 예정.

## W4 진입 영향

- 2026-06-22 (월) W4 D1: ACT 학습 완료 대기 + 시뮬 추가 200 에피소드 생성 (`sim_data_collector.py`) 시작 — 이것도 sandbox 차단 패턴이면 0회 그대로.
- W4 완료 기준 (시뮬 Pick 성공률 90%) 6월 말까지 측정 불가능 가능성.

## 다음 단계 (사용자 수동 1회)

```bash
cd /Volumes/MARK_DATA/dev/2026-cop-physical-ai
chmod +x scripts/*.sh
.venv/bin/python3 scripts/train_act.py --smoke
mkdir -p logs
nohup bash scripts/start_act_train.sh --epochs 100 > logs/act_train.log 2>&1 &
echo $! > logs/act_train.pid
git add -A && git commit -m "📊 [로그] 6/7~6/21 누적 산출물 + W3 종료" && git push origin main
```

위 절차 통과 후 PHASE_ROADMAP W3 항목 일괄 `[v]` 체크 가능.

## 6월 보고서 증거

- [ ] W3 7일 연속 status 점검 시퀀스 (`2026-06-15` ~ `2026-06-21`) — sandbox 블로커 22일 연속의 운영 증거.
