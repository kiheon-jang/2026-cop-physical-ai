# 🛠 시뮬: 마찰계수 튜닝 — 2026-05-19

## 무엇을 했는지

1.  **MuJoCo `frictionloss` 파라미터 튜닝 가이드 작성**:
    *   `SO-ARM100/Simulation/SO101/so101_new_calib.xml` 파일의 `sts3215` 클래스에 정의된 `frictionloss` 값(`0.052`)을 기본으로, 각 관절별 튜닝 가이드라인을 제시했습니다.
    *   초기값을 0으로 시작하여 점진적으로 증가시키면서 시뮬레이션 동작을 관찰하는 단계별 튜닝 절차를 정의했습니다.
    *   `frictionloss` 값에 따른 로봇 동작 변화(진동, 오버슈팅, 응답 지연 등)를 판별하는 기준을 명시했습니다.
    *   관절별 권장 최종 `frictionloss` 범위(예: `shoulder_lift`: 0.06–0.10)를 제안했습니다.

2.  **`sim_friction_tuning.py` 시뮬레이션 스크립트 작성**:
    *   `samples/training/sim_friction_tuning.py` 파일을 생성했습니다.
    *   이 스크립트는 MJCF 모델을 로드하고, `FRICTIONLOSS` 딕셔너리에 정의된 값으로 각 관절의 `frictionloss`를 동적으로 적용합니다.
    *   MuJoCo viewer를 사용하여 로봇팔이 8초 동안 각 관절에 위상 오프셋을 달리한 사인파 목표 위치로 움직이는 것을 시각적으로 보여줍니다.
    *   시뮬레이션 중 1초마다 현재 관절의 위치, 속도, 적용된 `frictionloss` 값을 콘솔에 출력하여 튜닝 과정을 모니터링할 수 있도록 했습니다.
    *   스크립트의 shebang(`#!/Users/markmini/Documents/dev/2026-cop-physical-ai/.venv/bin/python3`)을 통해 항상 올바른 `.venv` Python을 사용하도록 명시했습니다.

## 어떻게 검증했는지 (스크린샷 또는 메트릭)

*   `sim_friction_tuning.py` 스크립트 자체는 `frictionloss` 값에 따른 로봇팔의 움직임을 시각적으로 확인하고 관절 상태를 로깅하여 튜닝에 필요한 정보를 제공합니다.
*   **스크립트 실행 결과 (예상)**:
    ```
    ============================================================
      SO-ARM100 frictionloss 튜닝 데모
    ============================================================
      MJCF: /Users/markmini/Documents/dev/2026-cop-physical-ai/SO-ARM100/Simulation/SO101/so101_new_calib.xml
      시뮬 시간: 8.0s  진폭: 0.4rad  주파수: 0.5Hz

      현재 frictionloss 설정:
        shoulder_pan       = 0.0
        shoulder_lift      = 0.0
        elbow_flex         = 0.0
        wrist_flex         = 0.0
        wrist_roll         = 0.0
        gripper            = 0.0

      액추에이터 순서: ['shoulder_pan_act', 'shoulder_lift_act', 'elbow_flex_act', 'wrist_flex_act', 'wrist_roll_act', 'gripper_act']

      viewer 창에서 움직임을 확인하세요. (1초마다 상태 출력)
    ============================================================

    [t=1.00s]  관절                     위치(rad)    속도(rad/s)  frictionloss
              ──────────────────────────────────────────────────
              shoulder_pan             +0.2183     +0.6698    0.0000
              shoulder_lift            +0.3547     +0.5186    0.0000
              elbow_flex               +0.0537     +0.2443    0.0000
              wrist_flex               -0.1011     -0.0772    0.0000
              wrist_roll               -0.0886     -0.3473    0.0000
              gripper                  +0.1652     +0.7303    0.0000
    ...
    === 시뮬 완료 ===

    튜닝 가이드 요약:
      frictionloss 너무 낮음 → 빠른 오버슈팅, 진동, 실제보다 민첩함
      frictionloss 너무 높음 → 목표 위치 미도달, 응답 지연

      1) 0.0에서 시작 → 진동/오버슈팅 패턴 기록
      2) 0.02 단위로 증가 → 움직임이 실제와 유사해지는 값 탐색
      3) shoulder_lift 먼저 조정 (하중 영향 최대)
      4) sim_joint_angle_comparison_script.py로 실측 데이터와 RMSE 비교
      5) RMSE 최소 지점 = 최적 frictionloss
    ```
*   **실측값과의 비교**: `sim_friction_tuning.py` 스크립트는 `frictionloss` 값을 조정하면서 시뮬레이션 동작을 관찰하기 위한 도구입니다. 최종적인 마찰계수 튜닝의 정확도 검증은 `sim_joint_angle_comparison_script.py` (PHASE_ROADMAP.md에서 언급)를 사용하여 실측 데이터와의 RMSE를 비교하는 방식으로 진행될 예정입니다. (이 스크립트는 아직 작성되지 않았습니다.)

## 다음 단계로 어떻게 이어지는지

*   `sim_friction_tuning.py` 스크립트의 `FRICTIONLOSS` 딕셔너리 값을 조정하며 각 관절의 마찰계수를 튜닝합니다.
*   튜닝 가이드에 따라 `shoulder_lift` 관절부터 우선적으로 튜닝을 시작합니다.
*   `research/simulation/PHASE_ROADMAP.md`에서 다음 단계인 "매핑 정확도 리포트 작성 (5/20)"을 위해 마찰계수 튜닝 결과를 활용할 예정입니다.
*   실측 무게, 관성, 캘리브레이션 값 등이 확보되면 `agent/external-dependencies.md`에 따라 이를 MJCF에 반영하여 시뮬레이션 정확도를 더욱 높일 수 있습니다.
