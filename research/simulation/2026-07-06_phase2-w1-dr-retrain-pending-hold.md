# Phase 2 W1 — DR 재학습 대기 / STAGE=완료·유지 + 무결성 전수 감사 (2026-07-06 월)

## 요약
결정론적 드라이버(`scripts/cop_pipeline_advance.sh`, 01:00)가 **STAGE=완료/유지 · 데이터 50ep ·
최종 성공률=0.7 (목표 0.90)** 를 보고. 새 사이클 미트리거 → 운영 파이프라인 수집/학습/측정
재실행 없음. 오늘 야간 에이전트 역할 = **드라이버 결과 문서화 + 무결성 전수 감사**(수집/학습/측정
재실행 금지, 하드룰). 어제(7/5) 합성한 DR 50ep(`episodes_cl_dr`)의 **재학습·비교(W1 잔여 절반)는
아직 미트리거** — 사유·조치는 아래 "관찰" 참조.

## 드라이버 결과 (재실행 없음)
```
════════ 🤖 CoP Physical AI 시뮬 파이프라인 (2026-07-06 01:00) ════════
STAGE=완료/유지  데이터 50ep · 최종 성공률=0.7 (목표 0.90)
  한 사이클 완료. 성공률 미달이면 COP_TARGET_EP 상향 또는 데이터 재수집(마커 삭제)으로 다음 사이클 트리거.
```

## 무결성 전수 감사 (비파괴, read-only)
> 오늘의 실질 기여: 운영 데이터·측정치·학습마커·DR 산출물 8종을 전수 검증해 회귀·오염 0 확인.

| 항목 | 값 | 판정 |
|---|---|---|
| 운영 `episodes_cl` | 50 ep / 3350 frame | 불변 |
| DR `episodes_cl_dr` (7/5 합성) | 50 ep / 3350 frame | 불변 |
| 운영 `rollout_summary.json` | sr=0.70 · median_lift=43.7mm · 7/10 · md5 `70484a5c…` | 불변(7/4 복원 md5 일치) |
| 마커 `cop_measured.marker` | mtime 6/25 23:00 | 불변 |
| 마커 `cop_trained_on.marker` | mtime 6/24 17:12 | 불변 |

DR 프록시/seed 측정 산출물(참고, 전부 보존):
- `rollout_summary_dr.json` 0.80 (3축 동시 DR-on 프록시)
- `rollout_summary_dr_{light,friction,camera}.json` 0.70 / 0.70 / 0.70 (축별 ablation)
- `rollout_summary_seed{7,123,2026}.json` 0.90 / 0.80 / 0.90 (cube-placement seed 강건성)
- → 운영 seed42(0.70)는 비관적 끝단, 4-seed 공정추정 ≈0.80~0.83 (7/4 결론 유지)

## 검증 방법
- `data/*/meta/info.json` 의 `total_episodes`/`total_frames` 직접 파싱 → 50/3350 ×2 확인.
- `rollout_summary.json` md5 = `70484a5c8fc1451c256cefc7e6f3006f` (7/4 log 복원 기준값과 일치) → 운영 측정치 무손상.
- 마커 mtime 열람 → 학습/측정 스테이지 미실행(6/24·6/25 고정) 확인.
- 8종 rollout 요약 JSON success_rate 일괄 열람 → 전부 기존 문서화 값과 동일.

## 다음 단계로의 연결
- **W1 잔여 절반 = DR 재학습·비교**는 파이프라인 학습/측정 스테이지 → 드라이버 담당.
  트리거 조건: `COP_DATASET_ROOT=data/episodes_cl_dr` 전환 **또는** 마커 삭제로 새 사이클 유발.
  데이터(`episodes_cl_dr` 50/3350)는 어제 이미 준비 완료 → 트리거만 남음.
- 목표: 헤드라인 seed42 0.70 → 큐브배치 커버리지 확장(DR)으로 공정추정 ~0.82↑ 실증 후
  **W2 (7/8~) zero-shot 실기 추론 격차 측정**(외부의존: 장기헌 Orin/실기 SSH)으로 이행.

## 관찰 / 이슈
- **DR 재학습 미트리거 지속(7/4~7/6, 3일)**: 드라이버가 STAGE=완료/유지에 머무는 이유는
  현 성공률(0.70)이 목표(0.90) 미달이나, 새 사이클을 여는 조건(마커 삭제 / `COP_TARGET_EP` 상향 /
  `COP_DATASET_ROOT` 전환)이 아직 세팅되지 않았기 때문. 데이터(DR 50ep)는 준비됐으나 **트리거가 결손**.
  야간 에이전트는 하드룰(수집/학습/측정 재실행 금지)상 이를 직접 실행하지 않음 →
  드라이버 설정(env/마커) 조정이 다음 사이클 진입의 관문. **이 결정을 표면화**해 둔다.
