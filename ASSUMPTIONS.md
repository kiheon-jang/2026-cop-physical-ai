# 가정 대장 (ASSUMPTIONS)

> 목적: 시뮬 수치를 떠받치는 **검증되지 않은 전제**를 한 곳에 모은다.
> 각 항목은 근거 파일, 무엇이 이 가정을 무너뜨리는가(반증 조건), 현재 상태를 함께 적는다.
> 이 문서는 위원회 산출물이기도 하다 — **실기 트랙이 재개될 때 무엇을 측정해야 하는지의 목록**이다.
> 최종 갱신 2026-09-17 · 모든 항목은 이 세션에서 파일로 직접 확인함.

---

## A1. 플러그 결합이 1자유도 미끄럼으로 표현된다 — 끼임·비틀림이 발생 불가

| | |
|---|---|
| 근거 | `sim/assets/rs232_unplug_scene.xml:84-88` (슬라이드 조인트 1개), `:119-120` (`exclude pcb↔plug`, `exclude rs232_port↔plug`) |
| 실측 기하 | 포트 쉘 x −0.00590~0.00000, 플러그 쉘 x 0.00000~0.00590 — **x 겹침 0**. 두 쉘이 맞닿아 있을 뿐 삽입 형상이 없다. 포트 body 의 geom(flange·housing·shell)은 전부 속찬 박스로 **소켓 공동이 없다** |
| 결과 | 커넥터 분리의 지배적 실물 실패 모드(코킹·재밍·웨징)가 **어떤 보유력 값에서도 발생하지 않는다** |
| 반증 조건 | 실기에서 정렬 오차로 인한 분리 실패가 관측되면, 현재 모델은 그 실패를 표현할 수 없음이 확정된다 |
| 왜 그냥 못 고치나 | `:116` 주석 — 박스 접촉을 켜면 슬라이드가 막혀 과제가 아예 안 풀린다. exclude 는 실수가 아니라 의도된 우회다 |
| 상태 | **미해결.** 10월 전 수정 금지 — 양의 클리어런스 + 회전 자유도 도입은 재수집 + 재학습이 필요하고 기준선 비교가 깨진다 |

## A2. 플러그 보유력 = 7.2 N (쿨롱 마찰, 위치 무관)

| | |
|---|---|
| 근거 | `samples/training/sim_rs232_unplug.py:11-15`. Cinch M24308 데이터시트 접점 분리력 대역을 9핀 환산 |
| 알려진 결함 | 우리 파일 자신이 대역을 **"1.75 N(최소 **분리**력) ~ 30 N(최대 **삽입**력)"** 로 적고 있다 — 분리 하한과 삽입 상한을 섞어 로그 중앙을 취했다. 단위가 다른 두 양의 평균이다 |
| 실측 영향 | **2026-09-17 스윕: 1.75~12 N 에서 옛 기준 부분성공 0.9→0.8 로 평탄, 20 N 에서 0.6 으로 붕괴.** 즉 추정 오차가 결과를 흔드는 구간은 대역 상단뿐이다 |
| 반증 조건 | 당김 저울 1회 측정. 실측이 12 N 을 넘으면 현재 수치는 운영 범위 밖이다 |
| 상태 | **값은 유지, 곡선으로 보고.** 다른 "공표값"으로 교체해도 헤드라인이 안 움직임이 실측으로 확인됨 → 교체 작업의 기대수익 낮음 |

## A3. HHT 플러그의 잭스크류가 체결되어 있지 않다

| | |
|---|---|
| 근거 | `sim_rs232_unplug.py:29` — 나사 미체결 가정 명시 |
| 왜 치명적인가 | 나사가 조여져 있으면 당겨서 빼는 것이 **물리적으로 불가능**하다. 과제 정의가 "풀고 빼기"로 바뀌고 A2 곡선 전체가 무의미해진다 |
| 확인 시도 | `assets/reports/realtrack/rt_closeup.mp4` (480×360, 60s) 프레임 추출·확대·대비보정 판독 시도. 커넥터 쉘과 스크루락 포스트로 보이는 형상은 확인했으나 **체결 여부는 해상도·손떨림으로 확정 불가** |
| 반증 조건 | 실기 근접 사진 1장 |
| 상태 | **미해결. 실측 요청 1순위** — 비용 0, 레버리지 최대 |

## A4. 액추에이터가 지연·백래시·데드밴드 없이 즉시 응답한다

| | |
|---|---|
| 근거 | `sim/assets/so101_real.xml:36` `<position kp="998.22" kv="2.731">` — 지연 모델 없음 |
| kp 출처 | 같은 파일 주석: *"These gains are not a 1-to-1 mapping of the servo gains used in Lerobot"* — 제3자 저장소(RBE501) 유도값이며 서보 비례게인 16 가정 |
| 백래시 | `:38-40` 에 `default class="backlash"` (±0.5°) 가 선언돼 있으나 **이를 쓰는 관절이 0개**. 상류 4개 파일도 동일. 즉 **문서에만 있고 물리에는 없다** |
| 실기 대비 | 실기 명령/측정 로그 분석에서 100~133ms 지연이 보고됨(시뮬 0ms). 내가 재도출하지는 않음 |
| 반증 조건 | 실기 명령·측정 동시 로깅 10초분 |
| 상태 | **미모델링. 10월 전 도입 금지** — 관절 추가는 `qpos[:N_JOINTS]` 위치 인덱싱을 깨뜨려 재수집·재학습이 따라온다 |

## A5. 링크 질량·관성이 CAD 기본값이다

| | |
|---|---|
| 근거 | `so101_real.xml` 의 7개 `<inertial>` — 상류 TheRobotStudio 기본값 |
| 반증 조건 | 링크별 실측 무게. 3D 프린팅 인필만으로도 수십 % 변한다 |
| 상태 | **미수신** (Phase 2 부터 요청, `agent/external-dependencies.md:33-41`) |

## A6. 카메라 파라미터가 창작값이다

| | |
|---|---|
| 근거 | `rs232_unplug_scene.xml:52-53` — top `pos 0.225 0 0.60 fovy 52`, closeup `pos 0.38 -0.16 0.12 fovy 48` |
| 구조적 불일치 | 실기 closeup 은 **손목 장착**, 시뮬 closeup 은 `worldbody` 고정. 튜닝 가능한 수치 차이가 아니라 **관측 종류가 다르다** |
| 추가 간극 | 학습 데이터는 h264 왕복 프레임, 롤아웃은 무손실 raw 렌더 |
| 반증 조건 | 웹캠 모델명 + 내부 파라미터 + 손목 마운트 자세 |
| 상태 | **미수신.** 손목 재부모화는 데이터셋 재수집을 강제 → 10월 전 보류, 간극으로 문서화 |

## A7. 측정 배치가 선언된 존을 대표한다

| | |
|---|---|
| 선언 | `samples/training/sim_pcb_reset.py:30-32` — `ZONE_X (0.15, 0.30)`, `ZONE_Y (±0.075)`, yaw ±10° |
| 실측 | seed 42 의 10 배치가 **x 0.2032~0.2741**. 근단 x 0.150~0.203(존 폭 35.5%)에 **샘플 0건** |
| 기전 | 재추첨 편향 아님 — `rejected` 카운트 배치당 0~1 |
| 나머지 30회 | **배치 기록 없음** (traj 는 nominal 시드만 저장) → 2026-09-17 에 요약에도 기록하도록 수정, 9/18 측정분부터 전수 확인 가능 |
| 상태 | **미검증.** 0.875 가 존 전체를 대표한다고 말할 수 없다 |

## A8. 판정이 "임계 통과 그 스텝의 파지"로 정의된다

| | |
|---|---|
| 근거 | `scripts/render_act_rollout_rs232.py:129-135` |
| 결함 | 1스텝 순간 조건이라 592스텝을 파지한 롤아웃이 부분=실패, 완전=성공이 될 수 있다(실측 1/10). **부분성공 ⊇ 완전분리 포함관계가 깨진다** |
| 의도는 유효 | 같은 실측에서 파지 스텝 0회·12회인 롤아웃이 플러그를 8~11mm 밀어내 옛 기준 성공으로 집계됐다. 새 기준이 잡으려던 실패가 실재함이 확인됨 |
| 상태 | **판정 정의 결정 대기.** 내일 23:00 자동 측정이 이 지표를 게시한다 |

---

## 실기 트랙 재개 시 측정 목록 (우선순위)

1. **잭스크류 체결 여부** (A3) — 근접 사진 1장. 과제 정의 자체가 걸림
2. **플러그 보유력** (A2) — 당김 저울 1회. 곡선을 예측으로 바꿈
3. **명령·측정 동시 로그 10초** (A4) — 지연·강성 식별
4. **웹캠 모델·내부 파라미터·손목 마운트 자세** (A6)
5. **링크별 무게** (A5)
6. **포트/플러그 실클리어런스** (A1) — 끼임 모델링의 전제
