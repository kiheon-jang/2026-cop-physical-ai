# 공표 수치 대장 (CLAIMS)

> 목적: 이 프로젝트가 밖으로 내는 **모든 숫자**에 분모·판정기준·체크포인트·측정환경을 붙인다.
> 넷 중 하나라도 빠진 수치는 감사 불가능하므로 공표하지 않는다.
> 최종 갱신 2026-09-17 · 모든 값은 이 문서 작성 시점에 원본 파일에서 재계산해 확인함.

## 0. 모든 수치에 붙는 공통 단서

**시뮬레이션 측정값이다. 실물 로봇 검증은 수행되지 않았다.**
실기 트랙은 2026-08-26 이후 갱신이 없으며, RS232(3단계)는 실기로 수행된 적이 없다.

---

## 1. 3단계 — RS232 케이블 분리

| 항목 | 값 |
|---|---|
| 부분 분리 | **35/40 = 0.875** · Wilson 95% CI **[0.739, 0.945]** |
| 완전 분리 | **30/40 = 0.750** · Wilson 95% CI **[0.598, 0.858]** |
| 표본 | 4 시드(42, 7, 123, 2026) × 10 롤아웃 = 40회 |
| 판정 | 완전 = 플러그 변위 ≥ **5.90mm** (D 쉘 결합 깊이) / 부분 = ≥ **2.95mm** |
| 판정 방식 | `plug_partial_latch` — **파지 확인 없음** (2026-09-16 이전 기준) |
| 체크포인트 | `act_rs232_sim/epoch_0041` (2026-09-13 학습분) |
| 측정 시각 | 2026-09-15 23:02 ~ 23:06 |
| 씬 | `rs232_unplug_scene.xml` — **옛 케이블 배치(후드 옆 출구)** |
| 보존본 | `checkpoints/_published_baseline_20260913/epoch_0041` (2026-09-17 대피) |

### 반드시 함께 말해야 하는 것

1. **두 신뢰구간이 겹친다.** 0.875 와 0.750 의 차이는 n=40 에서 통계적으로 분리되지 않는다.
   부분이 완전보다 높다고 단정할 수 없다.
2. **4 시드를 통합(pooling)해 계산했다.** 같은 시드 안의 10 롤아웃은 독립이 아니므로
   실제 구간은 위 Wilson 구간보다 **더 넓다**.
3. **이 수치는 곧 대체된다.** 2026-09-16 에 (a) 케이블 인출 방향을 실기 영상 기준으로 정정,
   (b) 판정에 파지 확인 추가 — 두 변경 후 재학습이 2026-09-18 완료 예정.
4. **배치 셀별 분해가 40회 전체에 대해서는 불가능하다.** 요약 파일의 `results` 는 배치를 기록하지
   않는다(기록 필드 = `rollout, success, full_success, max_disp_mm, partial_frame, full_frame`).
   배치는 `history/*_traj.json` 에만 있고, traj 는 **nominal 시드(42) 10회분만** 저장된다.
   → 현재 배치 분석 가능 범위 = 40회 중 10회.
   2026-09-17 에 `results` 에도 `pcb` 를 기록하도록 한 줄 추가했으므로(`render_act_rollout_rs232.py`),
   **2026-09-18 측정분부터는 4시드 40회 전부 분해 가능**하다.

5. **⚠ 배치 커버리지 — 확인 가능한 10회가 선언 존의 근단 3분의 1을 전혀 밟지 않았다.**
   선언 배치 존은 `ZONE_X = (0.15, 0.30)` (`samples/training/sim_pcb_reset.py:30`).
   seed 42 의 10 배치 실측 범위는 **x 0.2032 ~ 0.2741** 로, **x 0.150~0.203 구간(존 폭의 35.5%)에
   샘플이 0건**이다. 균등추출에서 10개 최솟값이 0.2032 이상일 확률은 약 1.3% 다.

   - **원인이 재추첨은 아니다.** 관통 재추첨 카운트가 배치당 0~1 (합 6/30) 로, 근단을 걸러내는
     기전은 관측되지 않았다.
   - **실패가 x 순서로 몰린다.** 최저 x 2개(0.2032, 0.2056)가 가장 나쁜 결과이고,
     x ≥ 0.2176 배치는 전부 부분성공이다. 근단일수록 어렵다는 기존 관찰과 방향이 일치한다.
   - **나머지 3 시드 30회의 배치는 기록이 없어 확인할 수 없다** (traj 는 nominal 시드만 저장).

   → **0.875 가 선언된 배치 존 전체를 대표한다고 말할 수 없다.** 40회 중 10회만 배치가 검증
   가능하고, 그 10회는 가장 어려운 구간을 밟지 않았다. 2026-09-18 측정분부터 40회 전체의
   배치가 기록되므로 그때 재평가한다.

### 시드별 내역 (통합 전)

| 시드 | 부분 | 완전 |
|---|---|---|
| 42 | 9/10 | 7/10 |
| 7 | 7/10 | 6/10 |
| 123 | 9/10 | 9/10 |
| 2026 | 10/10 | 8/10 |

### 기준 변경의 영향 (2026-09-17 실측)

같은 가중치·같은 씬·같은 시드(42)에서 판정만 바꾼 결과:

| 판정 | 부분 | 완전 |
|---|---|---|
| 파지 확인 없음 (옛) | 0.9 | 0.7 |
| 파지 확인 있음 (새) | **0.3** | **0.4** |

**하락 원인은 성능 저하가 아니다.** 10 롤아웃 중 2개는 그리퍼 파지 스텝이 각각 0회·12회인데도
플러그가 8~11mm 움직여 옛 기준에서 성공으로 집계됐다 — 잡지 않고 밀어낸 것이다.
새 기준은 그것을 실패로 센다.

⚠ **판정 결함과 그 처리**: 파지 조건이 "임계를 넘는 그 스텝에 파지"라는 1스텝 순간 조건이라,
2.95mm 통과 순간에만 미파지였던 롤아웃이 부분=실패/완전=성공이 될 수 있었다(실측 1/10).
**부분성공 ⊇ 완전분리 포함관계가 깨졌다.**

→ **2026-09-17 적용**: 임계 통과 **직전 한 제어프레임**(≈33ms) 안의 파지를 인정한다.
통과 이후 파지는 인정하지 않는다(밀어낸 뒤 다시 잡는 경로는 계속 실패).
임계값·배치 존·시드는 불변 — **기준 완화가 아니라 샘플링 결함 제거**다.

⚠ **그러나 포함관계는 회복되지 않았다.** 동일 조건 실측에서 역전이 1건 → 1건으로 남았다
(롤아웃 8 해소, 롤아웃 7 신규 발생). 부분과 완전이 서로 다른 시점의 독립 latch 이기 때문이며
창 크기로 해결되지 않는다. **부분 < 완전 표시가 여전히 가능하다 — 미결.**
근거·대안: `research/decisions/2026-09-17_dr-headline-and-pinch-window.md`.
옛 방식 수치는 `success_rate_nopinch_legacy` 로 계속 병기된다.

> 위 표의 0.3 / 0.4 는 **수정 전** 1스텝 조건으로 측정된 값이다. 창 적용 후 값은
> 2026-09-18 측정분과 함께 갱신한다.

### 보유력 운영 범위 (2026-09-17 실측)

보유력 1.75~12 N 에서 옛 기준 부분성공 0.9~0.8 로 평탄, **20 N 에서 0.6 으로 붕괴**.
상세: `research/simulation/2026-09-17_rs232-retention-operating-envelope.md`

---

## 2. 2단계 — S1 리셋 버튼

| 항목 | 값 |
|---|---|
| 성공률 | **0.925** (4-seed 공정추정) |
| 판정 | LED 자동 판정 |
| 단서 | DR(도메인 랜덤화) 적용 시 약 0.45 로 하락한 측정이 있음 — 로드맵 기록 |

## 3. 1단계 — 집어 옮기기

| 항목 | 값 |
|---|---|
| 성공률 | **40/40** (4-seed) |
| 단서 | DR 적용 시 0.80 (무적용 0.70 대비) |

⚠ **1·2·3단계 수치는 서로 비교 가능하지 않다.** 판정 정의(LED / 변위 / 결착), 시드 수,
프레임 예산, 그리고 사용하는 팔 모델이 다르다 — 3단계만 `so101_real.xml`(실기 관절한계 반영),
1·2단계는 상류 기본 모델을 쓴다.

---

## 4. 표기 템플릿

숫자를 쓰는 모든 자리에 그대로 붙인다:

```
부분 분리 87.5% (4시드 × 10회 = 40회 · 완전 5.90mm/부분 2.95mm · epoch_0041 ·
2026-09-15 측정 · 시뮬레이션, 실물 검증 없음)
```

한 줄 축약: `시뮬 측정값 · 4시드 40회 · 실물 검증 없음`

**"실물 로봇 검증은 수행되지 않았습니다" 문장은 어떤 사유로도 생략하거나 완곡하게 바꾸지 않는다.**

---

## 5. 근거 파일

| 주장 | 파일 |
|---|---|
| 40회 측정 원본 | `research/simulation/inference_progress/rollout_summary_rs232*.json` |
| 측정 이력 | `research/simulation/inference_progress/history/` (15개) |
| 임계 근거 | `samples/training/sim_rs232_unplug.py:10-20` (Adam Tech DXXX-SR DE09 도면 .232″ = 5.90mm) |
| 보유력 근거·한계 | 같은 파일 11-15행 (Cinch M24308 대역, 분리 하한과 삽입 상한을 섞은 유도) |
| 파지 판정 구현 | `scripts/render_act_rollout_rs232.py:129-135` |
| 보유력 곡선 | `research/simulation/2026-09-17_rs232-retention-operating-envelope.md` |
