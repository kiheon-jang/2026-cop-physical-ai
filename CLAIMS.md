# 공표 수치 대장 (CLAIMS)

> 목적: 이 프로젝트가 밖으로 내는 **모든 숫자**에 분모·판정기준·체크포인트·측정환경을 붙인다.
> 넷 중 하나라도 빠진 수치는 감사 불가능하므로 공표하지 않는다.
> 최종 갱신 2026-09-24 · 모든 값은 이 문서 작성 시점에 원본 파일에서 재계산해 확인함.

## 0. 모든 수치에 붙는 공통 단서

**시뮬레이션 측정값이다. 실물 로봇 검증은 수행되지 않았다.**
실기 트랙은 2026-08-26 이후 갱신이 없으며, RS232(3단계)는 실기로 수행된 적이 없다.

### 신뢰구간이 담는 것과 담지 않는 것

이 문서의 Wilson 구간은 **롤아웃 표본추출의 이항 불확실성만** 담는다. 다음 셋은 전혀 들어 있지 않다.

1. **시드 내 비독립.** 같은 시드의 10 롤아웃은 독립이 아니므로 실제 구간은 표기된 것보다 넓다.
2. **파라미터 불확실성.** 보유력(A2)·지연(A4)·카메라(A6) 같은 미검증 값이 틀렸을 때의 영향은
   구간에 전파되지 않았다. 민감도 곡선은 한 축을 따로 훑은 것이지 구간에 합산된 것이 아니다.
3. **모델형태 불확실성.** 시뮬이 실물과 다른 구조를 가진 데서 오는 편향(A1)은 정량화되지 않았고,
   정량화할 방법도 현재 없다. 크기와 부호 모두 미정이다.

즉 **총 불확실성 = 표집오차 ⊕ 시뮬-실물 편향**인데, 표기된 구간은 앞항뿐이고 뒷항은 미정량이다.

### 왜 "시뮬을 더 정교하게 만들어 신뢰도를 올린다"는 경로가 없는가

시뮬 결과의 신뢰도를 올리려면 결국 **그 결과가 실물과 얼마나 맞는지**를 보여야 한다.
그런데 그것을 보이는 방법은 둘뿐이고, 둘 다 실물에 바닥을 댄다.

- **실물(또는 대표 시스템) 실측과 직접 대조** — 우리는 실기 롤아웃 0건이라 불가.
- **더 충실한 다른 시뮬과 대조** — 그 시뮬 자신이 "실물과 맞는가"를 증명해야 하므로 **재귀**다.
  우회로가 아니라 같은 질문을 한 칸 미루는 것일 뿐이다.

그래서 **시뮬만으로 도달 가능한 신뢰도에는 천장이 있다.**
게다가 우리는 그 천장에도 닿지 못한다 — 실물 과제를 결정하는 축(끼임·코킹·웨징)이
우리 모델에서 **구조적으로 표현 불가**이기 때문이다(`ASSUMPTIONS.md` A1).
정렬 강건성이 서로 다른 두 정책이 우리 씬에서는 같은 점수를 받는다.

이 문단의 결론은 하나다. **여기서부터는 정교함이 아니라 실측이 필요하다.**
시뮬 쪽에 자원을 더 넣어 이 한계를 넘는 경로는 존재하지 않는다.
(실기 재개 시 필요한 최소 측정 목록은 `ASSUMPTIONS.md` 말미에 있다. 1순위는 잭스크류 근접 사진 1장이다.)

> 위 두 절은 외부 규격이나 표준을 인용하지 않는다. 우리 자신의 데이터와 가정에서 따라 나오는 논증이다.
> 외부 권위를 빌려 쓰지 않는 이유는, 빌린 권위는 원문을 대조당하는 순간 논증보다 먼저 깨지기 때문이다.

---

## 1. 3단계 — RS232 케이블 분리

| 항목 | 값 |
|---|---|
| 부분 분리 | **24/40 = 0.600** · Wilson 95% CI **[0.446, 0.737]** |
| 완전 분리 | **24/40 = 0.600** · Wilson 95% CI **[0.446, 0.737]** |
| 표본 | 4 시드(42, 7, 123, 2026) × 10 롤아웃 = 40회 |
| 판정 | 완전 = 플러그 변위 ≥ **5.90mm** (D 쉘 결합 깊이) / 부분 = ≥ **2.95mm** |
| 판정 방식 | `plug_partial_latch_pinch` — **파지 확인 포함** (2026-09-17 적용, 기준 강화) |
| 체크포인트 | `act_rs232_sim/epoch_0041` (2026-09-18 02:42 학습 완주분) |
| 측정 시각 | 2026-09-18 23:02 ~ 23:06 |
| 씬 | `rs232_unplug_scene.xml` — **실기 영상 정합 케이블 배치**(2026-09-16 정정) |
| 보존본 | `~/CoP_backup_20260918/epoch_0041_rs232_published` (2026-09-24 대피, sha256 대조 완료) |

### 병기 의무 — 옛 판정(파지 확인 없음) 값

같은 40회를 옛 판정으로 다시 세면 **부분 37/40 = 0.925** · **완전 33/40 = 0.825** 다
(`success_rate_nopinch_legacy` / `full_success_rate_nopinch_legacy`).
**0.600 과 0.925 를 같이 내지 않으면 안 된다.** 둘을 따로 쓰면 어느 쪽이든 오도한다.

### 반드시 함께 말해야 하는 것

1. **0.875 → 0.600 은 성능 하락이 아니다.** 판정에 파지 확인을 넣어 **잡지 않고 밀어낸 롤아웃을
   실패로 재분류**한 결과다. 같은 40회의 옛 판정 값이 0.925/0.825 로 옛 baseline(0.875/0.750)보다
   **높다** — 케이블 출구 정합 재학습으로 모델 자체는 소폭 개선됐다. 헤드라인만 내려갔다.
2. **부분과 완전이 같은 값(0.600)이다.** 포함관계가 성립한다면 부분 ≥ 완전이어야 하므로
   이 동률은 우연이 아니라 아래 4번의 판정 결함이 남아 있다는 신호다.
3. **4 시드를 통합(pooling)해 계산했다.** 같은 시드 안의 10 롤아웃은 독립이 아니므로
   실제 구간은 위 Wilson 구간보다 **더 넓다**.
4. **⚠ 부분성공 ⊇ 완전분리 포함관계가 아직 깨져 있다 — 40회 중 2건(5%).**
   시드 42 롤아웃 #2(변위 10.47mm, 파지 907스텝), 시드 7 롤아웃 #7(변위 8.17mm, 파지 30스텝)이
   **완전=성공 / 부분=실패**로 집계됐다. 부분과 완전이 서로 다른 시점의 독립 latch 이기 때문이며
   파지 창 크기로는 해결되지 않는다. 포함관계를 보장하는 대안(부분 = 완전 OR (2.95mm + 파지))은
   부분성공의 독립적 의미를 잃는 대가 때문에 **2026-09-17 결정문에서 기각**했다 — 미결로 유지한다.
   시드별 집계(부분 0.6/0.5/0.7/0.6 vs 완전 0.5/0.6/0.7/0.6)로는 시드 7 의 1건만 보이고
   시드 42 의 1건은 상쇄돼 가려진다. **역전 건수는 `pinch_inversions` 필드를 시드별로 합산해야 한다.**
   근거·대안: `research/decisions/2026-09-17_dr-headline-and-pinch-window.md`.

5. **배치 커버리지 결함은 해소됐다 (2026-09-18 측정분).**
   선언 배치 존은 `ZONE_X = (0.15, 0.30)` (`samples/training/sim_pcb_reset.py:30`).
   `results` 에 `pcb` 를 기록하도록 고친 뒤 측정한 이번 40회는 **40/40 전부 배치가 기록**되며,
   실측 범위 **x 0.1777 ~ 0.2885**, 근단(x < 0.19) 샘플 **3건**이다.
   직전 공표분(0.875)은 40회 중 10회만 배치 확인이 가능했고 그 범위가 x 0.2032~0.2741 로
   근단 3분의 1을 전혀 밟지 않았다 — **이번 0.600 은 그 결함 없이 존 전역에서 측정된 값이다.**

### 시드별 내역 (통합 전)

| 시드 | 부분 | 완전 | 옛 판정 부분 | 옛 판정 완전 | 역전 |
|---|---|---|---|---|---|
| 42 | 6/10 | 5/10 | 9/10 | 8/10 | 1 |
| 7 | 5/10 | 6/10 | 9/10 | 7/10 | 1 |
| 123 | 7/10 | 7/10 | 10/10 | 10/10 | 0 |
| 2026 | 6/10 | 6/10 | 9/10 | 8/10 | 0 |
| **합계** | **24/40 = 0.600** | **24/40 = 0.600** | **37/40 = 0.925** | **33/40 = 0.825** | **2** |

### 실패 16건은 무엇이었나 (2026-09-24 재집계)

40회 중 실패 16건을 **플러그가 실제로 얼마나 움직였는가**로 분해하면 이렇다.
(원본 `research/simulation/inference_progress/rollout_summary_rs232*.json` 4개에서 직접 재계산.
성공 24 + 실패 16 = 40 으로 위 표와 일치한다.)

| 실패 유형 | 건수 | 읽는 법 |
|---|---|---|
| 변위 < 2.95mm — 거의 못 움직임 | **3** | 정책 실패 |
| 2.95mm ≤ 변위 < 5.90mm — 부분만 통과 | **4** | 정책 실패 |
| **변위 ≥ 5.90mm 인데 파지 게이트 탈락** | **9** | 대부분 판정 아티팩트 |

셋째 줄 9건의 (변위 mm, 누적 파지 스텝):
(6.33, 995) (7.05, 610) (7.42, 1419) (8.11, **0**) (8.17, 30) (9.58, 323) (10.47, 907) (10.80, 410) (12.72, **0**).
**9건 중 7건은 수백~1419 스텝을 실제로 파지했고**, 임계를 넘는 그 한 제어프레임(≈33ms)에 접촉이 없었을 뿐이다.

여기서 나오는 두 문장:

1. **헤드라인 0.600 과 옛 판정 0.825 의 간극은 거의 전부 파지 게이트에서 나온다.**
   변위 ≥ 5.90mm 인 롤아웃은 **33/40** 이고(변위 실측 범위 0.88~13.87mm), 이는 옛 판정 완전분리 33/40 = 0.825 와 같은 집합이다.
2. **잡지 않고 밀어낸 롤아웃은 40회 중 최대 3건(7.5%)이다.** 누적 파지 0스텝인 롤아웃이 40회 중 3건이다.

> **⚠ '최대' 인 이유 — 검출 한계.** `pinched()`(`scripts/render_act_rollout_rs232.py:92`)는
> **후드 geom 하나가 고정조와 가동조에 동시 접촉**하는지만 본다. 쉘·플랜지 등 다른 geom 을 잡거나
> 한쪽 조로만 미는 경로는 파지로 세지 않는다. 따라서 "파지 0스텝"은 파지가 없었다는 증명이 아니라
> **이 정의로는 검출되지 않았다**는 뜻이다. 3건은 상한이다.

**이 표는 0.600 이 왜 낮은지의 설명이지, 낮지 않다는 주장이 아니다.**
판정 기준은 바꾸지 않는다(A8 미결 유지). 헤드라인은 계속 낮은 쪽 숫자인 0.600 이다.

### 대체된 값 (이력 — 공표 금지)

2026-09-15 측정분 **부분 0.875 / 완전 0.750** 은 (a) 옛 케이블 배치, (b) 파지 확인 없는 판정,
(c) 40회 중 10회만 배치 확인 가능이라는 세 조건 아래 나온 값이다.
**2026-09-18 측정분으로 대체됐다. 어떤 자료에도 다시 쓰지 않는다.**

### 기준 변경의 영향 (2026-09-17 실측)

같은 가중치·같은 씬·같은 시드(42)에서 판정만 바꾼 결과:

| 판정 | 부분 | 완전 |
|---|---|---|
| 파지 확인 없음 (옛) | 0.9 | 0.7 |
| 파지 확인 있음 (새) | **0.3** | **0.4** |

**하락 원인은 성능 저하가 아니다.** 10 롤아웃 중 2개는 그리퍼 파지 스텝이 각각 0회·12회인데도
플러그가 8~11mm 움직여 옛 기준에서 성공으로 집계됐다 — 잡지 않고 밀어낸 것이다.
새 기준은 그것을 실패로 센다.

⚠ **판정 결함과 그 처리**: 파지 조건이 "임계를 넘는 그 스텝에 파지"라는 1스텝 순간 조건이라,
2.95mm 통과 순간에만 미파지였던 롤아웃이 부분=실패/완전=성공이 될 수 있었다(실측 1/10).
**부분성공 ⊇ 완전분리 포함관계가 깨졌다.**

→ **2026-09-17 적용**: 임계 통과 **직전 한 제어프레임**(≈33ms) 안의 파지를 인정한다.
통과 이후 파지는 인정하지 않는다(밀어낸 뒤 다시 잡는 경로는 계속 실패).
임계값·배치 존·시드는 불변 — **기준 완화가 아니라 샘플링 결함 제거**다.

⚠ **그러나 포함관계는 회복되지 않았다.** 동일 조건 실측에서 역전이 1건 → 1건으로 남았다
(롤아웃 8 해소, 롤아웃 7 신규 발생). 부분과 완전이 서로 다른 시점의 독립 latch 이기 때문이며
창 크기로 해결되지 않는다. **부분 < 완전 표시가 여전히 가능하다 — 미결.**
근거·대안: `research/decisions/2026-09-17_dr-headline-and-pinch-window.md`.
옛 방식 수치는 `success_rate_nopinch_legacy` 로 계속 병기된다.

> **갱신 완료 (2026-09-18 측정분).** 위 표의 0.3 / 0.4 는 **수정 전** 1스텝 조건·**옛 씬**·
> 시드 42 단독 값이다. 창 적용 + 케이블 출구 정합 + 재학습 후의 4시드 40회 값은
> 이 절 맨 위 표의 **0.600 / 0.600**(옛 판정 0.925 / 0.825)이다.
> 역전은 40회 중 **2건**으로 남아 있다(위 4번).

### 보유력 운영 범위 (2026-09-17 실측)

보유력 1.75~12 N 에서 옛 기준 부분성공 0.9~0.8 로 평탄, **20 N 에서 0.6 으로 붕괴**.
상세: `research/simulation/2026-09-17_rs232-retention-operating-envelope.md`

---

## 2. 2단계 — S1 리셋 버튼

| 항목 | 값 |
|---|---|
| 성공률 | **0.925** (4-seed 공정추정) |
| 판정 | LED 자동 판정 |
| 단서 | DR(도메인 랜덤화) 적용 시 약 0.45 로 하락한 측정이 있음 — 로드맵 기록 |

## 3. 1단계 — 집어 옮기기

| 항목 | 값 |
|---|---|
| 성공률 | **40/40** (4-seed) |
| 단서 | DR 적용 시 0.80 (무적용 0.70 대비) |

⚠ **1·2·3단계 수치는 서로 비교 가능하지 않다.** 판정 정의(LED / 변위 / 결착), 시드 수,
프레임 예산, 그리고 사용하는 팔 모델이 다르다 — 3단계만 `so101_real.xml`(실기 관절한계 반영),
1·2단계는 상류 기본 모델을 쓴다.

---

## 4. 표기 템플릿

숫자를 쓰는 모든 자리에 그대로 붙인다:

```
부분 분리 60.0% · 완전 분리 60.0% (4시드 × 10회 = 40회 · 완전 5.90mm/부분 2.95mm ·
파지 확인 포함 판정 · epoch_0041 · 2026-09-18 측정 · 시뮬레이션, 실물 검증 없음)
※ 파지 확인 없는 옛 판정으로는 같은 40회가 부분 92.5% / 완전 82.5%
```

한 줄 축약: `시뮬 측정값 · 4시드 40회 · 파지 확인 판정 · 실물 검증 없음`

**옛 판정 값(92.5/82.5)을 빼고 60.0 만 쓰거나, 60.0 을 빼고 92.5 만 쓰는 것 둘 다 금지한다.**

**"실물 로봇 검증은 수행되지 않았습니다" 문장은 어떤 사유로도 생략하거나 완곡하게 바꾸지 않는다.**

---

## 5. 근거 파일

| 주장 | 파일 |
|---|---|
| 40회 측정 원본 | `research/simulation/inference_progress/rollout_summary_rs232*.json` |
| 측정 이력 | `research/simulation/inference_progress/history/` (15개) |
| 임계 근거 | `samples/training/sim_rs232_unplug.py:10-20` (Adam Tech DXXX-SR DE09 도면 .232″ = 5.90mm) |
| 보유력 근거·한계 | 같은 파일 11-15행 (Cinch M24308 대역, 분리 하한과 삽입 상한을 섞은 유도) |
| 파지 판정 구현 | `scripts/render_act_rollout_rs232.py:129-135` |
| 보유력 곡선 | `research/simulation/2026-09-17_rs232-retention-operating-envelope.md` |
