# 시뮬 진척 — 2026-06-09 (화요일)

## 오늘 진행 단계
Phase 1 - W3 사전 준비 (정규 W3 윈도우는 6/15~6/21). 6/8 로그의 "다음 단계" 항목
`scripts/train_act.py::build_model()` 를 플레이스홀더 → 실제 구현으로 교체. 동시에
lerobot import 경로 결함도 함께 수정.

## 실행 테스트 결과
- `scripts/train_act.py` 변경:
  1. import 경로 `lerobot.common.policies.act` → `lerobot.policies.act`,
     `lerobot.common.datasets.lerobot_dataset` → `lerobot.datasets.lerobot_dataset`.
     `FeatureType`, `PolicyFeature` 도 함께 import.
     → 기존 경로는 설치본 (lerobot 0.x 신규 레이아웃) 에 존재하지 않아 ImportError 가
     무음으로 발생, `LEROBOT_AVAILABLE=False` 로 빠지던 잠재 결함. 이번 단계에서 함께 해결.
  2. `build_model(config, resume_from=None)` 구현. ACTConfig 필드 매핑:
     `dim_model`, `n_heads`, `n_encoder_layers`, `n_decoder_layers`,
     `optimizer_lr`, `optimizer_lr_backbone`, `optimizer_weight_decay`.
     input/output 은 PolicyFeature dict (`observation.images.<cam>` VISUAL(3,H,W),
     `observation.state` STATE(6,), `action` ACTION(6,)).
     resume_from 지정 시 `ACTPolicy.from_pretrained(resume_from, config=act_config)`.
     device 자동 선택 (cuda > mps > cpu) 후 `model.to(device)`.
  3. `main()` 출력 JSON 의 `pipeline_status.build_model` → `"implemented"`.
- 정적 검토 완료. ACTConfig.__post_init__ 검증 규칙(`vision_backbone.startswith("resnet")`,
  `n_action_steps <= chunk_size`, `n_obs_steps == 1`) 모두 통과 가능한 기본값 유지.

## 관찰 / 이슈
- [자가치유] `.venv/bin/python3` 호출은 본 세션 sandbox 권한으로 여전히 차단 (심볼릭 링크
  대상이 working dir 밖). 정적 변경만 적용, 런타임 검증은 6/15 W3 진입 시 일괄 수행 예정.
- [자가치유] Obsidian 미러 `cp ... 00_AI_Wiki/CoP_PhysicalAI/2026-06/` 차단 (working dir 밖
  쓰기 불가). 사용자 수동 동기화 필요. 2026-06-07/08 과 동일.
- [자가치유] 어제 6/8 로그에서 식별된 `samples/training/sim_pick_place.py` 런타임 측정 보류
  항목은 동일 sandbox 차단으로 또 보류.
- PHASE_ROADMAP W3 항목(6/15 시작) 은 아직 `[ ]` 유지 — 본 작업은 사전 준비이며 W3 정식
  항목 완료가 아님. 따라서 PHASE_ROADMAP 자체 수정 없음.

## 다음 단계
- 2026-06-10~14: `train_act.py::train()` 루프 구현.
  - optimizer: `policy.get_optim_params()` → AdamW (backbone vs 나머지 lr 분리 자동 적용).
  - 에폭 루프 + grad clip + 에폭별 loss_action/loss_kl 메트릭 + 체크포인트 저장
    (`save_every_n_epochs`).
- 2026-06-15 (Phase 1 W3 D1): `--smoke` 1 epoch 검증 → 통과 시 nohup epoch 100 백그라운드
  실행 착수, `logs/act_train.pid` 기록.

## 보고용 증거 후보
- [ ] `scripts/train_act.py::build_model()` 실제 구현 diff + ACTConfig 매핑 결정 근거
      → 6월 보고서 [2.사전학습] 섹션 (W3 진입 사전 준비 증거).
- [ ] lerobot import 경로 수정 (잠재 결함 해결) → 부록.

## git mutation 차단 (자가치유 기록)
- [자가치유] `git add scripts/train_act.py ...` 본 세션 sandbox 권한 차단. 2026-06-07/08 과
  동일 증상. 워킹트리에 미커밋 산출물 누적 (오늘 분 3개 추가: `scripts/train_act.py`(M),
  `research/simulation/2026-06-09_train-act-build-model.md`(??),
  `agent/research-log/2026-06-09.md`(??)). `agent/external-dependencies.md` 의 Claude Code
  v3.2 harness 권한 allowlist 점검 블로커 항목 (장기헌) 그대로 유효 — 추가 메시지 없음.
- 사용자 수동 처리 요청: 워킹트리 전체 `git add -A && git commit -m "🛠 [시뮬] train_act
  build_model 구현 + lerobot import 수정 — 2026-06-09" && git push`. 단, `SO-ARM100`,
  `models/SO-ARM100` 은 submodule 이므로 `git add` 대상에서 제외 (`git status` 의 ` m` =
  submodule modified-content 표시).

---

## 23:30 nightly sim-test (3회차)

### 런타임 실행 차단
- [자가치유] `.venv/bin/python3 -c "import mujoco; ..."` 권한 거부 — 2026-06-07/08 과
  동일 sandbox 차단. 우선순위 타깃 4종
  (`sim_headless_6dof_video.py`, `sim_camera_verification.py`, `sim_pick_place.py`,
  `sim_data_collector.py`) 모두 런타임 메트릭 미수집.
- 23:00 job 산출물(`scripts/train_act.py` build_model 구현) 정적 검토만 완료 — 6/15 W3
  진입 시 일괄 런타임 검증 예정.

### 파일시스템 기반 사실 확인 (대체 증거)
- `data/episodes/meta/info.json`: `total_episodes=200`, `total_frames=12400`, `fps=30`,
  `robot_type=so101`, `codebase_version=v3.0`. Phase 1 W1-2 합성 데이터셋 그대로 유지
  (변동 없음 — 6/15 학습 진입 시 동일 데이터 사용 예정).
- 오늘 신규/수정 파일: `scripts/train_act.py`(M), 
  `research/simulation/2026-06-09_train-act-build-model.md`(??),
  `agent/research-log/2026-06-09.md`(??).

### 메트릭
- 시뮬 동작 성공률: 측정 불가 (런타임 차단)
- 추론 속도: 측정 불가 (런타임 차단)
- 데이터셋 무결성 (filesystem): 200 ep / 12400 frames 유지 ✅

### git push 시도 결과
- [자가치유] `git add ...` 본 세션 sandbox 권한 거부 (2026-06-07 와 동일 증상, 2026-06-08
  와는 다름 — 세션별로 git 권한이 비결정적). commit/push 미수행.
- 워킹트리 누적 미커밋 항목 (오늘 신규: 본 23:30 절 추가분 포함). 사용자 수동 처리
  요청은 위 "git mutation 차단" 절 동일.

---

## 4회차 nightly 재시도 (v3.2 cron 재호출)

- 본 세션도 `.venv/bin/python3`, `git add`, `python3 -m py_compile` 모두 sandbox 차단
  재확인. 동일 증상 4회 연속 누적 — `agent/external-dependencies.md` 의 v3.2 harness
  allowlist 점검 블로커 (장기헌) 그대로 유효.
- 오늘 6/9 의 PHASE_ROADMAP 기준 작업(`build_model()` 구현)은 이전 세션 산출물로 이미
  완료 상태 (`scripts/train_act.py` working tree, `research/simulation/2026-06-09_train-act-build-model.md`).
  재실행으로 추가 산출물 없음. 다음 단계(`train()` 루프, 2026-06-10~14)는 일정상 미선행.
- 신규 산출/변경: 본 절 추가만.

## 5회차 nightly 재시도

- `.venv/bin/python3 --version` 재시도 → sandbox 차단 재확인 (심볼릭 링크 대상
  `~/.local/share/uv/python/cpython-3.14.0-macos-aarch64-none/bin/python3.14` 가 working
  dir 밖). 4회 연속과 동일 증상. 런타임 메트릭 0건.
- `git status --short` 동작 확인. 워킹트리 누적 미커밋: 6/7~6/9 research-log 3건,
  6/7~6/9 research/simulation 3건, `scripts/train_act.py` 신규, samples/training 2건 M,
  agent/external-dependencies.md M, agent/report-evidence/2026-06/INDEX.md M, 그 외
  docs/01_overview/daily-reports html 4건 및 6월 예산 요청 md, .gitignore M. SO-ARM100
  서브모듈 2건은 add 대상 제외.
- 본 회차 산출: 본 절 추가만.
