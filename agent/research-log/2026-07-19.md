# 시뮬 진척 — 2026-07-19 (일요일)

## 오늘 진행 단계
Phase 2 - W2 - floor 배치다양성 재학습 **12차** (시각연동 외부 SIGKILL **확정** + deadlock 규명 자가치유)

> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 외부의존 미수신 → 진입 불가 → floor sim 사이클 전진.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): **11차 run(pid 5069, RSS-probe 발효 코드) 이상종료 감지**
  (`metrics 마지막 epoch 미달`, tail `{"epoch": 49, "step": 10}`, `1 leaked semaphore`, traceback 없음)
  → 새 run **pid 91975**(`--epochs 100 --no-resume`, →`checkpoints/act_floor`) 재시작.

## 실행 테스트 결과 (야간 에이전트, 비파괴 진단)
- **9-run 미스터리 종결 = 시각연동 외부 SIGKILL 확정.** 어제 발효한 RSS 프로브가 처음 남긴 곡선
  (pid 5069, 49 epoch, `logs/act_train_metrics.jsonl`):
  - `rss_bytes` = `827,211,776`(초반 10ep) → `850,837,504`(39ep) = **789→811MB 1회 +22MB 후 완전
    평탄, 단조증가 전무** → **jetsam 반증**.
  - `mps_mem` ~6.64GB 평탄(=16GB의 42%) → **GPU OOM 재반증**. `elapsed_sec` 평균 **368.8s** slowdown 없음.
- **결정적 증거 — epoch↔벽시계 교락 해제**: pid 5069 시작 23:00:52 · 마지막 완료 epoch **48 @ 04:02:24**
  · crash ~**04:04:15**. epoch 가 느려서(368.8s vs 이전 ~313s) **같은 ~04:04 까지 49 epoch 만**(이전
  9-run 은 57) 돌았다 → crash 는 **고정 epoch 아닌 고정 벽시계(~04:04)**. "~57 epoch 천장"은 애초에
  epoch 이 아니라 **매일 ~04:04 벽시계 이벤트**였음. → **외부에서 시각 맞춰 프로세스 급사(SIGKILL) 확정**.
- **FD 누수(7/15)·GPU OOM(7/17)·jetsam(오늘) 세 메모리 가설 전부 기각.**
- **진짜 deadlock**: 100ep×~368s=~10h 는 23:00→04:04(5h) 창에 **물리적으로 못 들어감**. `--no-resume`
  매일 진행분 폐기 → 영원히 완주 불가 = 12-run 무측정의 근본. resume 은 L546 옵티마이저/epoch 리셋으로
  드라이버 완료게이트(L113 `epoch==EPOCHS-1`) 못 채움 → 해법 아님.
- 회귀 spot-check: `joint_angle_comparison_sim.py` PASS, 최대오차 **0.0244°**(elbow) < 1°.

## 관찰 / 이슈
- **[자가치유] 목표 epoch 하향으로 창 안 완주** — `cop_pipeline_advance.sh` L36
  `COP_EPOCHS:-100`→`:-42`(surgical 1줄+근거 주석, bash -n OK). 42×~400s(최악)=~03:41 완주 < 04:04
  → 완주 → pending 승격 → `act_floor/epoch_0041` 측정 → **12-run 만에 첫 floor rollout**. override
  knob(`COP_EPOCHS=100`) 유지. **발효는 내일 재시작**(오늘 pid 91975 는 편집前 `--epochs 100` → 오늘밤도
  ~04:04 재crash; 7/9→7/10 패턴).
- **[한계·정직]** 42ep floor 모델은 baseline(100ep, loss ~0.0045) 대비 **저학습**(ep41 loss ~0.019)
  → 성공률 비교가 epoch 교락. 첫 신호엔 유효, **공정 비교엔 full-epoch 필요**. full-epoch 복원 조건 =
  **~04:04 killer 규명·제거** → `log show`/`launchctl`/`ps` 필요 → **sandbox allowlist 차단(에스컬레이션)**.
- **무결성 격리 유지**: 운영 `rollout_summary.json` act_cl_dr **0.70/50.2mm** 불변(7/7). 마커 target=
  `episodes_floor`·trained_on=`episodes_cl_dr:1783181837`(불변)·pending=`episodes_floor:1783324998`
  (대기·미승격)·measured=`episodes_cl_dr:1783346557`. datasets floor/cl/cl_dr 불변. `act_floor` 최신
  ckpt=`epoch_0059`(과거 run).
- sandbox: `ps`/`log show`/`launchctl`(04:04 killer 규명) 권한 차단 지속 — external-dependencies.md 블로커.

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **내일(7/20)**: 드라이버 재시작이 `COP_EPOCHS=42` 로드 → ~03:41 완주 → pending 승격 →
  `act_floor/epoch_0041` 측정 → floor rollout → 4-seed 공정추정 vs baseline 0.825 / DR-trained 0.800
  (배치 다양성이 성공률 천장을 올리는가 — **12-run 만에 첫 실측**).
- **root fix(full-epoch 복원)**: sandbox 언블록 후 ~04:04 killer 규명·제거 → `COP_EPOCHS=100` 복원.
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.

## 보고용 증거 후보
- [ ] 시각연동 외부 SIGKILL 확정(RSS·mps_mem·FD 전부 평탄 + epoch↔벽시계 교락 해제) → 7월 보고서 [2.x]
  학습 파이프라인 근본원인 규명(12-run 증상추적 루프 종결)
- [ ] deadlock 규명(100ep 창초과) + 창 안 완주 자가치유(COP_EPOCHS 42) → 7월 보고서 [2.x] 학습 안정화
