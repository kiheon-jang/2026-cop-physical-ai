# 시뮬 진척 — 2026-07-04 (토요일)

## 오늘 진행 단계
Phase 2 - W1 - **실패집합 seed 강건성 검증**: cube-placement seed 를 바꿔 실패 rollout 이
이동하는지 측정 → 7/3 의 "남은 실패 = 특정 큐브배치(모방격차)" **가설을 실증**. (비파괴 프록시, 재학습 없음.)

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(01:00): **STAGE=완료/유지 · 데이터 50ep · 최종 성공률=0.7 (목표 0.90)**.
  COP_TARGET_EP 미상향·DR 마커 미설정 → 새 사이클 미트리거 → 파이프라인 수집/학습/측정 재실행 없음.
- 3자 정합: `data/episodes_cl` 50ep/3350frame · `rollout_summary.json` 0.70({2,5,8}) ·
  `logs/cop_measured.marker`/`cop_trained_on.marker` 존재.

## 실행 테스트 결과
- **방법(비파괴)**: 운영 `rollout_summary.json` 백업 → seed 3종 실행 → 각 결과를
  `rollout_summary_seed<N>.json` 로 격리 → 운영 파일 복원 후 **md5 대조로 무손상 보증**.
  (동일 epoch_0099, N=10, cpu, 각 run ~13.6s.)

  | seed | 성공률 | median lift | 실패 rollout(큐브배치) |
  |---|---|---|---|
  | **42 (운영 baseline)** | 0.70 (7/10) | 43.7mm | {2, 5, 8} |
  | 7 | 0.90 (9/10) | 44.8mm | {9} |
  | 123 | 0.80 (8/10) | 43.7mm | {0, 1} |
  | 2026 | 0.90 (9/10) | 44.8mm | {5} |

- **실패집합이 seed 마다 완전 이동**: {2,5,8}→{9}→{0,1}→{5} → 실패는 정책 고정약점이 아니라
  **특정 큐브배치**에 달림 = **7/3 모방격차 가설 실증**(assertion → evidence).
- **운영 seed42(0.70)는 성공률 분포(0.70~0.90)의 비관적 끝단**. 4-seed 평균 **0.825** →
  헤드라인 "70%"는 정책 실력을 **과소평가**. 큐브배치 분포 전반 공정추정 ≈**0.80~0.83**.
- 상세: `research/simulation/2026-07-04_phase2-w1-seed-robustness.md` (Obsidian 미러 완료).

## 관찰 / 이슈
- 비-DR 경로는 산출물을 `rollout_summary.json`(운영)에 하드코딩 → seed sweep 은 반드시 백업/복원 필요.
  본 회차 준수, 실행 후 운영 파일 **md5 byte-identical 복원 확인**. `episodes_cl` 50/3350 불변.
- 신규 산출물은 `rollout_summary_seed{7,123,2026}.json` 3종뿐(추가만). 임시 오케스트레이션 스크립트는 실행 후 삭제.
- [자가치유] 없음 — 드라이버 STAGE 정상, 마커·산출물 3자 정합, 운영 데이터/측정치 불변, 에러 없음.

## 보고용 증거 후보
- [x] seed 강건성: 실패 rollout 이 seed 마다 이동(고정 약점 아님) + 공정추정 ~82%(운영 70%는 비관적 끝단)
  → 7월 보고서 [Sim2Real 준비] 의 sim-side 기준선을 70%가 아닌 ~82%로 보정, 남은 gap 을
  "큐브배치 커버리지(데이터 다양성)" 로 규정.

## 다음 단계
- **W1 본격 잔여**(변함없음): `sim_data_collector.py --dr` 로 DR 50ep 합성(`data/episodes_cl_dr`) →
  ACT 재학습 → 일반화 검증 (드라이버 사이클, 수 시간, COP_TARGET_EP 상향/DR 마커 트리거 시).
  오늘 결과가 이 방향(배치 다양성 증대)이 남은 gap 정공법임을 정량 확증.
- **W2**: zero-shot 실기 추론 → 격차 측정 (외부의존: 장기헌 Orin/실기 SSH). 실기 격차 계산의
  sim-side 기준선은 ~82% 사용.
