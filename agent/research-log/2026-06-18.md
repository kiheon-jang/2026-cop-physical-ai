# 시뮬 진척 — 2026-06-18 (목요일)

## 오늘 진행 단계

Phase 1 - W3 D4 — ACT 학습 status 점검 (D1~D3 사용자 수동 절차 미시행 그대로 → D4 동일 패턴 반복).

PHASE_ROADMAP W3 (6/15~6/21) 대상 항목:
- [ ] LeRobot ACT 학습 파이프라인 구성 (`scripts/train_act.py`)
- [ ] nohup 백그라운드로 epoch 100 학습 실행
- [ ] 매일 크론이 `logs/act_train.log` 마지막 줄 확인 → research-log 진행률 기록

## 실행 테스트 결과

- 상세: `research/simulation/2026-06-18_act-w3-d4-status-check.md`
- 정적 점검 (Glob 통과): `scripts/` 4종 그대로 (`build_report_pptx.py`, `train_act.py`, `start_act_train.sh`, `check_act_train.sh`). `logs/` 미존재, `checkpoints/` 미존재 — ACT 학습 미시작 재확인. `data/episodes/` Phase 0 W4 산출물 50 에피소드 그대로.
- 런타임 점검: 본 회차 0건 (sandbox 차단 16일 연속).
- PHASE_ROADMAP W3 첫 두 항목 체크박스 변경 **보류** — 런타임 통과 전까지 `[ ]` 유지 (4일차 누적).

## 관찰 / 이슈

- [자가치유] `chmod +x scripts/start_act_train.sh scripts/check_act_train.sh` 본 회차 sandbox "requires approval" 거절 — 16일 연속. 두 스크립트 `-rw-r--r--` 유지 추정. 사용자 수동 1회 필요.
- [자가치유] `/Volumes/MARK_DATA/dev/2026-cop-physical-ai/.venv/bin/python3 -c "import mujoco; ..."` 본 회차 sandbox 거절 — 16일 연속. `.venv` 심볼릭 링크 대상이 sandbox allowlist 밖.
- [자가치유] `bash scripts/check_act_train.sh` 본 회차 미시도 (chmod 미통과로 의미 없음). 학습 미시작 상태 (`logs/` 미존재, `checkpoints/` 미존재) 는 Glob 으로 간접 확인.
- [자가치유] Obsidian 미러 `cp ... /Users/markmini/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/` 본 회차 미시도 (working-dir 밖 쓰기 sandbox 차단 16일 연속 패턴). 사용자 수동 동기화 필요.
- [자가치유] `git add` / `commit` / `push` 본 회차 mutation 차단 예상 — 16일 연속. 본 회차 추가 신규 파일 2건 (`agent/research-log/2026-06-18.md` + `research/simulation/2026-06-18_act-w3-d4-status-check.md`) 미커밋 상태 누적. 6/7~6/18 합산 미커밋 약 24건.
- `agent/external-dependencies.md` Claude Code v3.2 harness allowlist 점검 블로커 (장기헌) 16일 연속 — 본 nightly 의 모든 런타임 실행 차단의 근본 원인. 신규 외부 의존성 추가 없음.

## 다음 단계

- **사용자 수동 (즉시 1회)**: `chmod +x scripts/*.sh && .venv/bin/python3 scripts/train_act.py --smoke && bash scripts/start_act_train.sh --epochs 100` 백그라운드 실행 → 그 시점에서 PHASE_ROADMAP W3 첫 두 `[ ]` → `[v]` 일괄 체크 + `git add -A && git commit -m "📊 [로그] 2026-06-07~18 누적 산출물 — sandbox 16일 연속 차단 해소" && git push origin main` 일괄 처리.
- **2026-06-19 (금) W3 D5 nightly**: 사용자 절차 통과 가정 시 `check_act_train.sh` 표준 status 블록 (`pid=N alive=yes ckpt_count=≥1 log_age_sec<86400`) 을 당일 research-log "## ACT 학습 진행률" 섹션에 append. 미통과 시 본 D4 패턴 반복.
- **2026-06-22 (월) W4 D1**: ACT 학습 완료 대기 + 시뮬 추가 200 에피소드 생성 시작 (`sim_data_collector.py`).

## 보고용 증거 후보

- [ ] `research/simulation/2026-06-18_act-w3-d4-status-check.md` — W3 D4 status 점검 + sandbox 16일 연속 블로커 누적. 6월 보고서 [2.사전학습] W3 일일 운영 절차 증거.

---

## 추가 nightly 확인 (회차 +1, sim-environment v3.2)

- 정적 재확인 (Bash `ls` 통과): `scripts/` 4종 그대로 (`build_report_pptx.py`, `train_act.py`, `start_act_train.sh -rw-r--r--`, `check_act_train.sh -rw-r--r--`). `logs/` 미존재, `checkpoints/` 미존재 — ACT 학습 미시작 17일 연속.
- [자가치유] `/Volumes/MARK_DATA/dev/2026-cop-physical-ai/.venv/bin/python3 -c "import mujoco; ..."` 본 회차 sandbox "requires approval" 거절 — 17일 연속. `.venv` 심볼릭 링크 대상 (`~/.local/share/uv/python/cpython-3.14.0-macos-aarch64-none/`) sandbox allowlist 밖. 런타임 메트릭 0건.
- [자가치유] `git add agent/research-log/2026-06-18.md` 본 회차 sandbox 거절 — 17일 연속 mutation 차단. 본 회차 신규 산출물 (본 append 블록) git 미반영.
- [자가치유] Obsidian 미러 본 회차 미시도 — working-dir 밖 쓰기 sandbox 차단 패턴 동일.
- 정적 확인 (git status 통과 read-only): 워킹트리 6/7~6/18 미커밋 누적 — research-log 12건, 시뮬 리포트 12건, scripts/ 3건, daily-reports 11건, 수정 4종. 약 42건 누적.
- 근본 원인 `agent/external-dependencies.md` Claude Code v3.2 harness allowlist 블로커 (장기헌) 17일 연속. 신규 외부 의존성 없음. 사용자 수동 1회 일괄 처리 권고 그대로 유효.

---

## 23:30 sim-test 회차 (sim-test v3.2)

### 실행 테스트 결과

우선순위 4종 (sim_headless_6dof_video / sim_camera_verification / sim_pick_place / sim_data_collector) 모두 **실행 0회**.

- 시도: `/Volumes/MARK_DATA/dev/2026-cop-physical-ai/.venv/bin/python3 samples/training/sim_headless_6dof_video.py --help` → sandbox "requires approval" 거절. `.venv` 심볼릭 링크 대상이 working-dir 밖이라 동일 패턴 (16일 연속, 본 회차 +1).
- 정적 점검 (Glob 통과): 4종 priority 스크립트 파일 그대로 존재. 23:00 status 점검 산출물 1건 (`research/simulation/2026-06-18_act-w3-d4-status-check.md`) 확인.

### 메트릭

- 시뮬 동작 성공률: **N/A** (런타임 0건)
- 추론 속도: **N/A**
- CPU/메모리: **N/A**
- 학습 손실: **N/A** (`logs/` 미존재)

### 관찰 / 이슈

- [자가치유] sim-test 우선순위 4종 모두 `.venv/bin/python3` 호출 sandbox 차단 — 23:00 회차 동일 블로커. 사용자 수동 1회 (`chmod +x scripts/*.sh && bash scripts/start_act_train.sh ...`) 후 다음 cron 부터 가능.
- [자가치유] `git add -A && git commit -m "📊 [로그] 2026-06-18 ..." && git push origin main` 본 회차 mutation 시도 예정 — sandbox 통과 시 누적 미커밋 약 42건 일괄 정리. 거절 시 18일 연속 누적.
- Obsidian 미러 (`cp ... 00_AI_Wiki/CoP_PhysicalAI/2026-06/`) 미시도 — working-dir 밖 쓰기 sandbox 차단 패턴.

### 다음 단계

- 6/19 (금) W3 D5: 사용자 수동 절차 통과 시 `logs/act_train.log` 마지막 줄 + `checkpoints/` 카운트를 본 sim-test 회차 메트릭으로 기록.

### 보고용 증거 후보

- [ ] 본 append 블록 — 23:30 sim-test 회차 0건 실행 + sandbox 16일 연속. 6월 보고서 [2.사전학습] W3 nightly 운영 증거 (status 점검과 동일 블로커 사유).
