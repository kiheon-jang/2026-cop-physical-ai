# 시뮬 진척 — 2026-07-06 (월요일)

## 오늘 진행 단계
Phase 2 - W1 - **DR 재학습 대기 / STAGE=완료·유지 + 무결성 전수 감사**.
어제(7/5) DR 50ep 합성 완료 후, 재학습·비교(W1 잔여 절반)는 드라이버 사이클 미트리거로 대기.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(01:00): **STAGE=완료/유지 · 데이터 50ep · 최종 성공률=0.7 (목표 0.90)**.
  새 사이클 미트리거 → 운영 수집/학습/측정 재실행 없음.

## 실행 테스트 결과
- **무결성 전수 감사(비파괴)**: 데이터·측정·마커·DR 산출물 8종 회귀/오염 0 확인.
  | 항목 | 값 | 판정 |
  |---|---|---|
  | 운영 `episodes_cl` | 50 ep / 3350 frame | 불변 |
  | DR `episodes_cl_dr` | 50 ep / 3350 frame | 불변 |
  | 운영 `rollout_summary.json` | 0.70 · 43.7mm · 7/10 · md5 `70484a5c…` | 불변(7/4 복원값 일치) |
  | 마커 measured/trained | 6/25·6/24 mtime | 불변 |
- DR 프록시/seed 요약 전부 보존: dr 0.80 / dr_{light,friction,camera} 0.70·0.70·0.70 /
  seed{7,123,2026} 0.90·0.80·0.90 → 4-seed 공정추정 ≈0.80~0.83(7/4 결론 유지).

## 관찰 / 이슈
- **DR 재학습 미트리거 지속(7/4~7/6)**: 데이터(DR 50ep)는 준비 완료됐으나 드라이버 새 사이클
  트리거 조건(마커 삭제 / `COP_TARGET_EP` 상향 / `COP_DATASET_ROOT=episodes_cl_dr` 전환)이
  아직 세팅 안 됨. 야간 에이전트는 하드룰(수집/학습/측정 재실행 금지)상 직접 실행 안 함 →
  드라이버 설정 조정이 다음 사이클 관문. 표면화해 둠(블로커는 아님, 트리거 결손).
- [자가치유] 없음 — 어제 research-log·로드맵 최신, 드라이버 STAGE 정상, 마커/산출물/운영데이터 정합, 에러 없음.

## 다음 단계
- **W1 잔여 절반**: `COP_DATASET_ROOT=data/episodes_cl_dr` 재학습 → DR-trained rollout 비교
  (드라이버 사이클, 수 시간). 목표 seed42 0.70 → 배치커버리지 확장으로 공정추정 ~0.82↑ 실증.
- **W2 (7/8~)**: zero-shot 실기 추론 → 격차 측정(외부의존: 장기헌 Orin/실기 SSH). sim-side 기준선 ~82%.

---

## 🌙 23:00 야간 업데이트 — DR 재학습 진행중 (STAGE=학습중)

**위 01:02 '다음 단계'가 주간(13:31)에 실제로 실행됨.** 적대적 감사(19건 확정, critical 6건 수정)
후 DR 재학습이 트리거되어 23:00 시점 **학습중**이다.

### 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): **STAGE=학습중 · 타겟=episodes_cl_dr → checkpoints/act_cl_dr**.
  pid=74621 alive, ckpt_latest=epoch_0089(9개), last `{"epoch":99,"step":310,"loss":0.00498}`.

### 실측 (23:00)
- pid 74621 alive(`pgrep -f train_act`), train_start 배너 = episodes_cl_dr / act_cl_dr / 100epoch / 13:31:06.
- **epoch 99/100 마지막 epoch 진행중** — ETA(주간 ~22:30) 대비 소폭 지연. epoch_0099 는 종료 시 저장.
- loss 0.00498(l1 0.00398/kl 0.000101) 정상 수렴(6/25 CL 0.00456 동급).

### 무결성 (격리 유지, 비파괴)
- 운영 `rollout_summary.json` = `checkpoints/act/epoch_0099` 0.70/43.7mm **불변**(DR 측정 미실행).
- baseline 아카이브 `rollout_summary_baseline_cl.json` 0.70 보존. `episodes_cl`/`episodes_cl_dr` 각 50ep 불변.
- 마커 `cop_trained_on.marker`(구형식 1782287560) **미승격** + `.pending`=`episodes_cl_dr:1783181837` 대기중
  → 학습 미완이므로 정상. `cop_dataset_target`=episodes_cl_dr, `.next`=episodes_floor(주간 예약, 무관).
- **핵심**: 주간 감사의 ckpt 격리(act_cl_dr)+마커 2단계+baseline 아카이브 덕분에 학습중임에도
  운영 baseline(0.70) 산출물 무손상. 6 critical 수정 전이었다면 이미 파괴됐을 시점.

### 관찰
- STAGE=학습중은 정상(블로커 아님). 드라이버가 학습 미완을 인식해 측정을 **보류**(6/22 SILENT 멈춤 반대).
- [자가치유] 없음 — 로그/마커/데이터/운영산출물 전부 정합, 에러 없음.

### 다음 단계 (갱신)
- **다음 크론 사이클**: 학습 완료 → stage 2.5 완료검증 후 `.pending`→마커 승격 → stage 5 가
  `checkpoints/act_cl_dr/epoch_0099` 측정 → 새 `rollout_summary.json`(DR-trained) + history + traj 덤프.
- **측정 후**: DR-trained vs baseline 0.70 비교(n=10 단일시드 ±0.1 판별불가 → `--summary-suffix _seed{7,123,2026}`
  다중시드 공정추정). 이후 `.next=episodes_floor`(바닥 파지) 사이클.
- 상세: `research/simulation/2026-07-06_phase2-w1-dr-retrain-inflight.md`.

---

## 🌗 23:30 야간 sim-test — DR 재학습 **완료 감지**(측정은 다음 사이클) + 우선순위 4종 회귀

23:00 스냅샷(STAGE=학습중)에서 **약 2분 뒤 학습이 종료됨**을 23:30 sim-test 크론이 포착.
`checkpoints/act_cl_dr/epoch_0099/model.safetensors` mtime **23:02:36**, pid 74621 종료(`pgrep -f train_act` 없음),
`logs/act_train.log` = "학습 100 epoch 완료" (wall_clock **34291s ≈ 9.5h**, final loss 0.00498).
드라이버 stage 2.5 승격·stage 5 측정은 **다음 크론 사이클(내일 23:00)** 담당 — 설계대로 야간 sim-test는 재학습/측정 재실행 금지.

### 실행 테스트 결과 (우선순위 4종)
#### 스크립트: sim_headless_6dof_video.py
- 반복: 1회 — PASS. 2501프레임 렌더 → `research/simulation/video/sim_6dof_animation.mp4` 저장.
#### 스크립트: sim_camera_verification.py
- 반복: 1회 — PASS. overhead(640×480)+gripper(320×240) 2카메라 30프레임 동기 캡처.
#### 스크립트: sim_pick_place.py
- 반복: 3회 — 결정론적 0/3 (status=fail, min_approach_dist 0.3228m, max_lift 0.0mm).
- **정상 기대치**: legacy open-loop 고정포즈 데모(실 grasp 성능 아님). 실 성능은 closed-loop 70%(`_grasp_closedloop.py`).
  3회 완전 동일값 = 결정론 확인, 회귀 아님.
#### 스크립트: sim_data_collector.py
- 반복: 2ep 스모크(throwaway `data/episodes_smoke_test`, seed 42, 실행 후 삭제, 운영 무접촉).
- yield **2/2 = 100%**, lift 41.5/42.0mm, 67frame/ep, grasp재시도 1 — closed-loop expert 정상.

### 메트릭
- 학습 완료: 100 epoch, wall_clock 34291s(≈9.5h), final loss 0.00498(l1 0.00398/kl 0.000101).
- 수집기 yield: 100%(2/2), median lift ~41.8mm.
- 시뮬 렌더: 6dof 2501f / camera 60f(2×30) 정상.
- CPU/메모리: 스크립트 정상 완료(OOM·에러 없음, Apple Silicon MPS).

### 무결성 전수 확인 (비파괴 — 격리 유지)
| 항목 | 값 | 판정 |
|---|---|---|
| `episodes_cl` | 50ep / 3350frame | 불변 |
| `episodes_cl_dr` | 50ep / 3350frame | 불변(학습 입력원, 무접촉) |
| 운영 `rollout_summary.json` | 0.70 / 43.7mm, ckpt=`checkpoints/act/epoch_0099` (mtime 7/4) | **불변**(DR 측정 미실행) |
| baseline 아카이브 `rollout_summary_baseline_cl.json` | 0.70 / 43.7mm | 보존 |
| 마커 `cop_trained_on.marker` | 1782287560(구형식) | **미승격**(학습완료 직후, 다음 사이클 stage2.5 담당) |
| `.pending` | `episodes_cl_dr:1783181837` | 대기중 |

- **핵심**: 학습이 끝났어도 ckpt 격리(`act_cl_dr`)+마커 2단계+baseline 아카이브 덕에 운영 baseline(0.70) 무손상.
  측정은 드라이버 소관이라 야간 sim-test는 상태 포착만 하고 승격/측정하지 않음.

### 관찰 / 이슈
- **DR 재학습 완료**: 23:00 "학습중" → 23:02 종료. 다음 23:00 크론이 승격+측정 시 DR-trained rollout 첫 산출 예상.
- sim_pick_place 0/3 은 legacy open-loop 결정론 기준선(회귀 아님).
- [자가치유] 없음 — 스크립트 4종 정상, 마커/데이터/운영산출물 정합, 에러 없음. 23:00 doc·로드맵 최신.

### 다음 단계
- **내일 23:00 드라이버 사이클**: `.pending`→마커 승격 → `checkpoints/act_cl_dr/epoch_0099` 측정 →
  새 `rollout_summary.json`(DR-trained) + history + `rollout_traj_latest.json`. baseline 은 archive 보존.
- 측정 후 DR-trained vs baseline 0.70 비교(n=10 단일시드 ±0.1 판별불가 → `--summary-suffix _seed{7,123,2026}` 다중시드 공정추정).
- 이후 `.next=episodes_floor`(바닥 파지) 사이클.

### 보고용 증거 후보
- [x] DR 재학습 완료(100epoch, loss 0.00498, wall_clock 9.5h) → 7월 보고서 [Sim2Real 준비 / DR 재학습] 섹션
- [x] 우선순위 4종 sim-test 회귀(수집 100% yield, 렌더 정상, 운영 무결성) → 7월 보고서 [진척] 섹션
