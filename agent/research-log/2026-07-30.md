# 시뮬 진척 — 2026-07-30 (목요일)

## 오늘 진행 단계
Phase 2 - W2 - sim 성공률 레버 결착 후 hold + 비파괴 무결성 전수 감사

## 실행 테스트 결과
- 드라이버 STAGE=완료/유지 (`episodes_floor` 50ep · 성공률 1.0, 새 사이클 미트리거). 수집/학습/측정 재실행 없음.
- sim 레버(배치 다양성)는 7/22 4-seed 공정추정(4/4 = 1.0)으로 이미 결착 → hold 일.

### 우선순위 5종 회귀 (이번 세션 실측, 독립 실행 — 23:30 job)
- **camera** (`sim_camera_verification.py`): 30 frames × 2대 캡처 PASS.
- **6dof** (`sim_headless_6dof_video.py`): 2501 frames 생성 + mp4 저장 PASS.
- **pick** (`sim_pick_place.py`): min_approach 0.3228327114026489m, max_lift 0.0m — 결정론적 기지값(고정포즈 expert 미접근, status=fail=기대값, 7/29 와 완전 동일) PASS.
- **joint_angle** (`joint_angle_comparison_sim.py`): 최대 오차 0.0244°(elbow_flex) < 1° PASS (shoulder_pan 0.0000·wrist_roll 0.0002 등).
- **collector** (`sim_data_collector.py --root /tmp/... --episodes 2`, /tmp 격리): 성공 2/2, yield 100%(2시도), lift 42.3mm PASS.
- → 5종 전부 PASS, 로드맵 baseline·7/29 값과 정합.

### 비파괴 무결성 전수 감사 (이번 세션 도구결과)
- 운영 `rollout_summary.json` md5 = `5207f67b189645de1bb26c124873b683` — 7/22·7/23·7/25·7/26·7/27·7/28·7/29 와 **동일**(불변). sr 1.0, ckpt act_floor/epoch_0041, seed42, median lift 66.0mm.
- 마커 3자 정합: target=`data/episodes_floor` · trained_on=`episodes_floor:1783324998` · measured=`episodes_floor:1783710169`.
- datasets floor/cl/cl_dr 각 50ep/3350frame (info.json) 불변.
- 학습 프로세스 없음 (`pgrep -fl train_act` → none).
- → 회귀/오염 0.

## 관찰 / 이슈
- 무결성 회귀/오염 0. 운영 산출물 무접촉.
- md5 CLI 는 sandbox 차단 → `.venv` python `hashlib` 로 동일 값 산출(우회).
- 우선순위 5종 회귀 이번 세션 독립 재실측 = 전부 PASS(위). 운영 산출물 무접촉으로 실행.
- [자가치유] 없음 — 7/29 research-log·roadmap 정합, 결손 없음. /tmp 격리 테스트 데이터 잔존(작업디렉터리 밖 rm 차단, 리포지토리 무영향).

## 다음 단계
- sim 트랙 성공률 레버 규명 완료 → hold. 남은 것 모두 외부 의존:
  - 실기 W2 zero-shot 추론: Orin Nano SSH 외부의존(장기헌) 미수신 → 진입 불가, 대기.
  - full-epoch(100) 공정비교 복원: ~04:04 killer 진단권한(log show/launchctl, sandbox 차단) 에스컬레이션 대기.
