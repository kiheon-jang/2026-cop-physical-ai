# 시뮬 진척 — 2026-07-08 (수요일)

## 오늘 진행 단계
Phase 2 - W2 진입 — 드라이버가 W1(episodes_cl_dr) 사이클을 **STAGE=완료/유지(0.7)** 로 닫고,
7/7 W1 결론이 지목한 다음 레버 = **배치 다양성(`.next=episodes_floor`, 바닥/받침대 없는 파지)**
사이클로 전환 → ACT 재학습 착수(진행중, pid 94316).

> W2 dated 항목(zero-shot 실기 추론)은 **Orin/실기 SSH 외부의존 미수신** 으로 실기 진입 불가 →
> sim track 이 W1 결론에 따라 배치 커버리지 확장(floor) 실험으로 전진.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00):
  - 1차: 타겟 `episodes_cl_dr`→`act_cl_dr` **STAGE=완료/유지** 50ep·성공률 0.7 → **예약 사이클 전환**.
  - 2차: 타겟 `episodes_floor`→`act_floor` **STAGE=학습시작** — 50ep 로 ACT 100epoch 재학습.
  - `start_act_train`: **pid 94316**, args=`--epochs 100 --no-resume`, log=`logs/act_train.log`.

## 실행 테스트 결과 (야간 에이전트, 비파괴 관찰만)
- **학습 alive**: pid 94316 = `train_act.py --epochs 100`. train_start: dataset_root=`episodes_floor`,
  ckpt=`checkpoints/act_floor`, resume_from=null, ts `2026-07-08T23:00:11`. 현 **epoch 0 step 10 loss 25.5**
  (정상 초기값). ETA ~익일 새벽(직전 사이클 34291s≈9.5h 참조).
- **floor 데이터 실재**: `data/episodes_floor` 50ep/3350frame. 수집 로그(7/6 17:03): **50/50, yield 98%**,
  cube 배치 x≈0.11~0.15/y≈-0.007~0.018(다양성), lift 56~64mm. 7/6 주간 합성(commit db680ac/ce3d0f4) 후 대기 → 오늘 학습만 트리거.
- **마커 2단계 격리 정상**: target=`episodes_floor` · marker=`episodes_cl_dr:1783181837`(직전 승격값 유지,
  운영 rollout_summary 불변) · pending=`episodes_floor:1783324998`(신규 대기). 6/22 SILENT 반대·설계대로 baseline 무손상.
- **체크포인트 격리**: `act`·`act_cl_dr`·`act_floor` 3자 분리, 상호 파괴 없음.

## 관찰 / 이슈
- **floor = 배치 다양성 레버**: W1(7/1~7/7)이 프록시 4종+DR 실모델로 병목=섭동강건성 아닌 **배치
  커버리지(모방격차)** 확증(DR 축은 천장 0.825↔0.800 못 올림). floor 씬은 받침대 제거로 큐브 배치
  분포를 넓힘 → 학습 배치 다양성↑ = 7/7 정공법의 첫 실측 준비.
- **로드맵 정합**: W2 dated 실기 스텝은 Orin SSH 미수신으로 진입 불가 → floor sim 사이클로 전진(대체
  아님, W1 결론을 잇는 드라이버 `.next` sim 사이클). 실기 track 은 SSH 수신 시 재개.
- **[자가치유] 없음** — 7/7 log·로드맵 최신, 드라이버 STAGE 정상(완료→전환→학습시작), 마커 격리 정합,
  데이터셋 실재, 학습 alive, 에러 없음.

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **학습 완료 시**: pending 승격 → `act_floor/epoch_0099` 측정 → floor-trained rollout 산출,
  baseline 은 `rollout_summary_baseline_cl.json` 계열 보존.
- **비교 검증(향후)**: floor-trained 를 동일 4 seed(42/7/123/2026) 공정추정으로 baseline·DR-trained 와
  비교. **핵심 질문**: 배치 다양성이 성공률 천장(0.80~0.825)을 올리는가, 실패 배치가 이동/축소되는가.
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.

## 보고용 증거 후보
- [ ] W1 종료 → 배치 다양성(floor) 사이클 전환 = "DR 축 아닌 배치 커버리지가 다음 레버" 처방 실행 → 7월 보고서 [2.1] 다음 레버
- [ ] floor 데이터 50/50 yield 98%(배치 x0.11~0.15) → 7월 보고서 [2.2] 데이터 파이프라인 배치 확장
- [ ] 마커 2단계+ckpt 3자 격리로 학습중 baseline 무손상(무결성 설계 작동) → 7월 보고서 [2.2] 파이프라인 견고성
