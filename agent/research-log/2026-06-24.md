# 시뮬 진척 — 2026-06-24 (수요일)

## 오늘 진행 단계

Phase 1 - W4 - **closed-loop 자동수집 1사이클 완주** (PHASE_ROADMAP L162, 6/22~6/30 범위 첫 `[ ]`).
야간 전진은 결정론적 드라이버 `scripts/cop_pipeline_advance.sh` 가 수집→학습→측정을 처리했고,
본 회차는 STAGE 결과를 문서화·동기화·보고했다(파이프라인 재실행 없음).

## 실행 테스트 결과

23:00 STAGE=**학습중**.

- **수집 ✓**: closed-loop expert 로 50 에피소드 → `data/episodes_cl` (3350 frame, 30fps, so101, LeRobot v3.0). 16:39~16:52 완료(`logs/cop_data_collect.log`).
- **학습 진행중**: ACT 재학습 pid **20078** alive, `COP_DATASET_ROOT=data/episodes_cl`. **epoch 64/100**, loss 0.00767(l1 0.00505, kld 0.000262), ~322s/epoch(MPS), loss 단조감소 정상. 로그 mtime 23:01 활성. ETA ~02:00.
- **측정 대기**: `rollout_summary.json` = checkpoint epoch_0009 / scene_grasp_pads / **rollouts 2** / success 0/2 / median_lift 6.4mm. 이는 드라이버 측정(--rollouts 10)이 아니라 commit `16d2548`(render_act_rollout closed-loop 정합)의 **2-rollout 정합 스모크**이며, 대상 epoch_0009 는 6/21 open-loop baseline 체크포인트 → 0% 는 예상된 baseline. closed-loop 모델 유효 측정은 학습 완료 후 드라이버가 자동 수행.

---

## 23:30 sim-test 에이전트 — 스크립트 스모크 실행 결과

23:00 산출물 검증 후, headless 우선타깃 스크립트를 실제 실행해 동작 무결성을 측정.
스크립트들은 결정론적(시드 변동 없음)이라 1회 실행이 N회와 동일 → 대표 1~2회 실행.
**`sim_data_collector.py` 는 의도적으로 실행하지 않음** — 활성 파이프라인 드라이버
(`cop_pipeline_advance.sh`)가 소유한 closed-loop 수집기로, 학습 진행 중(pid 20078) 재실행 시
`data/episodes_cl` 1사이클 데이터를 오염시킴. `sim_viewer_6dof.py` 는 viewer(headless 비호환) 스킵.

### 스크립트: sim_headless_6dof_video.py
- 반복: 1회 (결정론적)
- 결과: ✅ 정상. 6관절(shoulder_pan/lift, elbow_flex, wrist_flex/roll, gripper) 사인파 구동,
  2501 frame 렌더 → `sim_6dof_animation.mp4` 저장 (5s @ 30fps, 500Hz sim step).

### 스크립트: sim_camera_verification.py
- 반복: 1회 (결정론적)
- 결과: ✅ 정상. 듀얼 카메라(overhead 640×480 + gripper 320×240) 동시 캡처 30 frame, PNG 저장.

### 스크립트: sim_pick_place.py
- 반복: 1회 (결정론적)
- 결과: ⚠️ 실행 정상 / **grasp 실패 (exit 1)** — `min_approach_dist=0.323m`(큐브에 32cm 못 미침),
  `max_lift=0mm`, approach_ok=false, lift_ok=false. **이것은 회귀가 아니라 기존 규명된 open-loop
  expert 결함**(grasp-rootcause, PHASE_ROADMAP L142~151)의 재확인이다. 이 legacy 스크립트는
  방향제어 없는 고정 관절 시퀀스라 0% 가 정상값이며, closed-loop 재학습(현재 진행 중)이 그 해법.

### 시스템 메트릭 (실행 시점)
- 메모리: 83.7% 사용 / 2.8GB 가용 / 16GB(17.2GB 리포트). ACT 학습(MPS) + 렌더 동시 부하. 정상 범위.
- 학습 진척(23:30 시점): **epoch 69/100**, loss 0.00683 (23:00 64→69 정상 전진), ~322s/epoch.
- Apple Silicon — GPU 메트릭 없음(MPS), 메모리만 측정.

### 경로 관찰 (이슈 아님)
- legacy 스크립트 3종은 `/Users/markmini/Documents/dev/2026-cop-physical-ai/...` 절대경로 하드코딩.
  해당 경로가 유효(모델·출력 디렉터리 존재)해 실행 성공 → 산출물은 Documents 트리에 기록됨.
  작업 트리(`/Volumes/MARK_DATA/...`)와 별개 경로지만 현재 동작에는 무해. 참고로만 기록.

## 관찰 / 이슈

- **6/23 계획(평행조 `scene_grasp_parallel.xml`) 은 폐기·대체됨**: 메모리 grasp-rootcause-correction 대로 평행조 불필요(스톡 단일조 + closed-loop 로 88%/75% 해결). 6/24 로드맵이 closed-loop 자동수집 트랙으로 갱신됨 — 평행조 작성 단계는 진행하지 않음.
- **측정-체크포인트 정합 주의**: `render_act_rollout.find_latest_checkpoint()` 는 mtime 최신을 잡는다. 현재 `checkpoints/act/` 최신은 epoch_0099(6/22 open-loop)이고 신규 closed-loop 학습은 별도 진행 중이라, 학습 완료 전 측정은 항상 baseline 을 잡는다. 학습 완료 후 새 모델 서명(MEASURED_MARK 불일치)으로 측정이 재트리거되어야 유효. → 본 회차는 baseline 임을 명시 기록만, 측정 강제 재실행 안 함(드라이버 담당).
- [자가치유] 없음 — 수집/학습 정상 진행, 산출물 무결, 에러 없음. 측정 baseline 건은 드라이버 정상 동작 범위라 self-heal 대상 아님(문서로 명확화).

## 다음 단계

- **2026-06-25**: 학습 완료(~02:00) 확인 → 드라이버 측정 스테이지가 신규 closed-loop 모델 10-rollout 측정 → `rollout_summary.json` 갱신. 유효 Pick 성공률 확보 시 L162 `[v]` 클로즈 + Phase 1 W4 완료 기준(🔄) 닫기.
- **외부 의존**: Orin Nano SSH(장기헌, external-dependencies.md) 미수신 → 배포 단계 이연 유지.

## 보고용 증거 후보
- [x] headless 시뮬 환경 무결성(6-DoF 비디오 + 듀얼 카메라 캡처 동시 동작) 23:30 실행 재확인 → 6월 보고서 [환경구축/텔레오퍼레이션] 섹션
- [ ] open-loop `sim_pick_place.py` 0% (32cm 미달) vs closed-loop 재학습 — open vs closed-loop grasp 대비 증거 → 6월 보고서 [2.사전학습] 섹션 (학습 완료 후 closed-loop rollout 성공률과 짝지어 보고)
