# 시뮬 진척 — 2026-07-05 (일요일)

## 오늘 진행 단계
Phase 2 - W1 - **DR 50ep 데이터셋 합성 완료**: 지난 4일(7/1~7/4)의 비파괴 프록시 검증이
가리킨 "본격 잔여(DR 50ep 합성→재학습→비교)"의 **첫 절반(합성)을 실제로 달성**.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`: **STAGE=완료/유지 · 데이터 50ep · 최종 성공률=0.7 (목표 0.90)**.
  새 사이클 미트리거 → 운영 파이프라인 수집/학습/측정 재실행 없음.

## 실행 테스트 결과
- **DR 50ep 합성**: closed-loop expert(`sim_data_collector.py`) + `--dr`(조명/마찰/카메라노이즈
  3축, 매 에피소드 reset 훅 무작위화) 로 `data/episodes_cl_dr` 신규 생성.
  - **50 에피소드 / 3350 프레임** (운영 `episodes_cl` 과 동일 규모).
  - 수집 로그(`logs/dr_collect_50ep.log`, 01:02~01:17): **성공 50/50 · 시도 58회 · yield 86%**,
    lift 40.2~45.3mm(전부 40mm 필터 통과).
  - **DR 실인가 증거**: ep별 파라미터 이동 — friction_scale 0.757~0.89, light/camera_noise 변동.

- **무결성 3자 정합(격리 확인)**:
  | 항목 | 값 | 판정 |
  |---|---|---|
  | DR `episodes_cl_dr` | 50 ep / 3350 frame | 신규·정상 |
  | 운영 `episodes_cl` | 50 ep / 3350 frame | 불변 |
  | 운영 `rollout_summary.json` | 0.70 · 43.7mm | 불변 |
  | 마커 measured/trained | 6/25·6/24 mtime | 불변 |
  - DR 수집이 **별도 데이터셋 루트**로 격리되어 운영 데이터/측정치/학습마커를 일절 건드리지 않음.

## 실행 회귀 테스트 (23:30 sim-test 에이전트, 우선순위 4종)
> 오늘 산출물 스크립트를 직접 실행해 회귀 없음을 검증. mujoco 3.8.0 / .venv.

| 스크립트 | 반복 | 결과 | 비고 |
|---|---|---|---|
| `sim_headless_6dof_video.py` | 1 | **PASS** — 2501 frames, mp4 저장 | 6축 사인동작 정상 |
| `sim_camera_verification.py` | 1 | **PASS** — 30 frames × 2 cam (overhead/gripper) | 오프스크린 렌더 정상 |
| `sim_pick_place.py` | 3 | **0/3 (open-loop 알려진 baseline)** | 결정론적 동일값 min_approach=0.3228m, lift 0mm. 회귀 아님(open-loop expert 한계 — closed-loop 이 해법) |
| `sim_data_collector.py` (CL smoke) | 2 ep | **2/2 · yield 100% · lift 42.5mm** | throwaway root `data/_smoke_20260705`(실행 후 삭제), 운영 `episodes_cl` 무접촉 |

- **운영 무결성 재확인**: `episodes_cl` 50/3350 · `episodes_cl_dr` 50/3350 · 운영 `rollout_summary.json` 0.70/43.7mm — 전부 불변.
- pick_place 0/3 은 6/22 이후 문서화된 open-loop 결정론적 실패(0.3228m 고정)로 오늘도 동일 → 회귀 없음.

## 관찰 / 이슈
- yield 86%(운영 91%와 동급) → DR 인가가 grasp 성공을 무너뜨리지 않음 = 7/2~7/3 강건성 결론과 정합.
- 4종 회귀 전부 예상대로(6dof/camera PASS, pick_place open-loop 0/3 baseline, data_collector CL 2/2). 신규 결함/회귀 없음.
- **재학습·비교는 오늘 실행하지 않음**: `train_act` 재학습·rollout 재측정은 **파이프라인 학습/측정
  단계**이므로 하드룰(수집/학습/측정 재실행 금지, 드라이버 담당)에 따라 야간 에이전트가 실행하지 않음.
  다음 드라이버 사이클(DR 마커 설정 또는 `COP_DATASET_ROOT=episodes_cl_dr` 전환)에서 수행.
- [자가치유] 없음 — 어제 research-log·로드맵 최신, 드라이버 STAGE 정상, 마커/산출물/운영데이터 정합, 에러 없음.

## 보고용 증거 후보
- [x] DR 데이터셋 50ep 합성 완료(3350frame, yield 86%) → 7월 보고서 [Sim2Real 준비]:
  "데이터 다양성 증대(Domain Randomization)로 큐브배치 커버리지 확장" 트랙의 데이터 산출물 근거.

## 다음 단계
- **W1 남은 절반(재학습·비교)**: `COP_DATASET_ROOT=data/episodes_cl_dr` 로 ACT 재학습 →
  DR-trained rollout 측정 → non-DR 대비 일반화 비교(드라이버 사이클, 수 시간). 목표: 헤드라인
  seed42 0.70 → 배치커버리지 확장으로 공정추정 ~0.82↑ 실증.
- **W2 (7/8~)**: zero-shot 실기 추론 → 격차 측정(외부의존: 장기헌 Orin/실기 SSH). sim-side 기준선 ~82%.
