# 시뮬 진척 — 2026-08-09 (일요일)

## 오늘 진행 단계
Phase 3 - W3 - S1 리셋버튼 완주 후 hold + 무결성 감사 재확인.
dated 항목(W1~W3) 전부 완료, 다음 pending = W4(8/26~) omen 핸드오프(외부 의존, 진입 불가).

## 실행 테스트 결과
- 드라이버 STAGE=완료/유지: 수집/학습/측정 재실행 없음 (`episodes_s1` 100ep · 성공률 0.925).
- **무결성 전수 감사(비파괴)**:
  - 마커 3자 정합 (8/8 값과 바이트 일치): target=`data/episodes_s1` ·
    trained_on=`episodes_s1:1785931493` · measured=`episodes_s1:1786060554`.
  - 학습 프로세스 없음 (`pgrep -fl train_act` → none).
  - 운영 rollout `rollout_summary_s1.json` md5 `fbeef25775e5846ba2e3ce887afd1929`
    → 8/8 대비 불변. seed42 9/10=0.90, 4-seed(42/7/123/2026) 평균 0.925(37/40).
  - 데이터셋 불변: episodes_s1 100ep/7231f · episodes_floor 50ep/3350f · episodes_cl 50ep/3350f.
- 문서: `research/simulation/2026-08-09_phase3-w3-s1-hold-integrity-audit.md`(+Obsidian 미러).

## 실행 테스트 결과 (23:30 nightly sim-test 에이전트)
오늘 신규/변경 sample 스크립트 없음(전부 5~8/7 dated). priority 렌더 3종 헤드리스 스모크 실행.
⚠️ 세 스크립트 모두 레거시 하드코딩 경로 `/Users/markmini/Documents/dev/...`(별도 리포 사본, 실존) 대상 → **운영 Volumes 데이터 무접촉**. MuJoCo 3.8.0.

### sim_headless_6dof_video.py — PASS
- 반복 1회(결정론적). 생성 프레임 **2501**(5s), `sim_6dof_animation.mp4` 저장. 크래시 0.

### sim_camera_verification.py — PASS
- 반복 1회. 캡처 **30프레임**(top 640×480 + gripper 320×240 동시 2-cam). 크래시 0.

### sim_pick_place.py — 실행 PASS / 파지 fail (기지·회귀 아님)
- 반복 1회(open-loop 레거시 데모). status=fail(approach_ok=false, lift_ok=false),
  min_approach_dist **0.323m**(임계 0.005), max_lift **0.0m**.
- 해석: open-loop 고정포즈 데모(0% 파지) — Phase 1 closed-loop(70%)·floor(1.0)·S1(0.925)로 대체된 레거시. 성공률 트랙 아님.

- sim_data_collector.py **미실행**(SKIP): 에피소드 write → hold 일 오염 리스크. 무결성 우선.

## 관찰 / 이슈
- 렌더 파이프라인 헬시(3종 크래시 0). 운영 md5 `fbeef25…` 독립 재확인 불변 · `cop_dataset_target`=episodes_s1 · train_act proc 없음 → 회귀/오염 0.
- 레거시 스크립트 경로 이원화(Documents 사본) — 정리 후보, 블로커 아님.
- 회귀/오염 0. Phase 3 완료 기준 성공률(≥0.70) 이미 0.925 충족. 남은 기준 = 데이터셋 omen 로드(W4 외부).
- sandbox `md5` 차단 → `.venv hashlib.md5`로 우회 산출(값 불변 확인).
- [자가치유] 없음. 8/8 research-log · 로드맵 동기 정상.

## 다음 단계
- W4(8/26~) sim2real omen 핸드오프: `act_s1_sim/epoch_0029` + `episodes_s1` 전달, LED ROI 캘리브 지원,
  Sim2Real 격차 보고(S1 정책 DR 민감도 aggregate ~0.45 포함).
- W2 잔여 omen lerobot 0.6.1 로드 스모크(외부 의존, W4 병합 가능).
