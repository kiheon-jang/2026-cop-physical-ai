# 시뮬 진척 — 2026-09-14 (월요일)

## 오늘 진행 단계
Phase 4 - W2 - RS232 ACT **재측정 hold** (`checkpoints/act_rs232_sim/epoch_0041`, 학습 완주 상태 유지).

## 실행 테스트 결과

### RS232 rollout 4-seed 재측정 (`epoch_0041`, N=10/seed, device=cpu)
드라이버가 23:01~23:05 4-seed 재측정 수행 (학습 프로세스 없음 = 완주 상태). 동일 체크포인트·결정론적이라 9/13 과 수치 동일.
- 반복: 4 seed × 10 rollout = 40
- 메트릭:
  - **부분성공(≥2.95mm)**: seed42 0.9 · seed7 0.7 · seed123 0.9 · seed2026 1.0 → **fair estimate 0.875 (35/40)**
  - **완전분리(≥5.9mm)**: seed42 0.7 · seed7 0.6 · seed123 0.9 · seed2026 0.8 → **fair estimate 0.75 (30/40)**
  - median 변위: 7.99 / 8.22 / 8.52 / 10.21 mm (seed42/7/123/2026)
  - 측정 wall time: **78.7~79.3s/seed** (cpu, max_frames=240) — timeout 우려 무 (300s 여유)
- 산출물: `rollout_summary_rs232{,_seed7,_seed123,_seed2026}.json`(갱신), history 5종(`20260914-2301~2305`).

### 시뮬 스택 헬스체크 (렌더 smoke)
- `sim_camera_verification.py`: 1회 → **PASS** (top/closeup 듀얼 카메라 30프레임 캡처).
- `sim_headless_6dof_video.py`: 1회 → **PASS** (6-DoF 2501프레임 → `sim_6dof_animation.mp4`).

## 관찰 / 이슈
- **오늘 23:00 산출물 0개** — cop_sim_env.py 신규 sim 문서 없음. Phase 4 W2 가 학습 완주 후 측정 hold 단계라 신규 씬/스크립트 생성 없음 = 정상 (23:00 job 실패 아님).
- **`cop_measured.marker` stale = 야간 재측정 churn 확정 (2일 연속)**: marker=`episodes_s1:1786060554` 인데 실제 rollout 은 `act_rs232_sim` 측정 완료. trained_on=`episodes_rs232:1789126035` 와 measured 불일치 → 드라이버가 매 야간 측정 미완으로 판정 → 동일 4-seed 재측정 반복. **결정론적·~319s·baseline 무손상이라 무해하나 반복 낭비**. 9/13 관찰이 9/14 재현으로 확정 = 일회성 아닌 구조.
  - [자가치유 후보(미실행)] `cop_measured.marker` 를 `episodes_rs232:1789126035` 로 정합하면 재측정 중단. **미실행 사유**: 하드룰상 마커 직접수정 회피 + 드라이버 비교로직 미확인 상태에서 잘못 세팅 시 향후 정상 측정 억제 위험. 드라이버측(`cop_pipeline_advance.sh`) 측정완료 후 measured 승격 누락 = 근본. 관찰만 유지, 근본수정은 드라이버 소유.
- 실패 rollout seed마다 이동 (seed42{6}·seed7{4,6,7}·seed123{4}·seed2026 없음) = floor/DR 트랙 동일한 배치 커버리지(모방격차) 패턴, 정책 고정약점 아님.
- [의도적 skip] `sim_pick_place.py`/`sim_data_collector.py` 미실행 — Phase 0 레거시 grasp 트랙(비회귀 fail 기존 확인), Phase 4 RS232 와 무관.

## 다음 단계
- W2 잔여: RS232 DR 합성 + 재학습 (드라이버 새 사이클 트리거 시).
- 측정기에 rollout별 플러그 초기좌표 덤프 추가 → 근단(x<0.19) 실패 배치 분해 (9/13 이월).
- `cop_measured.marker` 정합은 드라이버 소유 — 재측정 churn 3일차 지속 시 표면화 강화.

## 보고용 증거 후보
- [ ] RS232 4-seed 공정추정 성공률 (부분 0.875 / 완전분리 0.75, epoch_0041 안정 재현) → 9월 보고서 [2.X] Phase 4 섹션
- [ ] 측정 wall time ~79s/seed (2일 연속 안정) → 운영 안정성 근거
