# 시뮬 진척 — 2026-07-11 (토요일)

## 오늘 진행 단계
Phase 2 - W2 - floor 배치다양성 재학습 4차 (**FD 누수 근본강화 = RLIMIT_NOFILE 셀프-상승**)

> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 외부의존 미수신 → 진입 불가 → floor sim 사이클 전진.
> 어제 발효한 `persistent_workers` fix 가 crash 를 ~49→59 로 밀었으나 완주 실패 → 원인을 낮은
> FD 천장으로 좁히고 근본 fix 추가.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): 어제 run(pid 39732) **이상종료 감지** → **재학습 재시도**.
  - STAGE=학습시작 → **새 run pid 56445**(`--epochs 100 --no-resume`, →`checkpoints/act_floor`).
  - shutdown 시 `resource_tracker: 21 leaked semaphore objects` 경고(직전 run 잔재 = FD/세마포어 누수 방증).

## 실행 테스트 결과 (야간 에이전트, 비파괴 관찰 + 근본 fix)
- **어제 run(pid 39732) 부검**: metrics 마지막 = **epoch 59**(loss 0.0123, 04:02),
  `epoch_0059/model.safetensors` mtime 7/11 04:02, `epoch_0069`+ 없음 →
  **epoch 59~68 사이 이상종료**. 직전(pid 21661, fix 미적용) crash ~49 → **fix 로 +10 epoch 전진(부분효과)**.
- **근본원인 재규명**: 진짜 벽 = 누수 속도가 아니라 **크론 셸이 물려준 낮은 `RLIMIT_NOFILE`
  소프트 한도(macOS 기본 256)**. persistent_workers 로도 epoch 당 소량 FD/세마포어 누적 →
  `256 / ~4 ≈ 64 epoch` 후 `OSError [Errno 24]` → crash ~59 정합.
- **[자가치유]**: `scripts/train_act.py` `main()` 진입 즉시 `_raise_fd_limit()` — 학습 프로세스가
  **부모 셸과 무관하게 자기 FD 소프트 한도를 하드(무제한)까지** 상승. surgical(헬퍼1 + main 첫줄1
  + `env.fd_limit_raised` 관측필드1), 학습 로직 무변경.
  - **검증**: ast OK · 프로세스 내 소프트 256 강제→호출→`after (unlimited, unlimited)` PASS ·
    `--dry-run` status ok + `fd_limit_raised` 노출. **발효는 다음 run**(pid 56445 는 미수정 코드
    로드 → ~59 재crash 예상 → 내일 드라이버 재시작이 수정코드 로드→완주 기대. 7/9→7/10 패턴 동일).

## 관찰 / 이슈
- **[자가치유] RLIMIT_NOFILE 셀프-상승 추가**(위). persistent_workers(7/9)로 부족했던 나머지 =
  천장 자체를 제거. 하드룰상 실행중 pid 56445 는 kill/재시작 안 함 → 드라이버가 완주 관리.
- **무결성 격리 유지**: target=`episodes_floor` · trained_on=`episodes_cl_dr:1783181837`(직전 승격값) ·
  pending=`episodes_floor:1783324998`(대기·미승격) · measured=`episodes_cl_dr:1783346557`.
  학습 미완 → 승격/측정 보류 → 운영 `rollout_summary.json`(act_cl_dr, seed42, 7/10=**0.70**,
  median lift 50.2mm, measured 2026-07-07) **불변** → baseline 무손상.
- **datasets 전수 불변**: `episodes_floor`·`episodes_cl`·`episodes_cl_dr` 각 50ep/3350frame.
  ckpt `act`·`act_cl_dr`·`act_floor` 3자 분리. pid 56445 alive(epoch 1 step 10 loss 2.13, 36→2.1 수렴).

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **완주 시**(RLIMIT-fix 적용 코드로 재시작하면 256 천장 제거 → 100epoch 도달 기대): pending 승격 →
  `act_floor/epoch_0099` 측정 → floor-trained rollout.
- **비교 검증**: floor-trained 를 4 seed(42/7/123/2026) 공정추정으로 baseline(0.825)·DR-trained(0.800)
  와 비교. 핵심질문 = 배치 다양성이 성공률 천장을 올리는가 / 실패 배치가 이동·축소되는가(7/7 W1 처방).
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.
