# 시뮬 진척 — 2026-06-11 (목요일)

## 오늘 진행 단계
Phase 1 - W3 사전 준비 D1 (정규 W3 윈도우: 6/15~6/21). 6/10 로그 "다음 단계
(6/11~12)" 항목 — smoke 경로 세부 점검 (log dir 자동 생성, MPS device 폴백 분기) — 의 본 실행.

## 실행 테스트 결과
- `scripts/train_act.py` 변경 요약 (상세: `research/simulation/2026-06-11_train-act-smoke-path.md`):
  1. shebang / docstring / `ACTTrainingConfig.dataset_root` / `checkpoint_dir` 기본값을
     `/Users/markmini/Documents/dev/...` → `/Volumes/MARK_DATA/dev/...` 로 정정.
  2. `resolve_device(prefer=None)` 헬퍼 신규 — cuda → mps → cpu 자동 선택 + MPS 선택 시
     `PYTORCH_ENABLE_MPS_FALLBACK=1` 환경변수 setdefault.
  3. `build_model(config, resume_from=None, device=None)` — device kwarg 추가, 내부 위임.
     결과 JSON 에 `"device"` 노출.
  4. CLI `--device {cpu,cuda,mps}` 추가 (기본 None=자동).
  5. `ACTTrainingConfig.log_dir` 신규 (`/Volumes/MARK_DATA/dev/.../logs`) + `train()` 진입 시
     mkdir + epoch 메트릭 `logs/act_train_metrics.jsonl` JSONL append.
  6. DataLoader `pin_memory` 를 `torch.cuda.is_available()` 분기 (MPS/CPU 워닝 제거).

## 관찰 / 이슈
- [자가치유] `.venv/bin/python3 -m py_compile scripts/train_act.py` / `.venv/bin/python3
  scripts/train_act.py --dry-run` 본 세션 sandbox 여전히 차단 (심볼릭 링크 대상이
  working dir 밖). 6/7~6/10 동일 증상 8일 연속. 정적 변경만 적용, 런타임 검증은 6/15 W3
  D1 `--smoke` 시 일괄 수행.
- [자가치유] Obsidian 미러 `cp ... /Users/markmini/Documents/second-brain/00_AI_Wiki/...`
  working dir 밖 쓰기로 차단 — 사용자 수동 동기화 필요 (6/7~6/10 동일).
- PHASE_ROADMAP W3 항목(6/15 시작) 은 사전 준비 단계 — 본 작업은 smoke 진입 사전 점검이며
  W3 정식 항목 (학습 실행) 완료가 아님. 체크박스 변경 없음.
- `agent/external-dependencies.md` Claude Code v3.2 harness allowlist 점검 블로커
  (장기헌) 8일 연속 미해결 — 본 세션 sandbox 차단의 근본 원인.

## 다음 단계
- 2026-06-12: `--smoke --device cpu` 강제 분기 + num_workers=0 fallback 보강 검토.
- 2026-06-13~14: `scripts/start_act_train.sh` wrapper 신규 (nohup + pid +
  PYTORCH_ENABLE_MPS_FALLBACK=1 명시 + 자동 resume).
- 2026-06-15 (Phase 1 W3 D1): `--smoke` 1 epoch 통과 → `--epochs 100` nohup 백그라운드
  실행, `logs/act_train.pid` 기록 → PHASE_ROADMAP W3 첫 두 항목 `[v]` 체크.

## git mutation 차단 (자가치유 기록)
- [자가치유] `git add scripts/train_act.py research/simulation/2026-06-11_*.md
  agent/research-log/2026-06-11.md` 본 세션 sandbox mutation 차단 확정 — 6/7~6/10 동일
  증상 9일 연속. commit/push 미수행. 워킹트리에 오늘 추가분 누적
  (`scripts/train_act.py`(M, 6/10 부터 미커밋), `research/simulation/2026-06-11_train-act-smoke-path.md`(??),
  `agent/research-log/2026-06-11.md`(??)).
- [자가치유] Obsidian 미러 `cp ... 00_AI_Wiki/CoP_PhysicalAI/2026-06/` 본 세션도
  "For security, Claude Code may only copy files to/from the allowed working directories
  for this session" 명시 거절. 사용자 수동 동기화 필요 (9일 연속).
- 사용자 수동 처리 (블로커 시): `git add -A` (SO-ARM100/models/SO-ARM100 submodule 제외)
  → `git commit -m "🛠 [시뮬] train_act smoke 경로 점검 (log dir + MPS fallback) — 2026-06-11"`
  → `git push origin main`.

## 보고용 증거 후보
- [ ] `scripts/train_act.py::resolve_device()` + `--device` CLI — Mac Mini M5 MPS 환경에서
      ACT 학습 안정 진입 사전 준비 증거 (6월 보고서 [2.사전학습] W3 진입).
- [ ] `logs/act_train_metrics.jsonl` 영속화 — 매일 크론이 학습 진행률을 무거운 로그 파싱
      없이 추적 가능하게 함 (W3 운영 안정성 증거).

## 23:30 nightly sim-test (3회차 — 6/11 재호출)

- [자가치유] `.venv/bin/python3 -c "import mujoco; print(mujoco.__version__)"` 본 회차 sandbox
  "This command requires approval" 거절 재확인. 6/7 부터 11일 연속.
- [자가치유] `git add agent/research-log/2026-06-11.md` 본 회차 sandbox mutation 차단 재확인.
  단일 파일 `git add` 도 거절 — 화이트리스트 미적용 확정. 11일 연속.
- [자가치유] 우선순위 스크립트 (sim_headless_6dof_video.py, sim_camera_verification.py,
  sim_pick_place.py, sim_data_collector.py) 런타임 메트릭 본 회차 0회 수집. 1·2회차와 동일.
- 파일시스템 증거 (불변): `scripts/train_act.py` 452 lines / `data/episodes/meta/info.json`
  200 ep / 12,400 frames / so101 / chunk-000/file-000.parquet 보존. `logs/`, `checkpoints/`
  미생성 정상 (`--smoke` 미실행, 6/15 D1 예정).
- `agent/external-dependencies.md` v3.2 harness allowlist 점검 블로커 (장기헌) 11일 연속
  미해결 — nightly sim-test 메트릭 수집 및 산출물 push 실패의 단일 근본 원인.
- 사용자 수동 처리 권고 (블로커 해제 시까지 매일 동일):
  `git add -A ':!SO-ARM100' ':!models/SO-ARM100'`
  → `git commit -m "🛠 [시뮬] train_act smoke 경로 점검 + 6/7~11 nightly 로그 — 2026-06-11"`
  → `git push origin main`
  → `cp agent/research-log/2026-06-11.md ~/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/`

## 23:30 nightly sim-test (2회차 — 6/11 재호출)

- [자가치유] `.venv/bin/python3 -m py_compile scripts/train_act.py` 본 회차 sandbox 차단
  ("This command requires approval" 거절) 재확인. 6/7~6/10 동일 증상 10일 연속.
- [자가치유] `git add ... && git commit && git push` 본 회차 sandbox mutation 차단 재확인.
  추가 누적분 없음 (워킹트리 1회차와 동일: `scripts/train_act.py`(??),
  `research/simulation/2026-06-11_train-act-smoke-path.md`(??),
  `agent/research-log/2026-06-11.md`(M)).
- [자가치유] Obsidian 미러 `cp research/simulation/2026-06-11_train-act-smoke-path.md
  /Users/markmini/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/` 본 회차
  "may only copy files to/from the allowed working directories" 명시 거절. 10일 연속 미수행.
- 파일시스템 증거: 변경 없음 — `scripts/train_act.py` 452 lines / `data/episodes/meta/info.json`
  200 ep / 12,400 frames 보존. `logs/`, `checkpoints/` 미생성 (`--smoke` 미실행 정상).
- `agent/external-dependencies.md` v3.2 harness allowlist 점검 블로커 (장기헌) 10일 연속
  미해결 — 본 nightly 메트릭 수집 및 git push 실패의 근본 원인.

## 23:30 nightly sim-test (1회차 — 6/11)
- [자가치유] `.venv/bin/python3 -c "import mujoco, torch; ..."` 본 세션 Bash sandbox 권한
  차단 ("This command requires approval" 거절). 6/7~6/10 동일 증상 9일 연속. 우선순위
  스크립트 (`sim_headless_6dof_video.py`, `sim_camera_verification.py`, `sim_pick_place.py`,
  `sim_data_collector.py`) 런타임 메트릭 0회 수집.
- [자가치유] Obsidian 미러 `cp agent/research-log/2026-06-11.md
  /Users/markmini/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/` 본 세션도
  "For security, Claude Code may only copy files to/from the allowed working directories
  for this session" 거절 — 9일 연속 미수행. 사용자 수동 동기화 필요.
- 파일시스템 증거 (불변 유지): `data/episodes/meta/info.json` → 200 ep / 12,400 frames /
  30 fps / so101 / chunk-000/file-000.parquet 존재. Phase 1 W1-2 합성 완료 상태 보존.
- 파일시스템 증거 (오늘 추가): `scripts/train_act.py` 416 → **452 lines** (+36, smoke 경로
  점검 6항목 반영). `logs/`, `checkpoints/` 디렉터리 미생성 — `--smoke` 미실행 정상 상태
  (6/15 D1 예정).
- `agent/external-dependencies.md` v3.2 harness allowlist 점검 블로커 (장기헌) 9일 연속
  미해결 — 23:30 nightly sim-test 런타임 메트릭 수집 불가의 근본 원인.
