# 시뮬 진척 — 2026-06-14 (일요일)

## 오늘 진행 단계

Phase 1 - W3 사전 준비 D4 (정규 W3 윈도우: 6/15~6/21). 6/13 research-log "다음 단계
(2026-06-14)" 항목 — wrapper 와 짝이 될 "다음 날 크론 진행률 체크" 로직 운영 가이드
점검 + 형식 확정. `scripts/check_act_train.sh` 신규 작성.

## 실행 테스트 결과

- `scripts/check_act_train.sh` 신규 (상세: `research/simulation/2026-06-14_check-act-train-status.md`):
  1. pid 살아있음 검증 (`kill -0`) → `pid=N alive=yes|no|none` 단일 라인.
  2. 체크포인트 진행률 — `checkpoints/act/epoch_*/` 디렉터리 mtime 최신 + 개수.
     `start_act_train.sh` 2회차 패치와 동일한 디렉터리 glob 패턴 (train_act.py 의
     HF `save_pretrained` 포맷 일치).
  3. 로그 신선도 — `stat -f '%m'` 으로 log mtime epoch 추출 → `log_age_sec` 산출.
     `STALL_SEC` (기본 86400) 초과 시 exit 3.
  4. 표준 tail 블록 `--- last N lines --- ... --- end ---` 으로 형식 통일 → nightly
     에이전트가 그대로 잘라서 research-log "ACT 학습 진행률" 섹션에 append.
  5. exit code: 0 정상 / 1 미시작 / 2 이상종료 / 3 stall.

## 관찰 / 이슈

- [자가치유] 어제(6/13) 4회차 블록의 "`.venv/bin/python3` 부재, `.venv/bin/`에 `mjpython`
  만 존재" 자가치유 기록은 오기록. 본 회차 `ls .venv/bin/ | grep -E '^(python|mjpython)'`
  확인 시 `mjpython`, `python`, `python3`, `python3.14` 모두 존재 (Python 3.14 venv 표준
  구성). 본 회차 research-log "어떻게 검증했나" 섹션에 정정 기록.
- [자가치유] `.venv/bin/python3 -c "import mujoco"` 본 회차도 sandbox 차단 — 6/7 이후
  14일+1 연속. 런타임 검증은 6/15 W3 D1 일괄.
- [자가치유] `chmod +x scripts/check_act_train.sh` / `bash -n scripts/check_act_train.sh`
  본 회차 sandbox 차단. 정적 검토만. 6/15 W3 D1 사용자 수동 `chmod +x` 1회 필요.
- [자가치유] Obsidian 미러 `cp ... /Users/markmini/Documents/second-brain/00_AI_Wiki/...`
  working dir 밖 쓰기로 차단 — 14일+1 연속. 사용자 수동 동기화 필요.
- `agent/external-dependencies.md` Claude Code v3.2 harness allowlist 점검 블로커
  (장기헌) 14일+1 연속 미해결 — 본 세션 sandbox 차단의 근본 원인.
- PHASE_ROADMAP W3 항목(6/15 시작) 본 회차는 사전 준비. W3 정식 항목 미진입 →
  체크박스 변경 없음.

## 다음 단계

- 2026-06-15 (월) Phase 1 W3 D1:
  1. `.venv/bin/python3 scripts/train_act.py --smoke` 통과 검증.
  2. `bash scripts/start_act_train.sh --epochs 100` 으로 nohup 백그라운드 학습 시작
     (사용자 사전 `chmod +x scripts/start_act_train.sh scripts/check_act_train.sh` 권장).
  3. PHASE_ROADMAP W3 첫 두 항목 (`train_act.py` 파이프라인 + nohup 실행) `[v]` 체크.
- 2026-06-16 (화) 이후: 매일 23:00 nightly 가 `scripts/check_act_train.sh` 호출 → 출력을
  당일 research-log "ACT 학습 진행률" 섹션에 append. exit code 2/3 발생 시
  `agent/external-dependencies.md` 에 incident 항목 추가.

## 보고용 증거 후보

- [ ] `scripts/check_act_train.sh` — Mac mini M5 환경에서 ACT 학습(epoch 100, 수일 소요)
      일일 진행률 추적의 결정론적 status 출력. 6월 보고서 [2.사전학습] W3 일일 운영
      절차 증거 (start_act_train.sh 의 짝).

---

## 23:30 nightly sim-test 회차 (cop_sim_test_metrics v3.2)

### 실행 테스트 결과

- 우선 타겟 4종 (`sim_headless_6dof_video.py`, `sim_camera_verification.py`,
  `sim_pick_place.py`, `sim_data_collector.py`) 및 `bash scripts/check_act_train.sh`
  본 회차 모두 sandbox 차단. `.venv/bin/python3 -c "import mujoco"` 도 차단.
- 반복: 0회 / 성공률: 측정 불가.

### 관찰 / 이슈 (sim-test)

- [자가치유] 23:00 회차 산출물(`scripts/check_act_train.sh` + 동일 일자 research doc)
  은 존재 확인 — 23:00 job 자체는 정상. 23:30 회차는 실행 단계만 sandbox 차단.
- [자가치유] 14일+1 연속 sandbox 차단 — 근본 원인 `agent/external-dependencies.md`
  Claude Code v3.2 harness allowlist 점검 블로커 (장기헌) 미해결. 본 회차 신규
  외부 의존성 없음 — 동일 블로커 누적만 기록.
- PHASE_ROADMAP W3 첫 항목(6/15 시작)은 정규 윈도우 진입 전이라 본 회차에서도
  체크박스 변경 없음.

### 다음 단계 (sim-test 관점)

- 6/15 W3 D1 사용자 수동 절차 시 `chmod +x scripts/check_act_train.sh
  scripts/start_act_train.sh` 1회 + `.venv/bin/python3 scripts/train_act.py --smoke`
  통과 시 nohup 백그라운드 학습 시작. 그 시점부터 23:30 회차는 학습 진행률
  append 패턴으로 전환.

---

## 3회차 — nightly sim-environment 정합성 점검 (본 회차)

### 작업

01:02 / 23:30 회차 산출물 (`scripts/check_act_train.sh`, 동일 일자 research doc,
research-log 본 파일) 정독 → 추가 코드 변경 불필요. wrapper 짝(start_act_train.sh)
의 디렉터리 glob 패턴 (`epoch_*/`) 과 check 측 동일성 재확인 — `scripts/check_act_train.sh:47,51`
모두 `epoch_*/` (HF save_pretrained 디렉터리) 사용, train_act.py 의 실제 저장
포맷 (6/13 3회차 패치) 와 정합.

### 자가치유

- [자가치유] `bash -n scripts/check_act_train.sh` 본 회차 sandbox 거절. 14일+1 연속.
  정적 reread 만 (47줄 / 51줄 glob 직접 확인).
- [자가치유] `bash scripts/check_act_train.sh` 본 회차 sandbox 거절 — 런타임 status
  블록 미수집. 학습 미시작 상태 (exit code 1 예상: PID 파일 없음, ckpt 없음)
  로직만 정적 검증.
- [자가치유] `.venv/bin/python3 -c "import mujoco"` 본 회차 sandbox 거절. 14일+1 연속.
- [자가치유] `git add scripts/check_act_train.sh research/simulation/2026-06-14_check-act-train-status.md
  agent/research-log/2026-06-14.md` 본 회차 "This command requires approval" 거절.
  14일+1 연속 mutation 화이트리스트 미적용 확정. commit/push 미수행.
- [자가치유] Obsidian 미러 working dir 밖 쓰기 차단 — 14일+1 연속. 사용자 수동 동기화.

### 사용자 수동 처리 (블로커 시)

```
git add scripts/check_act_train.sh scripts/start_act_train.sh scripts/train_act.py \
        research/simulation/2026-06-14_check-act-train-status.md \
        research/simulation/2026-06-13_start-act-train-wrapper.md \
        research/simulation/2026-06-07_*.md research/simulation/2026-06-08_*.md \
        research/simulation/2026-06-09_*.md research/simulation/2026-06-10_*.md \
        research/simulation/2026-06-11_*.md research/simulation/2026-06-12_*.md \
        agent/research-log/2026-06-07.md agent/research-log/2026-06-08.md \
        agent/research-log/2026-06-09.md agent/research-log/2026-06-10.md \
        agent/research-log/2026-06-11.md agent/research-log/2026-06-12.md \
        agent/research-log/2026-06-13.md agent/research-log/2026-06-14.md \
        agent/external-dependencies.md
git commit -m "🛠 [시뮬] W3 사전 준비 마감 — check_act_train.sh + 6월 누적 — 2026-06-14"
git push origin main
chmod +x scripts/start_act_train.sh scripts/check_act_train.sh
cp research/simulation/2026-06-14_check-act-train-status.md \
   agent/research-log/2026-06-14.md \
   ~/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/
```

PHASE_ROADMAP 6/15~ W3 항목은 정규 윈도우 진입 전 → 체크박스 변경 없음.

---

## 4회차 — 23:30 nightly sim-test (cop_sim_test_metrics v3.2 재진입)

### 실행 테스트 결과

- 우선 타겟 4종 (`sim_headless_6dof_video.py`, `sim_camera_verification.py`,
  `sim_pick_place.py`, `sim_data_collector.py`) 및 `bash scripts/check_act_train.sh`,
  `.venv/bin/python3 -c "import mujoco"` 모두 본 회차 sandbox "requires approval"
  로 차단. 반복 0회 / 메트릭 미수집. 14일+1 연속 동일 차단 패턴.

### 자가치유

- [자가치유] `bash scripts/check_act_train.sh` sandbox 차단 — 런타임 status 블록
  미수집. 학습 미시작 상태(PID 파일 없음 → exit 1 예상) 는 `logs/` 디렉터리
  부재(`ls logs/: No such file or directory`) 로 간접 확인.
- [자가치유] `.venv/bin/python3 -c "import mujoco"` sandbox 차단. 14일+1 연속.
- [자가치유] Obsidian 미러 `cp ... ~/Documents/second-brain/...` working dir 밖
  쓰기 차단 — 사용자 수동 동기화 필요.
- 본 회차 신규 외부 의존성 없음 — 동일 블로커(`agent/external-dependencies.md`
  Claude Code v3.2 harness allowlist 점검, 장기헌) 누적.

### 다음 단계

- 6/15 W3 D1: 사용자 수동 `chmod +x scripts/start_act_train.sh
  scripts/check_act_train.sh` + `.venv/bin/python3 scripts/train_act.py --smoke`
  검증 → 통과 시 `bash scripts/start_act_train.sh --epochs 100` 백그라운드 실행.
  그 시점부터 23:30 회차는 `check_act_train.sh` 표준 status 블록을 본 섹션에
  append.
