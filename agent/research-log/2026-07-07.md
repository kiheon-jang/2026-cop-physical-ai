# 시뮬 진척 — 2026-07-07 (화요일)

## 오늘 진행 단계
Phase 2 - W1 - **DR-trained 모델 측정 + 다중시드 공정추정 비교 → W1(7/1~7/7) 종료**.
어제(7/6) 23:02 학습완료된 DR 재학습 모델을 오늘 드라이버가 측정, 야간 에이전트가 공정추정 비교 부착.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): **STAGE=측정 · 타겟=episodes_cl_dr → checkpoints/act_cl_dr**.
  학습완료 확정 → 측정 진행. **seed42 success_rate 0.7 (7/10, median lift 50.2mm)**.
- 마커 2단계 승격 정상: `.pending`(episodes_cl_dr:1783181837) → `cop_trained_on.marker` 승격,
  `.pending` 제거. 운영 `rollout_summary.json` 이 DR-trained 성적으로 갱신(마커 승격 = 정당 덮어쓰기),
  이전 baseline 은 `rollout_summary_baseline_cl.json`(0.70/43.7mm) 보존.

## 실행 테스트 결과
- **다중시드 공정추정 비교(비파괴, `_cldr_seed*` 접미사)** — DR-trained 를 baseline 동일 3 seed 로 측정:

  | seed | baseline (act) | DR-trained (act_cl_dr) |
  |---|---|---|
  | 42   | 0.70 · 43.7mm · {2,5,8} | 0.70 · **50.2mm** · {2,5,8} |
  | 7    | 0.90 · 44.8mm · {9}     | 0.80 · **49.8mm** · {3,9}   |
  | 123  | 0.80 · 43.7mm · {0,1}   | 0.80 · **50.2mm** · {0,1}   |
  | 2026 | 0.90 · 44.8mm · {5}     | 0.90 · **50.5mm** · {5}     |
  | **평균** | **0.825** | **0.800** |

- **정직한 결론 3가지**:
  1. **성공률 개선 없음** — 0.825→0.800(seed7 실패 +1 = n=10 노이즈, 통계적 동등). DR 재학습이
     binary 성공률을 올렸다고 말할 수 없음.
  2. **median lift 만 일관 +6mm**(44→50mm, 4/4 시드) — 노이즈 아님, 하지만 40mm 임계값 위라 이진판정 무영향.
  3. **실패 큐브배치 거의 불변**(42{2,5,8}·123{0,1}·2026{5}, seed7만 {9}→{3,9}) → 실패는 특정 배치에 고정.

## 관찰 / 이슈
- **W1 실증 결론**: 병목은 **섭동강건성이 아니라 큐브배치 커버리지(모방격차)**. 7/2(on/off 동등)·
  7/3(축별 ablation 불변)·7/4(seed마다 실패 이동) 프록시 가설을 **DR 실모델 재학습으로 확증**.
  → **DR 축 증강은 sim 성공 천장을 못 올린다.** 올리려면 학습 데이터의 배치 다양성을 넓혀야 함
  (`.next=episodes_floor` 바닥 파지 = 배치 다양성↑ 정공법).
- 무결성: baseline 아카이브·baseline seed 요약·`episodes_cl(_dr)` 각 50ep 전부 불변. 신규는
  `rollout_summary_cldr_seed{7,123,2026}.json` 3종만 추가. train_act 프로세스 없음(7/6 23:02 종료).
- [자가치유] 없음 — 어제 research-log·로드맵 최신, 드라이버 STAGE 정상, 마커 승격/산출물/데이터 정합, 에러 없음.

## 다음 단계
- **W2 (7/8~)**: zero-shot 실기 추론 → Sim2Real 격차 측정. **외부의존**: 장기헌 Orin/실기 SSH
  (external-dependencies.md 미수신 지속) → 미수신 시 sim 트랙 유지. sim-side 공정추정 기준선 **~0.80~0.825**
  (seed42 0.70 은 비관적 끝단). DR-trained 가 실기 섭동에 더 강건한지가 W2 핵심 검증점.
- **드라이버 다음 사이클**: `.next=episodes_floor`(바닥 파지, 배치 다양성↑) — 배치 커버리지 확장으로
  sim 성공 천장을 실제로 올릴 수 있는지 검증.
