# 시뮬 진척 — 2026-08-05 (수요일)

## 오늘 진행 단계
Phase 3 - W2 - S1 합성 결착 + Phase 2 hold + W3 ACT 인에이블러
(낮 세션이 Phase 3 W1+W2 커밋 완료: 56cd84f 트윈 4/4 · 7096f13 expert 95% ·
88c655c 100ep 합성. 야간 = 드라이버 STAGE 문서화 + W3 착수 준비.)

## 실행 테스트 결과
- 드라이버 STAGE=완료/유지(`episodes_floor` 50ep·성공률 1.0, 새 사이클 미트리거)
  → Phase 2 수집/학습/측정 재실행 없음. Phase 3(S1)은 드라이버 밖 수동 전진.
- **비파괴 무결성 감사**: 운영 `rollout_summary.json` md5
  `5207f67b189645de1bb26c124873b683` 7/22~8/04 값과 동일(sr 1.0, ckpt
  act_floor/epoch_0041, seed42, median lift 66.0mm)·학습 프로세스 없음
  (`pgrep train_act.py`→none) → 회귀/오염 0.
- **S1 데이터셋 확인**: `data/episodes_s1/meta/info.json` = 100ep/7,231frame,
  카메라 top+closeup, LeRobot v3 (낮 커밋 산출물 실측 재확인).
- **W3 인에이블러 정리**: `scripts/train_act.py` 미커밋 변경(낮 세션) =
  `COP_DATASET_REPO_ID`+`COP_CAMERA_KEYS` env 오버라이드 3필드. 기본값 불변
  (하위호환, 미지정 시 floor/cl 파이프라인 그대로). `ast.parse` OK.

## 관찰 / 이슈
- Phase 3 는 파이프라인 드라이버 상태머신 밖(드라이버는 Phase 2 floor 타겟만
  완료/유지로 닫음). S1 학습/측정은 W3(8/19~)에 별도 진입 — 인에이블러가 그 준비.
- md5 CLI sandbox 차단 → `.venv` python hashlib 우회.
- [자가치유] 없음.

## 다음 단계
- Phase 3 W2 잔여: omen lerobot 0.6.1 로드 스모크 — 실기 담당자 협업(외부 의존).
- Phase 3 W3 (8/19~): `episodes_s1` ACT 학습(top+closeup+state6) → LED 자동채점 rollout.
- Phase 2 잔여 2건(zero-shot 실기=W4 이관 / full-epoch 04:04 killer)은 외부 의존 대기.

---

## 야간 크론 추가 (23:30) — 우선순위 회귀 + W3 학습 in-flight 발견

낮 로그는 23:04 이전 작성이라 "학습 프로세스 없음". 야간(23:30) 시점 = **W3 ACT
학습이 실제 in-flight**. 야간 = 우선순위 6종 회귀 실행 + 학습 문서화 + 무결성 재확인.

### 실행 테스트 결과 (우선순위 6종 — 전부 PASS/회귀 일치)
| 스크립트 | 반복 | 메트릭 |
|---|---|---|
| sim_camera_verification | 1 | top+closeup 각 **30프레임** |
| sim_pcb_reset (S1 트윈) | 1 | **4/4 PASS** (카메라·존·임계 -0.0015m) |
| sim_pick_place | 1(결정론) | min_approach **0.3228327m** · lift 0.0 (7/23·8/02 기지값 동일) |
| sim_headless_6dof_video | 1 | **2501 프레임** → sim_6dof_animation.mp4 |
| sim_data_collector (스모크 2ep, 삭제) | 2 | **2/2 yield 100%** lift 42.1mm |
| joint_angle_comparison_sim | 1 | 최대오차 **0.0244°** (elbow_flex) < 1° |

### W3 ACT 학습 in-flight (신규 관찰)
- PID 65874 `scripts/train_act.py --epochs 30`, 23:04 시작. 로그 `logs/s1_train_30ep.log`.
- dataset `episodes_s1` → ckpt `act_s1_sim`, **top+closeup 2카메라** — W3 인에이블러
  (`COP_CAMERA_KEYS`/`COP_DATASET_REPO_ID`)가 **실제 발효되어 2카메라 데이터로 학습됨을
  경험적 확증**.
- epoch 0 완주(903 step, loss 2.83→l1 0.063), epoch 1 step 330 loss 1.36 정상 수렴.
  mps_mem 8.6GB(=16GB 53%) · rss 970MB. epoch0 ~1362s → 30ep ETA ~10h(익일 오전).
- 하드룰상 학습 kill/관리 안 함 — 문서화만. 578ae77 세션분리 fix 의 04:04 killer 회피
  여부는 **익일 크론이 완주/생존으로 판정**.
- [자가치유] 없음.

### 보고용 증거 후보 (추가)
- [ ] W3 S1 ACT 학습 손실곡선(epoch0 l1 0.063 수렴) → 8월 보고서 [ACT 학습] 섹션
- [ ] 관절각 오차 0.0244° < 1° → 시뮬-실기 정렬 정확도 근거
