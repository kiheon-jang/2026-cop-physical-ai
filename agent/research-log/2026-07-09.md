# 시뮬 진척 — 2026-07-09 (목요일)

## 오늘 진행 단계
Phase 2 - W2 - 배치 다양성(floor) 사이클 ACT 재학습 (**재시도**) + FD 누수 자가치유

> 어제(7/8) 착수한 floor 학습(pid 94316)이 epoch ~50 부근에서 이상종료 →
> 드라이버가 감지·재시작(pid 21661). 크래시 원인 = DataLoader FD 누수 → 코드 자가치유.
> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 미수신으로 진입 불가 → floor sim 사이클 전진.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00):
  - 타겟 `episodes_floor`→`act_floor` 학습 **이상종료 감지**(metrics 마지막 epoch 미달) →
    `OSError: [Errno 24] Too many open files` → **재학습 재시도 예약**.
  - STAGE=학습시작 → **새 run pid 21661** 시작(`--epochs 100 --no-resume`, fresh FD 테이블).

## 실행 테스트 결과 (야간 에이전트, 비파괴)
- **어제 run(pid 94316) 크래시 규명**: `checkpoints/act_floor/` 는 epoch_0009(7/8 23:56)…
  epoch_0049(7/9 03:45)까지만 → epoch 50~55 사이 크래시. 속도 정상(03:45=ep49)이었으므로
  성능 아닌 **FD 고갈**이 원인.
- **근본원인**: `train_act.py` DataLoader 1회 생성(L497) + epoch 루프가 매 epoch 재순회(L336) +
  `num_workers=4` `persistent_workers` 미설정 → **매 epoch 워커 4개 재spawn** → macOS spawn +
  낮은 `ulimit -n` 에서 파이프 FD 누적 → ~50 epoch 후 `Too many open files`.
- **자가치유**: DataLoader 에 `persistent_workers=workers > 0` 추가(워커 1회 spawn 후 재사용,
  smoke `num_workers=0` 는 False 게이트). surgical 1줄+주석, 학습 로직 무변경.
- **검증**: `ast.parse` OK / DataLoader 스모크 `workers=0 persistent=False`·`workers=2 persistent=True`
  (3 epoch 재순회 워커 재사용) `DATALOADER_SMOKE_OK`.
- **현 학습 alive**: pid 21661, epoch 0 step 200 loss **38.8→5.67**(정상 수렴), CPU ~12% RSS ~226MB.

## 관찰 / 이슈
- **[자가치유] DataLoader FD 누수 수정**: `persistent_workers=True` 추가로
  `OSError [Errno 24] Too many open files`(매 epoch 워커 재spawn) 재발 방지.
- **fix 발효 시점**: pid 21661 은 수정 **이전** 모듈 로드 → 이 run 은 fix 미적용, 다시 ~epoch 50 부근
  재크래시 가능. 그 경우 내일 드라이버가 이상종료 재감지 → **수정 코드로 재시작 → 완주**. 하드룰상
  야간 에이전트는 학습 kill/재실행 안 함(드라이버 담당) → pid 21661 유지, fix 는 다음 사이클 발효.
- **무결성 격리 유지**: target=`episodes_floor` · marker=`episodes_cl_dr:1783346557`(직전 승격값 유지,
  운영 rollout_summary 불변) · pending=`episodes_floor:1783324998`(대기). 학습 미완 → 승격/측정 보류 →
  baseline 무손상. ckpt `act`·`act_cl_dr`·`act_floor` 3자 분리.

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **학습 완료/재시작 완주 시**: pending 승격 → `act_floor/epoch_0099` 측정 → floor-trained rollout.
- **비교 검증**: floor-trained 를 4 seed(42/7/123/2026) 공정추정으로 baseline(0.825)·DR-trained(0.800)
  와 비교. 핵심질문: 배치 다양성이 성공률 천장을 올리는가 / 실패 배치가 이동·축소되는가(7/7 W1 결론
  = 병목=배치 커버리지 실증).
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.
