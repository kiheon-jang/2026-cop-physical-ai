# 시뮬 진척 — 2026-07-10 (금요일)

## 오늘 진행 단계
Phase 2 - W2 - floor 배치다양성 재학습 3차 (**FD-fix 발효 run**, in-flight)

> 어제(7/9) 자가치유한 DataLoader FD 누수 수정(`persistent_workers`)이 **처음 발효되는 run**.
> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 외부의존 미수신 → 진입 불가 → floor sim 사이클 전진.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00):
  - 타겟 `episodes_floor`→`act_floor`, 어제 run(pid 21661) **이상종료 감지**(metrics 마지막 epoch 미달)
    → 로그 원인 `OSError: [Errno 24] Too many open files` → **재학습 재시도**.
  - STAGE=학습시작 → **새 run pid 39732** 시작(`--epochs 100 --no-resume`, train_start 23:00:26).

## 실행 테스트 결과 (야간 에이전트, 비파괴 관찰)
- **오늘의 핵심 = fix 발효 시점 전환**:
  - 어제 pid 21661 = 수정 **이전** 코드 로드 → 예측대로 ~epoch 50 FD 고갈 재크래시
    (`checkpoints/act_floor/epoch_0049` 7/9 03:45 마지막).
  - 오늘 pid 39732 = 23:00:26 시작 = FD-fix 커밋 `c827ffe`(7/9) **이후** 디스크 코드 로드 →
    `grep persistent_workers scripts/train_act.py` L185~196 존재 → **수정이 이 run 에 실제 적용**.
    `git log -- scripts/train_act.py` 최신=c827ffe 이후 무변경 = pid 39732 로드 코드 = fixed.
  - ⇒ 매 epoch 워커 재spawn 없음 → ~epoch 50 FD 고갈 없이 **100epoch 완주 기대**.
- **run alive & 정상 수렴**: log mtime 23:01:56(신선), 마지막 `epoch 0 step 120 loss 6.42`
  (시작 27.1 → 6.4 정상 하강). **23:30 재확인: pid 39732 alive, epoch 6 step 320 loss 0.534**
  → 어제 크래시 지점(~epoch 50) 이전 정상 진행, FD-fix 실효 확인.

### 우선순위 4종 회귀 (야간 에이전트 실행, 비파괴 — 스크래치 루트만 사용)
| 스크립트 | 반복 | 결과 | 메트릭 |
|---|---|---|---|
| `sim_headless_6dof_video.py` | 3 | **3/3 PASS** | 6-DoF 각 관절 애니메이션, 2501 frame/run, mp4 저장 정상 |
| `sim_camera_verification.py` | 3 | **3/3 PASS** | top+gripper 2카메라 동기 캡처 30 frame/run, 이미지 저장 정상 |
| `sim_pick_place.py` | 3 | **0/3 (데모·예상)** | 정적 데모 expert, min_approach **0.3228m**(결정론적 3회 동일), lift 0mm — closed-loop 아님(기존과 동일 회귀 기준값) |
| `sim_data_collector.py` | 2 ep | **2/2 yield 100%** | closed-loop 수집 성공 2/2, lift 43.9mm, 67 frame/ep, grasp재시도 1 (스크래치 `data/_smoke_0710` → 삭제, 운영 데이터셋 무접촉) |

→ 4종 전부 이전 회귀 기준(3/3·3/3·데모0/3·수집2/2 yield100%)과 **일치, 회귀 없음**. 학습 부하 중
동시 실행에도 스크립트 안정.

## 관찰 / 이슈
- **[자가치유] 없음(신규)** — 어제 발효한 `persistent_workers` fix 가 이 run 에 처음 적용된 것이
  오늘의 진척. 하드룰상 야간 에이전트는 학습 kill/재실행 안 함 → pid 39732 유지, 드라이버가 완주 관리.
- **무결성 격리 유지**: target=`episodes_floor` · marker=`episodes_cl_dr:1783181837`(직전 승격값 유지) ·
  pending=`episodes_floor:1783324998`(대기·미승격) · measured=`episodes_cl_dr:1783346557`.
  학습 미완 → 승격/측정 보류 → 운영 `rollout_summary.json`(act_cl_dr, seed42, 7/10=**0.70**,
  median lift 50.2mm, measured 2026-07-07) **불변** → baseline 무손상.
- **datasets 전수 불변**: `episodes_floor` 50/3350 · `episodes_cl` 50/3350 · `episodes_cl_dr` 50/3350.
  ckpt `act`·`act_cl_dr`·`act_floor` 3자 분리.

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **완주 시**(fix 덕에 이번엔 ~epoch 50 크래시 없이 100epoch 도달 기대): pending 승격 →
  `act_floor/epoch_0099` 측정 → floor-trained rollout.
- **비교 검증**: floor-trained 를 4 seed(42/7/123/2026) 공정추정으로 baseline(0.825)·DR-trained(0.800)
  와 비교. 핵심질문 = 배치 다양성이 성공률 천장을 올리는가 / 실패 배치가 이동·축소되는가(7/7 W1 실증 처방).
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.
