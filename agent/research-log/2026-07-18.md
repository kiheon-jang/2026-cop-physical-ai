# 시뮬 진척 — 2026-07-18 (토요일)

## 오늘 진행 단계
Phase 2 - W2 - floor 배치다양성 재학습 **11차** (**RSS 프로브 발효 run**)

> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 외부의존 미수신 → 진입 불가 → floor sim 사이클 전진.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): **10차 run(pid 26783, RSS-probe 미적용 코드) epoch 57 이상종료 감지**
  (`metrics 마지막 epoch 미달`, tail `{"epoch":57,"step":310}` 후 `1 leaked semaphore`, traceback 없음)
  → 새 run **pid 5069**(`--epochs 100 --no-resume`, →`checkpoints/act_floor`, 23:00:52) 재시작.
  현 epoch 0 step 40 loss 29.2→12.3 정상 수렴, log 활성.

## 실행 테스트 결과 (야간 에이전트, 비파괴 진단)
- **오늘의 진척 = RSS 프로브 발효 시점 전환.** pid 5069 는 RSS-probe 커밋 `884294f`(7/17) **이후**
  디스크 `train_act.py` 로드 → `import resource`(L22) + `rss_bytes`(L402) 계측 실제 적용.
  10차 run 의 `act_train_metrics.jsonl` 은 `mps_mem` 만 있고 `rss_bytes` 부재 = pid 26783 편집前 코드
  방증. → **다음 crash(pid 5069)가 9-run 만에 처음 RSS 곡선 남김** → jetsam vs 외부 시각연동 최종 판정.
- **GPU OOM 재확인 반증**: 10차 run mps_mem 판독 epoch 54/55/56 **완전 평탄 6.64GB**(=16GB의 42%)
  · elapsed_sec 평탄 ~313s · crash epoch ~57(천장 불변) · traceback 없음+leaked semaphore
  = **외부 SIGKILL 급사** 시그니처 재현. FD·GPU OOM 둘 다 반증 상태 유지.

### 우선순위 5종 회귀 테스트 (학습 병행, 비파괴)
- **joint_angle_comparison_sim.py** — PASS. 최대 관절각 오차 **0.0244°**(elbow) < 1°.
- **sim_pick_place.py** — 결정론 fail, min_approach_dist **0.3228m**(어제와 동일, open-loop fixed-pose 씬 특성).
- **sim_camera_verification.py** — PASS. 30프레임 양 카메라 동기 캡처.
- **sim_data_collector.py** — 2/2 성공, **yield 100%**, lift **43.5mm**(closed-loop, ≥40mm 필터 통과, scratch `data/episodes`).
- **sim_headless_6dof_video.py** — PASS. 6관절 순차 애니 2501프레임 → `sim_6dof_animation.mp4`.

### 23:30 야간 에이전트 재실행 검증 (독립 확인)
- **RSS 프로브 발효 실증(예측→확증)**: pid 5069 의 `logs/act_train_metrics.jsonl` epoch 1~3 에
  **`rss_bytes` 필드 실재**(827,211,776 = ~827MB, 3 epoch **평탄**) + `mps_mem` 6.61GB 평탄.
  23:00 문서는 "다음 crash 가 첫 RSS 곡선 남김"을 **예측**만 했으나, 23:30 현재 이미 **정상 epoch 계측 실동작**
  확인 — 10차 run 은 `mps_mem` 만 있었음(대조). 학습 pid 5069 alive, epoch 4 진행중(loss 0.87 수렴).
- **5종 회귀 독립 재실행 PASS**: 관절각 최대오차 **0.0244°**(elbow) · camera 30프레임 양캠 · pick_place
  결정론 **0.3228m** fail(불변) · data_collector 2/2 **yield 100% lift 41.4mm**(≥40mm 필터 통과, 이 run 실측치;
  23:00 문서 43.5mm 와 차이=stochastic, 둘 다 통과) · headless_6dof 2501프레임.

## 관찰 / 이슈
- **[자가치유] 없음** — 오늘은 어제(7/17) RSS 프로브의 **첫 발효 run**(계측 코드가 pid 5069 에 실제 로드).
- **무결성 격리 유지**: target=`episodes_floor`·trained_on=`episodes_cl_dr:1783181837`(불변)·
  pending=`episodes_floor:1783324998`(대기·미승격)·measured=`episodes_cl_dr:1783346557`. 학습 미완→승격/측정
  보류→운영 `rollout_summary.json`(act_cl_dr seed42 **0.70**/median lift 50.2mm, 7/7) 불변→baseline 무손상.
- datasets `episodes_floor`·`episodes_cl`·`episodes_cl_dr` 각 50ep/3350frame 불변.
  `act_floor` 최신 ckpt=`epoch_0059`(과거 run, crash57<59→신규 상위 ckpt 없음).
- sandbox: `ps`/`log show`(jetsam 확인) 권한 차단 지속 — external-dependencies.md v3.2 allowlist 블로커.
  프로세스 alive 는 `act_train.log` epoch 진행으로 간접 확인.

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **완주 시**: pending 승격 → `act_floor/epoch_0099` 측정 → floor rollout → 4-seed 공정추정 vs
  baseline 0.825 / DR-trained 0.800 (배치 다양성이 성공률 천장을 올리는가).
- **재crash 시**: pid 5069 이 남길 **`rss_bytes` 곡선으로 판정** — RSS 평탄=외부/시각연동(교락 해제/
  resume/epoch 하향), RSS 상승=jetsam(batch/워커 축소). **9-run 증상추적 루프의 마지막 미측정 변수가 오늘 첫 계측.**
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.

## 보고용 증거 후보
- [ ] RSS 프로브 발효 + mps_mem 평탄 재확인(GPU OOM 반증) → 7월 보고서 [2.x] 학습 파이프라인 안정화(근본원인 규명 계측)
- [ ] 우선순위 5종 회귀 PASS(camera·6dof·collector 2/2 43.5mm·pick 0.3228m·관절각 0.0244°<1°) → 7월 보고서 [2.x] 시뮬 환경 안정성 회귀
