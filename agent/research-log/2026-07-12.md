# 시뮬 진척 — 2026-07-12 (일요일)

## 오늘 진행 단계
Phase 2 - W2 - floor 배치다양성 재학습 5차 (**RLIMIT-fix 발효 run, in-flight**)

> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 외부의존 미수신 → 진입 불가 → floor sim 사이클 전진.
> 어제(7/11) 추가한 RLIMIT_NOFILE 셀프-상승 fix 가 오늘 새 run 에 처음 적용됨.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): 어제 run(pid 56445) **이상종료 감지** → **재학습 재시도**.
  - STAGE=학습시작 → **새 run pid 85398**(`--epochs 100 --no-resume`, →`checkpoints/act_floor`).
  - shutdown 시 `21 leaked semaphore objects` + `OSError [Errno 24]` (직전 run 잔재, FD 누수 방증).

## 실행 테스트 결과 (야간 에이전트, 비파괴 관찰)
- **오늘의 진척 = FD 근본 fix 발효 시점**: pid 85398 은 FD-fix 커밋 `3fa8bb6`(7/11) **이후**
  디스크 `train_act.py` 를 로드 → 두 fix 실제 적용:
  - `persistent_workers=workers>0` (L196, 7/9 fix) — 워커 매 epoch 재spawn 방지
  - `_raise_fd_limit()` main 진입 즉시 (L452, 7/11 fix) — 자기 FD 소프트 한도 하드(무제한) 상승 → 256 천장 제거
  → 지난 4 run(pid 94316·21661·39732·56445) 을 ~49~59 epoch 에서 죽인 FD 천장이 이 run 에서 제거 → **100epoch 완주 기대**.
- **run 정상 수렴**: pid 85398 alive, log 말미 epoch 0 step 30 loss 36.7→17.4 정상 하강.
- **코드 검증**: grep — persistent_workers/L196 · _raise_fd_limit/L427·L452 · fd_limit_raised/L466 디스크 실재.

## 우선순위 스크립트 회귀 테스트 (야간 에이전트 실행)
23:30 sim-test 에이전트가 우선순위 4종 직접 실행 (비파괴, 스크래치 루트 사용):
- **sim_camera_verification.py** — 1회, PASS: 30프레임 양 카메라 캡처, `video/` 저장.
- **sim_headless_6dof_video.py** — 1회, PASS: 2501프레임 6관절(shoulder_pan~gripper) 애니메이션 `sim_6dof_animation.mp4` 저장.
- **sim_pick_place.py** — 3회, **0/3 fail (결정론적)**: `min_approach_dist=0.3228m` 3회 동일 = 고정포즈 open-loop 데모(비확률적). 알려진 0% 성공(6/22 근본원인 문서) 회귀 기준선 — 예상대로. closed-loop 파이프라인 아님.
- **sim_data_collector.py** — floor 씬 스모크 2ep(스크래치 `episodes_smoke_0712`, 실행 후 삭제): **2/2 성공, yield 100%, lift 65.3mm**, 큐브 바닥배치(TABLE_TOP=0). closed-loop grasp 가 floor 씬에서 정상 동작 확인.
- SKIP: sim_viewer_6dof.py(headless 비호환). CPU/메모리 지표 미수집(sandbox `ps` 차단).
- **무결성 재확인**: 스크래치 루트만 사용 → 운영 데이터 무손상. datasets episodes_cl/cl_dr/floor 각 **50ep 불변**. 운영 `rollout_summary.json` = act_cl_dr/epoch_0099 seed42 **0.70**/50.2mm/measured 2026-07-07 **불변**.

## 관찰 / 이슈
- **[자가치유] 없음.** 어제 RLIMIT fix 의 첫 발효가 오늘 진척 자체. 하드룰상 pid 85398 kill/재시작 안 함 → 드라이버가 완주 관리.
- **무결성 격리 유지**: target=`episodes_floor` · trained_on=`episodes_cl_dr:1783181837`(직전 승격값·불변) ·
  pending=`episodes_floor:1783324998`(mtime 7/12 23:00, 대기·미승격) · measured=`episodes_cl_dr:1783346557`.
  학습 미완 → 승격/측정 보류(6/22 SILENT 반대·설계대로) → 운영 `rollout_summary.json`
  (act_cl_dr, seed42, 7/10=**0.70**, median lift 50.2mm, measured 2026-07-07) **불변** → baseline 무손상.
- **datasets 전수 불변**: `episodes_floor`·`episodes_cl`·`episodes_cl_dr` 각 50 episodes(`meta/info.json`). ckpt act·act_cl_dr·act_floor 3자 분리.

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **완주 시**(256 천장 제거 → 100epoch 도달 기대): pending 승격 → `act_floor/epoch_0099` 측정 → floor-trained rollout.
- **비교 검증**: floor-trained 를 4 seed(42/7/123/2026) 공정추정으로 baseline(0.825)·DR-trained(0.800) 와 비교.
  핵심질문 = 배치 다양성이 성공률 천장을 올리는가 / 실패 배치가 이동·축소되는가(7/7 W1 처방).
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.
