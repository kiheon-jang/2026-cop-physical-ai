# 시뮬 진척 — 2026-06-19 (금요일)

## 오늘 진행 단계

Phase 1 - W3 D5 — ACT 학습 status 점검 (D1~D4 사용자 수동 절차 미시행 그대로 → D5 동일 패턴 반복).

PHASE_ROADMAP W3 (6/15~6/21) 대상 항목:
- [ ] LeRobot ACT 학습 파이프라인 구성 (`scripts/train_act.py`)
- [ ] nohup 백그라운드로 epoch 100 학습 실행
- [ ] 매일 크론이 `logs/act_train.log` 마지막 줄 확인 → research-log 진행률 기록

## 실행 테스트 결과

- 상세: `research/simulation/2026-06-19_act-w3-d5-status-check.md`
- 정적 점검 (Bash `ls` 통과): `scripts/` 4종 그대로 (`build_report_pptx.py`, `train_act.py -rwxr-xr-x`, `start_act_train.sh -rw-r--r--`, `check_act_train.sh -rw-r--r--`). `logs/` 미존재, `checkpoints/` 미존재 — ACT 학습 미시작 18일 연속.
- 런타임 점검: 본 회차 0건 (sandbox 차단 18일 연속).
- PHASE_ROADMAP W3 첫 두 항목 체크박스 변경 **보류** — 런타임 통과 전까지 `[ ]` 유지 (5일차 누적, 6/21 W3 마지막일까지 2일 남음).

## 관찰 / 이슈

- [자가치유] `/Volumes/MARK_DATA/dev/2026-cop-physical-ai/.venv/bin/python3 -c "import mujoco; ..."` 본 회차 sandbox "requires approval" 거절 — 18일 연속. `.venv` 심볼릭 링크 대상 (`~/.local/share/uv/python/cpython-3.14.0-macos-aarch64-none/`) sandbox allowlist 밖. 런타임 메트릭 0건.
- [자가치유] `source .venv/bin/activate` / `. .venv/bin/activate` 본 회차 시도 — "evaluates arguments as shell code" 거절. POSIX `.` 도 동일. .venv 활성화 sandbox 차단 18일 연속.
- [자가치유] `chmod +x scripts/start_act_train.sh scripts/check_act_train.sh` 본 회차 sandbox 거절 패턴 — 18일 연속. 두 스크립트 `-rw-r--r--` 유지. 사용자 수동 1회 필요.
- [자가치유] `git add agent/research-log/2026-06-18.md` 본 회차 sandbox 거절 — 18일 연속 mutation 차단. 본 회차 신규 산출물 2건 (`agent/research-log/2026-06-19.md` + `research/simulation/2026-06-19_act-w3-d5-status-check.md`) git 미반영 예상.
- [자가치유] Obsidian 미러 `cp ... /Users/markmini/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/` 본 회차 미시도 — working-dir 밖 쓰기 sandbox 차단 패턴 18일 연속. 사용자 수동 동기화 필요.
- 워킹트리 6/7~6/19 미커밋 누적 약 44건 (research-log 13건, 시뮬 리포트 13건, scripts/ 3건, daily-reports 11건, 수정 4종). 13일 연속 누적.
- 근본 원인 `agent/external-dependencies.md` Claude Code v3.2 harness allowlist 블로커 (장기헌) 18일 연속. 신규 외부 의존성 추가 없음. 사용자 수동 1회 일괄 처리 권고 그대로 유효.

## 다음 단계

- **사용자 수동 (즉시 1회)**: `cd /Volumes/MARK_DATA/dev/2026-cop-physical-ai && chmod +x scripts/*.sh && .venv/bin/python3 scripts/train_act.py --smoke && bash scripts/start_act_train.sh --epochs 100 &` 백그라운드 실행 → 그 시점에서 PHASE_ROADMAP W3 첫 두 `[ ]` → `[v]` 일괄 체크 + `git add -A && git commit -m "📊 [로그] 2026-06-07~19 누적 산출물 — sandbox 18일 연속 차단 해소" && git push origin main` 일괄 처리.
- **2026-06-20 (토) W3 D6 nightly**: 사용자 절차 통과 가정 시 `check_act_train.sh` 표준 status 블록 (`pid=N alive=yes ckpt_count=≥1 log_age_sec<86400`) 을 당일 research-log "## ACT 학습 진행률" 섹션에 append. 미통과 시 본 D5 패턴 반복.
- **2026-06-21 (일) W3 D7 마감**: W3 마지막일. 미통과 시 W4 (6/22~) 로의 진입이 ACT 학습 완료 대기 상태로 변경됨.
- **2026-06-22 (월) W4 D1**: ACT 학습 완료 대기 + 시뮬 추가 200 에피소드 생성 시작 (`sim_data_collector.py`). Orin Nano SSH 정보 마감일.

## 보고용 증거 후보

- [ ] `research/simulation/2026-06-19_act-w3-d5-status-check.md` — W3 D5 status 점검 + sandbox 18일 연속 블로커 누적. 6월 보고서 [2.사전학습] W3 일일 운영 절차 증거.

---

## 23:30 nightly sim-test (13회차) 결과 append

- 우선순위 타겟 4종 (`sim_headless_6dof_video.py`, `sim_camera_verification.py`, `sim_pick_place.py`, `sim_data_collector.py`) 본 회차 실행 시도: `/Volumes/MARK_DATA/dev/2026-cop-physical-ai/.venv/bin/python3 -c "import mujoco; ..."` sandbox "requires approval" 거절 — 19일 연속 (23:30 잡 기준 13일 연속). 런타임 메트릭 0건.
- [자가치유] 23:00 회차의 정적/심볼릭 링크 진단(`.venv/bin/python3` → `~/.local/share/uv/python/cpython-3.14.0-macos-aarch64-none/bin/python3.14`) 그대로 유지 — 23:30 회차 신규 정보 없음. 사용자 수동 1회 절차 권고 그대로 유효.
- 파일시스템 증거 (mutation 없이 확인 가능): `data/episodes/meta/info.json` 200 ep / 12400 frames / so101 그대로, `logs/` 부재, `checkpoints/` 부재 — Phase 1 W3 D1~D5 ACT 학습 미시작 상태 유지.
- 메트릭: 시뮬 동작 성공률 N/A (실행 차단), 추론 속도 N/A, 시뮬-실기 관절각 오차 N/A (Phase 1+ 산출 보류), 학습 손실 N/A (W3 미진입), CPU/메모리 사용률 N/A.
- 다음 단계: 6/20 (토) W3 D6 nightly — 사용자 수동 절차 통과 시 `check_act_train.sh` status 블록 append, 미통과 시 동일 패턴 반복 (6/21 W3 마지막일).

---

## 추가 nightly 확인 (sim-environment v3.2, 회차 +1)

- 정적 점검 (Bash `ls` 통과): `scripts/train_act.py -rwxr-xr-x`, `scripts/start_act_train.sh -rw-r--r--` (실행권한 미부여 유지), `scripts/check_act_train.sh -rw-r--r--` (실행권한 미부여 유지). `logs/`, `checkpoints/` 부재 — ACT 학습 미시작 19일 연속.
- [자가치유] `/Volumes/MARK_DATA/dev/2026-cop-physical-ai/.venv/bin/python3 -c "import mujoco; print(mujoco.__version__)"` 본 회차 sandbox "requires approval" 거절 — 19일 연속. `.venv` 심볼릭 링크 대상 sandbox allowlist 밖. 런타임 메트릭 0건.
- [자가치유] `git add` / `git commit` / `git push` 본 회차 시도 — mutation sandbox 차단 19일 연속 예상. 워킹트리 미커밋 6/7~6/19 누적 약 44건 그대로.
- 근본 원인 `agent/external-dependencies.md` Claude Code v3.2 harness allowlist 블로커 (장기헌) 19일 연속. 사용자 수동 1회 일괄 처리 권고 유효.

---

## 23:30 nightly sim-test v3.2 (회차 +2, 본일 3회차)

- `/Volumes/MARK_DATA/dev/2026-cop-physical-ai/.venv/bin/python3 -c "import mujoco; ..."` 본 회차 sandbox "requires approval" 거절 — 19일 연속, 본일 3회차 동일. 우선순위 타겟 4종(`sim_headless_6dof_video.py`, `sim_camera_verification.py`, `sim_pick_place.py`, `sim_data_collector.py`) 실행 0회. 메트릭 N/A.
- 파일시스템 증거 (mutation 없이 확인): `data/episodes/meta/info.json` so101 / 200 ep / 12400 frames / fps 30 / splits train 0:200 그대로. `logs/`, `checkpoints/` 부재 — ACT 학습 미시작 19일 연속.
- [자가치유] `git add agent/research-log/2026-06-19.md` 본 회차 sandbox 거절 — mutation 19일 연속 차단. 본 회차 신규 산출물 없음 (기존 로그 append만).
- [자가치유] Obsidian 미러 `cp ... /Users/markmini/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/` 본 회차 미시도 — working-dir 밖 쓰기 차단 패턴 그대로.
- 다음 단계 그대로: 사용자 수동 1회 절차 (chmod + smoke + start_act_train.sh + git add -A + commit + push) 권고 유효.
