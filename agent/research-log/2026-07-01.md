# 시뮬 진척 — 2026-07-01 (수요일)

## 오늘 진행 단계
Phase 2 - W1 - **Domain Randomization 기반 모듈 착수** (Sim2Real 진입 첫날).
야간 드라이버 `cop_pipeline_advance.sh`(01:00)는 **STAGE=완료/유지** (50ep · 성공률 0.7 · 목표 0.90)
— Phase 1 cycle1 홀딩 유지, 파이프라인 재실행 없음. 본 회차 = STAGE 문서화 + Phase 2 W1 신규 작업.

## 실행 테스트 결과
- **DR 모듈 신규**: `samples/training/sim_domain_randomization.py` (headless, viewer 없음).
  - 3축 무작위화: 조명(diffuse 0.4~0.8 / ambient 0.2~0.45 / 광원위치 ±0.5m), 마찰(×0.7~1.3),
    카메라 가우시안 노이즈(std 2~10). 공개 API: `randomize_scene()` / `apply_camera_noise()`.
- **self-test 8샘플** (씬 `scene_grasp_pads.xml`, overhead_camera):
  - 적용범위 전부 설정범위 내 (diffuse 0.437~0.708, ambient 0.206~0.392, friction 0.802~1.208, noise 3.47~9.97).
  - DR 반영 증거: 프레임 평균밝기 110.3~124.7 가 light_diffuse 와 상관 + 노이즈 그레인 육안 확인.
  - 산출: `research/simulation/dr_samples/dr_sample_00~07.png` (640×480).
- **드라이버 STAGE 정합 재확인**: `rollout_summary.json` 7/10=0.70, median lift 43.7mm, scene=scene_grasp_pads.xml.

## 관찰 / 이슈
- 독립 무작위화 위해 매 샘플 모델 재로드(마찰 곱셈 누적 방지). 수집기 연결 시 동일 처리 필요(W1 잔여).
- 성공률 70% < 90% 홀딩 — DR 데이터 증대(50→200ep)가 향상과 Sim2Real robustness 양쪽의 후속.
- [자가치유] 없음 — 드라이버 STAGE 정상, 마커·산출물 정합, 운영 데이터셋(`data/episodes_cl` 50ep) 무손상, 에러 없음.

## 다음 단계
- **W1 잔여**: `randomize_scene()` 를 `sim_data_collector.py` 에피소드 reset + `render_act_rollout.py` 에 연결
  → DR 데이터셋(`data/episodes_cl_dr`) 합성·측정.
- **W2**: zero-shot 실기 추론 → 격차 측정 (외부의존: 장기헌 Orin/실기 SSH 수신 시). 미수신 시 DR on/off rollout 비교로 프록시 측정.

## 보고용 증거 후보
- [x] DR 모듈 3축 무작위화 + 8샘플 적용범위 검증 → 7월 보고서 [Sim2Real 준비]
- [x] Phase 1→2 전환: cycle1 70% 홀딩 유지 + Phase 2 W1 착수 → 7월 보고서 [진척]

---

## 야간 회차(23:00) — DR 를 수집기/측정기에 연결
> 01:00 회차가 DR 모듈을 작성했고, 본 야간 회차는 다음 W1 항목 **"DR 연결"** 을 진행.
> 드라이버(23:00) STAGE=완료/유지(50ep · 0.7) — 파이프라인 재실행 없음.

### 실행 테스트 결과
- **DR 연결(opt-in `--dr`, 기본 off → 드라이버 파이프라인 불변)**:
  - `sim_domain_randomization.py`: `snapshot_baseline`/`restore_baseline` 헬퍼 추가(friction 곱셈·light_pos 덧셈 누적 방지).
  - `sim_data_collector.py`: 에피소드 reset 마다 restore→randomize→`mj_setConst`, `record()` 에 카메라 노이즈.
  - `render_act_rollout.py`: rollout reset + 관측 RGB 에 DR, 결과는 `rollout_summary_dr.json` 별도 저장.
- **비파괴 스모크**:
  - 수집기 `--dr --root /tmp/... --episodes 2` → **2/2(yield 100%)**, friction 1.188 vs 0.802(누적 없음), lift 41.8/42.7mm.
  - 측정기 `--dr --rollouts 2` → **2/2 = 1.0**, median 45.4mm, `dr:true` → `rollout_summary_dr.json`.
- **무결성**: `episodes_cl` 50ep/3350frame 무손상, 운영 `rollout_summary.json` 7/10=0.70 불변, `py_compile` 3파일 OK, 서브모듈 변경 없음.

### 관찰 / 이슈 (야간)
- friction DR 은 expert yield 를 낮출 수 있으나 성공필터(lift≥40mm)가 실패 폐기 → 저장 시연은 유효. 조명/노이즈 DR 이 시각 Sim2Real 핵심.
- [자가치유] 없음 — 드라이버 STAGE 정상, 마커·산출물 정합, 운영 데이터셋 무손상, 에러 없음.

### 다음 단계 (야간)
- **W1 잔여(본격)**: `--dr` 로 DR 50ep 합성(`data/episodes_cl_dr`) → ACT 재학습 → DR on/off rollout 비교. 드라이버 사이클(수 시간)로 진행.
- 상세: `research/simulation/2026-07-01_phase2-w1-dr-wiring.md`.

---

## 야간 sim-test 회차(23:30) — 우선순위 스크립트 회귀 실행
> 코드 작업 없음. 오늘 산출물/우선순위 타깃을 **실제 실행**해 회귀·메트릭 수집. 운영 데이터 무접촉.

### 실행 테스트 결과 (.venv, headless)
| 스크립트 | 반복 | 실행성공 | 소요(avg) | 결과 |
|---|---|---|---|---|
| `sim_headless_6dof_video.py` | 3 | 3/3 (100%) | 9.1s | 2501프레임 → `sim_6dof_animation.mp4` 정상 |
| `sim_camera_verification.py` | 3 | 3/3 (100%) | 0.6s | overhead+gripper 2카메라 30프레임 동기 캡처 정상 |
| `sim_pick_place.py` | 3 | 3/3 실행 (grasp **0/3**) | 10.5s | 스크립트 정상(video+JSON), grasp task는 **open-loop 0%**(min_approach 0.323m ≫ 5mm) — 문서화된 legacy open-loop 기준선. closed-loop 해법은 `scripts/`/수집기 측 |
| `sim_data_collector.py` (스모크) | 3ep×임시루트 | yield **3/3=100%** | 14s/ep | closed-loop expert lift 41.5~43.2mm, 재시도1. **운영 루트 미접촉**(`--root /tmp` 사용, 기본 루트는 rmtree 하므로 회피) |

- **메트릭**:
  - 시뮬 실행 성공률(우선순위 4종): 12/12 스크립트 실행 성공. grasp task 성공률은 파이프라인 정책값(아래) 참조.
  - 추론/렌더 속도: 6dof 2501프레임 9.1s(≈275fps 오프스크린), camera 30프레임 0.6s, collector 14s/ep(67프레임+h264 인코딩 포함).
  - closed-loop 수집 yield: 100%(3/3), lift 41.5~43.2mm — 6/25 기준선(yield 91%, median 43.7mm)과 정합.
  - 메모리(Apple Silicon, GPU 없음): camera_verify 피크 RSS ~686MB, 6dof(2501프레임 버퍼) ~1.1GB. CPU-only.

### 무결성 재확인 (스모크 후)
- `data/episodes_cl`: 50ep / 3350frame **무손상** ✓
- `research/simulation/inference_progress/rollout_summary.json`(운영): 7/10 = **0.70** 불변 ✓
- `rollout_summary_dr.json`: 2/2(01:00~23:00 회차 스모크 산물, DR robustness 프록시) 격리 유지 ✓
- 임시 산출물(`_tmp_episodes_smoke*`, 러너) 삭제, `git status` clean. 서브모듈 변경 없음.

### 관찰 / 이슈 (23:30)
- `sim_pick_place.py`(legacy open-loop)는 grasp 0/3이 **정상 기대치** — Phase 1에서 규명된 open-loop 0% 기준선. 실 성능 지표는 closed-loop 수집기/`rollout_summary.json`(70%)이 대표.
- `sim_data_collector.py` 기본 루트 실행은 운영 `data/episodes_cl`을 rmtree 재생성하므로, 야간 회귀는 **반드시 임시 루트 스모크**로 한정(오늘 그대로 준수).
- [자가치유] 없음 — 우선순위 4종 전부 실행 성공, 운영 데이터/측정치 불변, 에러 없음.
