# 시뮬 진척 — 2026-06-27 (토요일)

## 오늘 진행 단계

Phase 1 - W4 - **closed-loop 자동수집 1사이클 완료/유지** (PHASE_ROADMAP L162, 6/22~6/30 범위).
야간 전진은 결정론적 드라이버 `scripts/cop_pipeline_advance.sh` 가 처리. 01:00 **STAGE=완료/유지** (데이터 50ep · 최종 성공률 0.7 · 목표 0.90).
본 회차 = STAGE 결과 검증·문서화·보고 + sim 환경 무결성 재확인 (파이프라인 재실행 없음).

## 실행 테스트 결과

- **드라이버 STAGE=완료/유지**: 마커 정합 → 재학습/재측정 없이 유지 (설계대로).
  - `logs/cop_trained_on.marker`=1782287560 / `logs/cop_measured.marker`=1782134830 → 현 체크포인트와 정합.
- **산출물 디스크 검증**:
  - `data/episodes_cl`: 50 ep / 3350 frame / so101 ✓
  - `checkpoints/act/epoch_0099/model.safetensors`: mtime 6/25 02:17, 336MB (closed-loop 신선) ✓
  - `research/simulation/inference_progress/rollout_summary.json`: 10 rollout 중 **7 성공 = 0.70**, median lift **43.7mm**, threshold 40mm ✓ (실패 3건 = rollout 2/5/8, lift 3.7~6.9mm)
  - `cop_rollout.log` 10/10 라인 ↔ summary 일치 ✓
- **환경 무결성 재확인** (재학습 아님):
  - `.venv/bin/python3` → mujoco 3.8.0 import ✓ (Apple Silicon)
  - `SO-ARM100/Simulation/SO101/scene_grasp_pads.xml` 존재 ✓
  - git 서브모듈 없음 → main repo 직접 커밋 안전 ✓

### 우선순위 스크립트 실제 실행 (23:30 sim-test, 신규)

01:00 드라이버 회차는 디스크/마커 대조만 했으므로, 23:30 회차에서 우선순위 4종을 **실제 N회 실행**해 동작·성능·메모리를 재측정했다.

| 스크립트 | 반복 | 성공률 | 평균 시간 | peak RSS | 비고 |
|---|---|---|---|---|---|
| `sim_headless_6dof_video.py` | 5 | **5/5 (1.00)** | 9.23s | ~1117MB | 2501 프레임 mp4 저장, 안정 |
| `sim_camera_verification.py` | 5 | **5/5 (1.00)** | 0.64s | ~688MB | overhead+gripper 2캠 30프레임 동기 캡처 |
| `sim_pick_place.py` (레거시 open-loop) | 5 | **0/5 (0.00)** | 10.81s | ~2939MB | 결정론적 실패, min_approach_dist=0.323m (그리퍼 큐브 미접근) — 알려진 레거시 결함 |
| `sim_data_collector.py` (closed-loop 스모크) | 5ep/scratch | **5/5 성공, yield 100%** | — | — | `--root data/episodes_smoke --episodes 5`, lift 43.6~46.1mm, 335프레임, robot=so101, info.json/chunk-000 정상. **측정 후 scratch 삭제, 운영 `data/episodes_cl`(50ep/3350frame) 무손상 확인** |

- 모든 실행 `.venv/bin/python3`(mujoco 3.8.0). `sim_viewer_6dof.py`는 headless 비호환으로 SKIP(규정대로).
- closed-loop 수집기 yield 100%(스모크)는 6/25 1사이클 수집 yield 91%와 정합 — 수집 파이프라인 정상 동작 재확인.
- 레거시 `sim_pick_place.py` 0%는 closed-loop 트랙(70%)과 무관한 구식 open-loop arc-sweep 결함(roadmap L148~151 규명 완료). 신규 작업 아님 — 회귀 모니터링용 기준선으로만 기록.

## 관찰 / 이슈

- **홀딩 상태가 정상 동작**: 마커(학습/측정 모델 서명)가 현 체크포인트와 일치하므로 드라이버가 STAGE=완료/유지 출력. 신규 사이클은 드라이버의 마커 리셋 / `COP_TARGET_EP` 상향 시에만 트리거 → 본 회차 수동 트리거하지 않음(드라이버 담당).
- **성공률 간극(70% < 90%)**: ACT 모방격차 + sim 데이터 50ep 한계. 90%+ 는 Sim2Real 진입 조건이라 데이터 증대(50→200ep)/씬 다양화가 자연스러운 후속.
- **미커밋 산출물**: `docs/01_overview/daily-reports/2026-06-26.html` (어제 일일 리포트 생성물) untracked 존재 → 함께 커밋.
- **23:30 실행 회차 결론**: 우선순위 3종(headless/camera/collector) 100% 정상, 레거시 pick_place만 0%(기존 결함). sim 환경·수집 파이프라인 실행 레벨에서 건재. 메모리 최대 ~2.9GB(pick_place, 60fps 5s 프레임 버퍼) — 16GB 머신 여유.
- [자가치유] 없음 — 드라이버 STAGE 정상, 4종 실행 정상(레거시 0%는 알려진 결함), 운영 데이터셋 무손상, 에러 없음.

## 다음 단계

- Phase 1 W4 dated 항목 사실상 종료. 남은 것 = PHASE_ROADMAP L168 "학습 모델 Orin Nano 배포" → 외부 의존(장기헌 SSH, external-dependencies 우선순위2) 미수신으로 이연. `[ ]` 유지.
- 7월 Phase 2(Sim2Real) 진입 전 sim 성공률 70%→90%+ 향상(데이터 증대/씬 다양화) — 다음 사이클은 드라이버가 마커 리셋/타깃 상향 시 자동 전진.

## 보고용 증거 후보
- [x] closed-loop rollout 70%(7/10, median lift 43.7mm) vs open-loop 0% → 6월 보고서 [2.사전학습]
- [x] sim 환경 무결성 재확인(mujoco 3.8.0 + 마커·산출물 정합) → 환경 안정성 근거 [2.시뮬환경]
- [x] 우선순위 4종 실행 회귀 측정(headless/camera/collector 100%, collector yield 100%, 레거시 pick_place 0% 기준선) → 파이프라인 실행 안정성 [2.시뮬환경]
