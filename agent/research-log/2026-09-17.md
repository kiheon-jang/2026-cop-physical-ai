# 시뮬 진척 — 2026-09-17 (목요일)

## 오늘 진행 단계
Phase 4 - W2 - RS232 ACT 재학습(케이블 출구 실기 정합 + 핀치 판정 재수집본) 진행 문서화 (Day 2, in-flight). 드라이버 STAGE=학습중.

## 실행 테스트 결과
- 드라이버 STAGE=**학습중**: pid 48360 alive(state `Us`=세션 분리, 04:04 killer 회피), `train_act.py --epochs 42` → `checkpoints/act_rs232_sim`, dataset=`episodes_rs232`(정정 재수집 100ep/16,093f). 라이브 **epoch 37/42**(36 완료) loss **0.00904**(l1 0.00784·kl 0.000119), from-scratch 곡선 정상(ep7 0.0431→ep37 0.0090).
- **체크포인트 신선도 직접 검증**(오늘 핵심): 드라이버 `ckpt_latest=epoch_0041`(Sep 13)·dir mtime Sep 12~13 이 언뜻 "새 run 미저장"으로 보였으나 → 내부 파일 mtime 확인: `epoch_0009/model.safetensors` **Sep 17 00:59**·`epoch_0029/model.safetensors` **Sep 17 17:04** 신선. 로그 `checkpoint_saved` epoch_0019/0029 기록 존재. dir mtime 불변 = `save_pretrained` 인플레이스 덮어쓰기 특성(결함 아님, 9/16·6/25 로그 기록 특성). → 정정 모델 정상 저장 중 확증. 잔여 5 epoch(~4h) → ETA ~03:00~03:30 KST 9/18.
- 마커 무결성: trained_on=`episodes_rs232:1789126035`(정정 前 승격)·pending=`episodes_rs232:1789243031`(대기·미승격)·measured=`episodes_rs232:1789243031`. 학습 미완→승격/측정 보류(설계대로)→baseline 무손상(`rollout_summary_rs232.json` Sep 15 23:02·seed42 0.9/0.7·4-seed 0.875/0.75·`cop_rollout.log` Sep 15 23:06 불변 = 야간 측정 미실행).

## 관찰 / 이슈
- **[자가치유] 없음** — 에러 없음, 학습 정상 수렴. 체크포인트 "미저장" 의심은 조사 결과 False alarm(내부 파일 mtime·로그로 정상 저장 확증).
- **신규 모델 측정 누락 위험 없음**: MODEL_SIG=`DS_BASE:최신 ckpt 내부 최신 mtime` 라, 재학습이 epoch_0041(Sep 13)을 ~03:00 덮어쓰면 mtime 갱신 → measured(`1789243031`)와 불일치 → 드라이버 자동 재측정(핀치 판정 + `*_nopinch_legacy` 병기). measured==pending 이어도 스킵 안 됨(dataset-sig 아닌 ckpt-mtime 기반).
- **이월 항목 해소(9/13~16 "다음 단계")**: 근단(x<0.19) 실패 분해용 플러그 초기좌표 덤프 = **이미 충족**. `render_act_rollout_rs232.py` per-rollout 레코드가 `"pcb": placement`(=`{x,y,yaw_deg,rejected}`) 덤프 중, 커넥터가 PCB body 자식이라 `pcb.x`=플러그 초기 x → 기존 summary/trajectory 에서 직접 근단 분해 가능. 측정기 코드 변경 불필요(ponytail: 존재 필드 재사용).
- **churn(9/13~15) 재발 없음 확인**: 9/16 낮 timeout 600/3600 + measured 마커 기록 조치 후, 오늘밤 STAGE=학습중은 churn 아닌 정정 재학습의 정상 전진.
- [의도적 skip] Phase 0 레거시 `sim_pick_place.py`/`sim_data_collector.py` 미실행(RS232 무관 + 학습 중 `data/episodes*` 쓰기 경합 회피).

## 23:30 sim-test 결과 (렌더 헬스 — 학습 비경합)

학습(pid 48360) 진행 중이라 `data/episodes*` 쓰기 경합 회피 → 읽기 전용 렌더 헬스체크만 실행(PNG/mp4 출력, 데이터셋 미접촉).

### 스크립트: sim_camera_verification.py
- 반복: 3회
- 성공률: 3/3 = **1.0**
- 메트릭: overhead 640×480 + gripper 320×240 각 30프레임/회 캡처 정상. 카메라 ID 조회·2카메라 동기 캡처 정상.

### 스크립트: sim_headless_6dof_video.py
- 반복: 1회
- 성공률: 1/1 = **1.0**
- 메트릭: 6-DoF 사인 구동 5초 → **2501프레임** 렌더 → `sim_6dof_animation.mp4` 저장 정상.

### 학습 프로세스 헬스 (재실행 아님, 관측)
- pid 48360 alive, state `Us`(세션 분리), cpu ~13%, rss ~0.38GB 평탄(OOM/누수 없음).
- 라이브 epoch **38/42** step 90 loss **0.00841**(l1 0.00741·kl 0.0001) — 23:00 시점 epoch 37 대비 정상 전진.
- [의도적 skip] `sim_pick_place.py`(Phase 0 레거시 known-fail)·`sim_data_collector.py`(데이터 쓰기 경합)·`sim_viewer_6dof.py`(headless 비호환).

## 다음 단계
- 학습 완주(~03:00 9/18) → 정정 epoch_0041 새 mtime → 드라이버 자동 재측정(핀치 + `*_nopinch_legacy`) → 4-seed(42/7/123/2026) 공정추정 → 새/옛 기준 병기 보고 → 사이트 갱신.
- 로드맵 855 `[ ]` RS232 DR 합성 + 재학습 — 정정 baseline 측정 확정 후 드라이버 새 사이클(stale baseline 비교 방지).
- 실기 실측 대기: HHT 플러그 분리력·잭스크류 체결, closeup 카메라(손목) 자세, 서보 강성.
- 미결(외부): Gmail 앱 비밀번호 재발급(메일 07:00 SMTP 535, 9/12~ 지속) — 사용자 조치 대기.
