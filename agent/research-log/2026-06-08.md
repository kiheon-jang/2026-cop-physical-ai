# 시뮬 진척 — 2026-06-08 (월요일)

## 오늘 진행 단계
Phase 1 - W3 사전 준비 (정규 W3 윈도우는 6/15~6/21). 6/8~14 가용 일자에 `scripts/train_act.py` 의 3개 플레이스홀더 중 `load_dataset()` 부터 실제 구현으로 채워 넣음 (어제 6/07 로그의 "다음 단계"에 명시된 항목).

## 실행 테스트 결과
- `scripts/train_act.py::load_dataset()` 플레이스홀더 → 실제 구현 교체.
  - delta_timestamps 생성: 관측은 과거 `n_obs_steps` 프레임 (음수 timestamp), 액션은 미래 `chunk_size` 프레임 (양수).
  - camera_keys 리스트를 `observation.images.<key>` 로 자동 확장.
  - DataLoader(batch_size=8, shuffle, num_workers=4, pin_memory, drop_last) 반환.
- 정적 검토 완료. `info.json` features 키와 매핑 일치 확인.

## 관찰 / 이슈
- [자가치유] `.venv/bin/python3` 런타임 실행이 본 세션 sandbox 권한으로 차단됨 (6/07 과 동일). 정적 변경만 적용하고 실행 검증은 6/15 W3 진입 시 일괄 수행.
- [자가치유] Obsidian 미러 `cp ... 00_AI_Wiki/CoP_PhysicalAI/2026-06/` sandbox 권한 차단 (6/07 과 동일). 사용자 수동 동기화 필요.
- [자가치유] `git add` / `git commit` / `git push` 가 본 세션 sandbox 권한으로 차단됨. 파일은 워킹트리에 작성 완료 (`scripts/train_act.py`, `research/simulation/2026-06-08_train-act-load-dataset.md`, `agent/research-log/2026-06-08.md`). 다음 크론 또는 사용자 수동 commit 필요. v3.2 harness 권한 설정 점검 권장.
- PHASE_ROADMAP W3 항목(6/15 시작) 은 아직 `[ ]` 유지 — 본 작업은 사전 준비이며 W3 정식 항목 완료가 아님.
- 어제 식별된 `sim_pick_place.py` 런타임 검증은 동일 sandbox 차단으로 또 보류.

## 다음 단계
- 2026-06-09~14: `train_act.py::build_model()` 구현 (ACTConfig 파라미터 매핑 확인 + device 이동 + 체크포인트 resume).
- 2026-06-15 (Phase 1 W3 D1): `train()` 루프 구현 + nohup 백그라운드 epoch 100 실행 착수.
- 보류: `sim_pick_place.py` 런타임 측정 (sandbox 권한 해제 시).

## 후속 크론 재시도 (동일 세션 재실행)
- `scripts/train_act.py` `pipeline_status.load_dataset` 값: `"placeholder"` → `"implemented"` 수정 (실제 구현 완료 사실 반영).
- [자가치유] `git add` / `cp` (Obsidian 미러) / `.venv/bin/python3` 호출 sandbox 차단 재확인 — 본 세션 권한 변화 없음. v3.2 harness 권한 설정 점검 필요 항목으로 누적.

## 23:30 nightly sim-test 재시도 기록
- 23:00 본 작업 세션과 동일한 Bash sandbox 차단 상태 유지 (`/Volumes/MARK_DATA/dev/2026-cop-physical-ai/.venv/bin/python3 ...`, `cp ... 00_AI_Wiki/...` 모두 승인 필요).
- 우선순위 스크립트 (`sim_headless_6dof_video.py`, `sim_camera_verification.py`, `sim_pick_place.py`, `sim_data_collector.py`) 런타임 실행 및 메트릭 수집 모두 보류.
- 파일 작성/편집은 가능 — INDEX.md 에 2026-06-08 섹션 추가, 본 로그에 재시도 기록 추가.
- [자가치유] v3.2 harness 권한 설정 재점검 필요 — 23:00/23:30 두 잡 연속 동일 차단. `.venv/bin/python3`(외부 심볼릭 링크 대상 경로) 와 Obsidian `cp` 가 allowlist 후보. `git` 단순 명령은 본 회차에서 동작 확인 — 차단 대상 아님으로 정정.

### 23:30 재실행 (두 번째)
- `.venv/bin/python3` 호출: 여전히 sandbox 차단. 심볼릭 링크 대상이 `/Users/markmini/.local/share/uv/python/cpython-3.14-macos-aarch64-none/bin/python3.14` 로 working dir 밖이라 차단됨이 확인됨 (`readlink .venv/bin/python3` → `python` → 외부 경로).
- `git` 단순 명령(`git status --short`)은 본 세션에서 동작 — 1차 23:30 기록의 "git 차단" 서술은 잘못. 정정.
- Obsidian 미러 `cp`: 차단 재확인 (working dir 밖 경로 쓰기 불가).
- 런타임 메트릭 측정 0건, 파일시스템 증거는 기존(`data/episodes/meta/info.json` 200 ep / 12400 frames) 동일.
- 본 회차 산출: 본 로그 정정 + INDEX 보강 + git commit/push 시도 (1차에서 누락된 항목).
- [자가치유] `git status --short`는 허용되나 `git add <files>` 는 본 세션 sandbox 권한으로 차단. 즉 git 명령 전체가 막힌 게 아니라 mutation(add/commit/push) 만 차단됨이 확인됨. 1차 23:30 기록 추가 정정. 결과적으로 `agent/research-log/2026-06-{07,08}.md`, `agent/report-evidence/2026-06/INDEX.md`, `research/simulation/2026-06-0{7,8}_*.md`, `scripts/train_act.py`, `samples/training/sim_{pick_place,data_collector}.py` 등 다수 산출물이 워킹트리에 누적된 상태로 미커밋. 사용자 수동 커밋 또는 harness write-mode allowlist 필요.

## 보고용 증거 후보
- [ ] `scripts/train_act.py::load_dataset()` 실제 구현 diff → 6월 보고서 [2.사전학습] 섹션 (W3 진입 사전 준비 증거)
