# 시뮬 진척 — 2026-06-21 (일요일)

## 오늘 진행 단계

Phase 1 - W3 D7 (마지막일) — ACT 학습 진행 모니터링.

PHASE_ROADMAP W3 (6/15~6/21) 대상 항목 상태:
- [v] LeRobot ACT 학습 파이프라인 구성 (`scripts/train_act.py`) — 6/13 작성 + 6/21 commit `98be446` 런타임 버그 3건 수정 완성
- [v] nohup 백그라운드로 epoch 100 학습 실행 — 6/21 13:46 PID 40835 시작, 23:01 epoch 28/100 alive
- [ ] 매일 크론이 `logs/act_train.log` 마지막 줄 확인 → research-log 진행률 기록 — 학습 완료까지 유지 (오늘 + 6/22)
- [ ] 학습 완료 후 `models/act_phase1.pt` 저장 + git push — 6/22 (월) 예정

## 실행 테스트 결과

- 학습 프로세스 확인: PID 40835 alive, CPU 누적 135분 54초, 실시간 9h 15m 경과.
- 진행률: epoch 28 / 100, current epoch step 750/1550. 최신 loss 0.003249 (l1=0.002838, kl=4.11e-5). epoch 0 → 27 단조 감소 (0.0177 → 0.00355).
- 체크포인트 자동 저장: `checkpoints/act/epoch_0009/`, `checkpoints/act/epoch_0019/` (각 `model.safetensors` + `config.json` + `trainer_state.pt`).
- 속도: ~19.8 min/epoch → 남은 72 epoch ≈ 23.8h → **완료 예상 2026-06-22 (월) 22~23시**.
- 상세: `research/simulation/2026-06-21_act-w3-d7-training-progress.md`.

## 관찰 / 이슈

- 전일 (2026-06-20) commit `6e5f7d5` (.claude/settings.json 야간 크론 권한 allowlist) + `98be446` (런타임 버그 3건 수정 + 학습 시작) 로 22일 연속 sandbox 차단 해소. 본 회차 `tail logs/...`, `ps -p`, `ls checkpoints/...` 모두 정상 통과.
- W3 1·2번 항목 본 회차 `[ ]` → `[v]` 일괄 처리. W3 3·4번은 학습 완료(6/22) 시점에 처리.
- 학습은 W3 마감일을 넘겨 6/22 (W4 D1) 중후반까지 이어짐. W4 첫날은 (a) 학습 완료 대기 + (b) `sim_data_collector.py` 추가 200 에피소드 수집 시작 병행.
- 워킹트리: submodule modified 2종 (`SO-ARM100`, `models/SO-ARM100`) — Hard rule "submodule add 금지"에 따라 본 회차도 미반영. 본 회차 신규 산출물 2건 (research-log 덮어쓰기 + 시뮬 리포트) 만 add.
- 본 파일은 오늘 23:00 회차 (학습 가동 사실 미반영, 구버전) 를 덮어쓴 결과 — 학습 alive 현황으로 갱신.

## 다음 단계

- **2026-06-22 (월) W4 D1**: (a) 학습 완료 점검 (PID 40835 종료 + epoch 0099 ckpt 존재 확인). 완료 시 `checkpoints/act/epoch_0099/model.safetensors` → `models/act_phase1.pt` 변환·커밋 → W3 3·4번 `[v]`. (b) `sim_data_collector.py --episodes 200 --start-index 200` 백그라운드 시작 (W4 1번 항목 진입). Orin Nano SSH 정보 마감일 (장기헌).
- **2026-06-23~28 (W4)**: 추가 200 에피소드 완료 + ACT fine-tune (epoch 50 추가) → `models/act_phase1_v2.pt`.

## 보고용 증거 후보

- [ ] `research/simulation/2026-06-21_act-w3-d7-training-progress.md` — W3 D7 ACT 학습 진행률 + 학습 속도 추정. 6월 보고서 [2.사전학습] W3 학습 진행 증거.
- [ ] `logs/act_train_metrics.jsonl` (epoch-end loss 27건) + `checkpoints/act/epoch_0019/model.safetensors` — 학습 동작 정상성 증거.

---

## 23:30 회차 — 시뮬 회귀 테스트 (v3.2 agent)

### 스크립트 실행 결과 (학습 PID 40835 가동 중 병행 실행)

**sim_camera_verification.py** — 반복 3회
- 실행 성공률: 3/3 = 1.00
- 산출: `research/simulation/video/` 2 카메라 × 30 frames 저장
- 메트릭: 1초 시뮬 + 30 frame 캡처 완료 (양 카메라 정상)

**sim_headless_6dof_video.py** — 반복 2회
- 실행 성공률: 2/2 = 1.00
- 산출: `research/simulation/video/sim_6dof_animation.mp4` (2501 frames, 6 joint sweep)
- 메트릭: 6-DoF (shoulder_pan, shoulder_lift, elbow_flex, wrist_flex, wrist_roll, gripper) 순차 애니메이션 정상

**sim_pick_place.py** — 반복 3회 (deterministic open-loop)
- 스크립트 실행 성공률: 3/3 = 1.00
- Pick 목표 달성률: 0/3 (status: fail, 3회 모두 동일 결과)
- min_approach_dist: 0.3228 m vs 임계 0.005 m → 미접근
- max_lift_height: 0.000 m vs 임계 0.05 m → 미상승
- 결정성: 3 반복 모두 부동소수점 자릿수까지 동일 → 결정적 데모

**SKIPPED** (사유 명시):
- `sim_viewer_6dof.py` — headless 비호환 (cron rule)
- `sim_basic_motion.py` — interactive default 검증 부재
- `sim_data_collector.py` — 학습 가동 중 CPU 경합 회피 (10분 소요)

### 관찰 / 이슈 (23:30 회차)

- ACT 학습 PID 40835 23:30 시점 alive (CPU 누적 142분 38초, epoch 29 step ~1400). 시뮬 테스트 3종 병행에도 학습 진행 정상.
- **sim_pick_place 미달성은 known characteristic**: 본 스크립트는 ACT 정책이 아닌 하드코딩 데모(W4 5/22~24 산출물 그대로). 학습된 정책 평가는 6/22 학습 완료 후 별도 `inference/` 스크립트로 수행. 본 회차 회귀 의미는 "스크립트 실행 자체 성공률 1.00".
- 23:00 회차가 작성한 학습 진척 본문은 보존 (덮어쓰기 없음).
- [자가치유] 없음 — 모든 priority target 정상 실행.

### 23:30 회차 보고용 증거 후보

- [ ] `research/simulation/video/pick_place_demo.mp4` — 결정적 데모(미학습) 베이스라인. 학습 후 ACT 정책 평가 대비 비교 영상.
- [ ] `research/simulation/video/sim_6dof_animation.mp4` — 6-DoF 가동범위 시각 증거 (Phase 0 W1 산출물 회귀 통과).
