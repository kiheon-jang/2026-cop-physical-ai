# 시뮬 진척 — 2026-07-15 (수요일)

## 오늘 진행 단계
Phase 2 - W2 - floor 배치다양성 재학습 8차 (**FD 가설 경험적 반증 + MPS OOM 재규명**, in-flight)

> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 외부의존 미수신 → 진입 불가 → floor sim 사이클 전진.
> 어제(7/14) "num_workers=0 발효 run"(pid 48167)이 **또 epoch 57 이상종료** → 워커 가설 붕괴.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): pid 48167(num_workers=0 fix **적용** 코드) **epoch 57 이상종료 감지**
  (`metrics 마지막 epoch 미달`) → 새 run **pid 73001**(`--epochs 100 --no-resume`, →`checkpoints/act_floor`,
  train_start 23:00:14) 재시작. shutdown = `1 leaked semaphore`(어제 21 → 오늘 1, 워커 소멸 방증).

## 실행 테스트 결과 (야간 에이전트, 비파괴 진단)
- **오늘의 반전 = FD 누수 가설 직접 반증**. `_fd_leak_probe.py`(신규): `episodes_floor` +
  num_workers=0 DataLoader 를 **4 full-epoch(각 418 step 완전 순회)** 재순회 → `/dev/fd` 측정:
  `baseline 6 / epoch0~3 전부 open_fds=6` = **데이터 경로 FD 완전 고정, 누수 재현 불가**.
  → 3일간(7/9·7/11·7/13) 쫓던 워커 FD 누수는 red herring.
- **crash 재규명 근거 3종**:
  1) num_workers=0 실제 적용(세마포어 21→1)에도 crash epoch = **57**(지난 7 run 과 동일 천장).
  2) 이번 run **OSError [Errno 24] traceback 부재** — epoch 57 도중(step 370/418) 사망 = SIGKILL 시그니처(OOM).
  3) epoch 시간 **완전 평탄 ~313~314s**(metrics 0~56) — 점진 slowdown 아님 → 고정량 자원 누적 후 급사.
- → **유력 원인 = MPS 메모리 누적 OOM**(16GB M5, torch MPS 할당자 캐시 미반환). 워커 FD 는 2차 증상.

## 관찰 / 이슈
- **[자가치유]** `train_act.py` epoch 루프 surgical 2건:
  - 완화: epoch 종료마다 `torch.mps.empty_cache()` + `gc.collect()`(mps 게이트, 정확도 무영향).
  - 계측: `epoch_metric["mps_mem"]=torch.mps.driver_allocated_memory()` — 다음 run 이 메모리 곡선 남겨
    OOM 가설 **직접 확증/반증**. (이전 3 fix 와 차이 = 가설을 계측으로 검증하는 점.)
  - 검증: `ast` OK · torch.mps API 존재 확인 · `--smoke` 완주(cpu, `mps_mem:null`, gc 무해).
  - **발효는 다음 run**: pid 73001 은 수정 이전 코드(오늘밤 ~57 재crash 예상) → 내일 드라이버 재시작이
    수정+계측 코드 로드 → 완주 또는 메모리 곡선 확보.
- **무결성 격리 유지**: target=`episodes_floor` · trained_on=`episodes_cl_dr:1783181837`(직전 승격값·불변) ·
  pending=`episodes_floor:1783324998`(대기·미승격) · measured=`episodes_cl_dr:1783346557`. 학습 미완 →
  승격/측정 보류(6/22 SILENT 반대·설계대로) → 운영 `rollout_summary.json`(act_cl_dr, seed42, **0.70** /
  median lift 50.2mm, 2026-07-07) **불변** → baseline 무손상.
- **datasets 전수 불변**: `episodes_floor`·`episodes_cl`·`episodes_cl_dr` 각 50ep/3350frame.
  ckpt act·act_cl_dr·act_floor 3자 분리. `checkpoints/act_floor` 최신 = `epoch_0059`(과거 run).

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **완주 시**: pending 승격 → `act_floor/epoch_0099` 측정 → floor-trained rollout →
  4-seed(42/7/123/2026) 공정추정 vs baseline 0.825 / DR-trained 0.800(배치 다양성이 천장 올리는가).
- **재crash 시**: 다음 run `mps_mem` 곡선으로 OOM 확정 → batch_size 축소 등 진짜 root fix.
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.

## 보고용 증거 후보
- [ ] FD 누수 가설 프로브 반증(open_fds 6 고정) → 8-run ~57 천장의 진짜 원인 MPS OOM 재규명 → 7월 보고서 [2.x] 학습 파이프라인 안정화(근본원인 정정)
- [ ] 계측(mps_mem 로깅) 추가로 다음 run 이 가설 자기검증 → 진단 방법론 개선
