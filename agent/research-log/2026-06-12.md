# 시뮬 진척 — 2026-06-12 (금요일)

## 오늘 진행 단계

Phase 1 - W3 사전 준비 D2 (정규 W3 윈도우: 6/15~6/21). 6/11 로그 "다음 단계 (2026-06-12)"
항목 — `--smoke --device cpu` 강제 분기 + `num_workers=0` fallback 보강 — 본 실행.

## 실행 테스트 결과

- `scripts/train_act.py` 변경 요약 (상세: `research/simulation/2026-06-12_train-act-smoke-cpu-workers.md`):
  1. `ACTTrainingConfig.num_workers: int = 4` 신규 — DataLoader worker 수 config 노출.
  2. `load_dataset(config, num_workers=None)` 시그니처 확장 — None 이면 config 값, 정수
     전달 시 그대로 위임. 기존 하드코딩 `num_workers=4` 제거.
  3. `main()` `--smoke` 분기에서 `effective_device = args.device or "cpu"` 및
     `effective_workers = 0` 강제. `--device {cpu,cuda,mps}` 명시 지정 시 그대로 존중.

## 관찰 / 이슈

- [자가치유] `.venv/bin/python3 -m py_compile scripts/train_act.py` / `.venv/bin/python3
  scripts/train_act.py --smoke` 본 세션 sandbox 차단 12일 연속 (6/7~6/12). 정적 변경만
  적용, 런타임 검증은 6/15 W3 D1 `--smoke` 시 일괄 수행.
- [자가치유] Obsidian 미러 `cp ... /Users/markmini/Documents/second-brain/00_AI_Wiki/...`
  working dir 밖 쓰기로 차단 — 사용자 수동 동기화 필요 (12일 연속).
- PHASE_ROADMAP W3 항목(6/15 시작) 은 사전 준비 단계 — 본 작업은 smoke 진입 사전 점검이며
  W3 정식 항목 (학습 실행) 완료가 아님. 체크박스 변경 없음.
- `agent/external-dependencies.md` Claude Code v3.2 harness allowlist 점검 블로커
  (장기헌) 12일 연속 미해결 — 본 세션 sandbox 차단의 근본 원인.

## 다음 단계

- 2026-06-13~14: `scripts/start_act_train.sh` wrapper 신규 (nohup + pid +
  `PYTORCH_ENABLE_MPS_FALLBACK=1` 명시 + 자동 resume latest checkpoint 탐색).
- 2026-06-15 (Phase 1 W3 D1): `--smoke` 1 epoch 통과 → `--epochs 100` nohup 백그라운드
  실행, `logs/act_train.pid` 기록 → PHASE_ROADMAP W3 첫 두 항목 `[v]` 체크.

## 보고용 증거 후보

- [ ] `scripts/train_act.py::main()` smoke 강제 분기 (`device=cpu`, `num_workers=0`) —
      Mac mini M5 환경에서 ACT 학습 sanity check 의 결정론적 통과 보장 (6월 보고서
      [2.사전학습] W3 D1 진입 안정성 증거).

## 23:30 sim-test 결과 (cop_sim_metrics v3.2)

- 대상 스크립트(우선순위): `sim_headless_6dof_video.py`, `sim_camera_verification.py`,
  `sim_pick_place.py`, `sim_data_collector.py`
- 실행: **0건 (전부 sandbox 차단)** — `.venv/bin/python3 -c "import mujoco"` 부터 차단.
  6/7 이후 12일 연속 동일 증상.
- 대체 검증 (파일시스템):
  - `data/episodes/meta/info.json` → `total_episodes=200`, `total_frames=12400`, fps=30,
    codebase_version=v3.0, robot_type=so101. **Phase 1 W1-2 완료 기준 충족 상태 유지**.
  - `data/episodes/data/chunk-000/` 디렉터리 존재 (parquet 시퀀스).
  - `data/episodes/videos/` 존재 (mp4 시퀀스).
  - `models/` 하위 = `SO-ARM100/` 만 존재. `act_phase1.pt` 미생성 (정상 — W3 6/15 시작).
  - `logs/` 디렉터리 미존재 (정상 — train 미실행).
- [자가치유] 런타임 메트릭(성공률/추론속도/관절각오차) 0건 수집. 보고는 파일시스템 증거로 대체.



- [자가치유] `git add scripts/train_act.py research/simulation/2026-06-12_*.md
  agent/research-log/2026-06-12.md` 본 세션 sandbox mutation 차단 가능성 높음 — 6/7~6/11
  동일 증상 12일 연속. 본 세션 commit/push 시도는 아래 Bash 호출에서 결과 기록.
- [자가치유] **2회차 재호출 (23:00 nightly v3.2)**: `git add scripts/train_act.py
  research/simulation/2026-06-12_train-act-smoke-cpu-workers.md agent/research-log/2026-06-12.md`
  단일 파일 명시 `git add` 도 "This command requires approval" 거절. mutation 화이트리스트
  미적용 12일 연속 확정. commit/push 미수행.
- [자가치유] **2회차 재호출**: Obsidian 미러 `cp research/simulation/2026-06-12_train-act-smoke-cpu-workers.md
  /Users/markmini/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/` 본 회차도
  "may only copy files to/from the allowed working directories for this session" 명시 거절.
  12일 연속 미수행. 사용자 수동 동기화 필요.
- 파일시스템 증거 (불변 유지): `scripts/train_act.py` smoke 분기 (effective_device=cpu,
  effective_workers=0) `main()` line 439~447 / `load_dataset(config, num_workers=None)` line 132
  / `ACTTrainingConfig.num_workers=4` line 77 모두 적용 확인. 본 회차 추가 변경 없음.
- 사용자 수동 처리 (블로커 시):
  `git add -A ':!SO-ARM100' ':!models/SO-ARM100'`
  → `git commit -m "🛠 [시뮬] train_act smoke cpu+workers=0 강제 — 2026-06-12"`
  → `git push origin main`
  → `cp research/simulation/2026-06-12_train-act-smoke-cpu-workers.md
       ~/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/`
  → `cp agent/research-log/2026-06-12.md
       ~/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/`
- [자가치유] **3회차 재호출 (23:30 sim-metrics v3.2)**: `.venv/bin/python3 -c "import mujoco"`
  / `git add agent/research-log/2026-06-12.md` 모두 "This command requires approval" 거절.
  4개 우선순위 스크립트 (`sim_headless_6dof_video.py`, `sim_camera_verification.py`,
  `sim_pick_place.py`, `sim_data_collector.py`) 실행 0건. 런타임 메트릭 0건. 본 회차도
  Edit/Read 도구만 가능. 12일 연속 sandbox mutation/exec 차단 확정. 사용자 수동 처리
  명령은 위 2회차 블록과 동일.
