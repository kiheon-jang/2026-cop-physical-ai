# 시뮬 진척 — 2026-06-15 (월요일)

## 오늘 진행 단계

Phase 1 - W3 D1 — ACT 학습 smoke 검증 + nohup epoch 100 백그라운드 학습 시작
(6/14 research-log "다음 단계" 항목 정확히 본 일자 절차).

PHASE_ROADMAP 대상 항목 (W3, 6/15~6/21):
- [ ] LeRobot ACT 학습 파이프라인 구성 (`scripts/train_act.py`)
- [ ] nohup 백그라운드로 epoch 100 학습 실행

## 실행 테스트 결과

- 상세: `research/simulation/2026-06-15_act-w3-d1-smoke-attempt.md`
- 정적 점검 (통과): `train_act.py` smoke 분기 (line 429~444, max_epochs=1 / max_steps=2 / device=cpu / workers=0), `start_act_train.sh` cold-start 로직 (line 58~69), `check_act_train.sh` glob 패턴 (line 47, 51) — 6/14 정합성 점검 결과 그대로 유효.
- 런타임 점검: 본 회차 미수행 (전체 sandbox 차단). smoke 통과 여부 미확정.
- PHASE_ROADMAP W3 첫 두 항목 체크박스 변경 **보류** — 런타임 검증 통과 전까지 `[ ]` 유지.

## 관찰 / 이슈

- [자가치유] `chmod +x scripts/start_act_train.sh scripts/check_act_train.sh` 본 회차 sandbox "requires approval" 거절 — 14일+2 연속. 두 스크립트 `-rw-r--r--` 유지. 사용자 수동 1회 필요.
- [자가치유] `.venv/bin/python3 scripts/train_act.py --smoke` 본 회차 sandbox 거절 — 14일+2 연속.
- [자가치유] `bash scripts/check_act_train.sh` 본 회차 sandbox 거절. 학습 미시작 상태 (`logs/` 미존재, `checkpoints/` 미존재) 는 디렉터리 stat 으로 간접 확인.
- [자가치유] Obsidian 미러 `cp ... /Users/markmini/Documents/second-brain/00_AI_Wiki/CoP_PhysicalAI/2026-06/` working dir 밖 쓰기 차단 — 14일+2 연속. 사용자 수동 동기화 필요.
- [자가치유] `git add` / `commit` / `push` 본 회차 mutation 차단 예상 — 14일+2 연속. 6/14 research-log "사용자 수동 처리" 블록의 `git add` 명령 그대로 유효 (오늘 신규 파일 2건 추가만).
- `agent/external-dependencies.md` Claude Code v3.2 harness allowlist 점검 블로커 (장기헌) 14일+2 연속 — 본 nightly 의 모든 런타임 실행 차단의 근본 원인. 신규 외부 의존성 추가 없음.

## 다음 단계

- **사용자 수동 (오늘 / 내일까지)**: `chmod +x` → `train_act.py --smoke` 통과 확인 → `start_act_train.sh --epochs 100` 백그라운드 실행. 그 시점에서 PHASE_ROADMAP W3 첫 두 `[ ]` → `[v]` 일괄 체크 후 commit.
- **2026-06-16 (화) W3 D2 nightly**: 사용자 수동 절차 통과 가정 시, `check_act_train.sh` 표준 status 블록 (`pid=N alive=yes ckpt_count=≥1 log_age_sec<86400`) 을 당일 research-log "## ACT 학습 진행률" 섹션에 append. 통과 미확인 시 동일 패턴 반복.
- **2026-06-22 (월) W4 D1**: ACT 학습 완료 대기 + 시뮬 추가 200 에피소드 생성 시작 (`sim_data_collector.py`).

## 보고용 증거 후보

- [ ] `research/simulation/2026-06-15_act-w3-d1-smoke-attempt.md` — W3 D1 smoke 절차 + sandbox 블로커 14일+2 연속 누적 기록. 6월 보고서 [2.사전학습] W3 일일 운영 절차 증거.

## 23:30 nightly (sim-test) 확인

- 23:30 cron (sim-test + metrics) 본 회차도 동일 sandbox 패턴: `.venv/bin/python3` / `chmod` / `git add` / `cp` 모두 "requires approval" 거절. `git status` 등 read-only 만 통과.
- 정적 확인: `logs/act_train.*` 미존재 (Glob), `checkpoints/**` 미존재 (Glob) → 사용자 수동 절차(`chmod +x` → smoke → `start_act_train.sh`) 아직 미시행 상태 그대로. 학습 미시작.
- 23:30 회차에서 신규 메트릭 수집 없음 — 23:00 회차의 정적 점검 결과가 본 일자 최종 상태.
