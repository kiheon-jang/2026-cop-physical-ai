# 시뮬 진척 — 2026-09-16 (수요일)

## 오늘 진행 단계
Phase 4 - W2 - 운영 점검: hermes 경고 마커 삭제 + **야간 재측정 churn(9/13~15) 근본원인 확정·해소** + 완료 항목 로드맵 반영.

## 실행 테스트 결과

### 1. hermes fleet_restart_pending 마커 삭제 (사용자 지시)
- 삭제 전: 9/7 04:01 생성, `expected_sha=2d7799275046ef5cd48f4a11dbff9c49cddee9ee` = 현재 hermes-agent HEAD(게이트웨이 pid 23312 가 같은 코드로 9/11 10:41 재시작) → **stale 확정**.
- 백업 후 삭제. 검증: `_pending_fleet_restart_needed()` **False**, `_receipt_reports_stale_runtime()` **False**(영수증 fleet state=current·sha=HEAD), `hermes cron list` 경고 출력 없음.

### 2. 야간 재측정 churn 근본원인 (9/13·14·15 3일 연속)
- `hermes cron runs 76b3cd4eb4fc`: 3일 모두 **exit 2 / top_level_crash — 드라이버가 300초 timeout**. 23:30 잡(cop_sim_test.py)은 정상 → 문서·커밋은 있었으나 **23:00 잡의 Claude 단계는 3일 미실행**(로드맵 미갱신 원인).
- 기전: `~/.hermes/scripts/cop_sim_env.py` 가 드라이버를 `timeout=300` 으로 호출. RS232 4-seed 측정은 **79.5s/seed × 4 ≈ 318s + 모델 로드** → 측정 산출물은 쓰이지만 드라이버가 죽어 stage 5 의 `cop_measured.marker` 기록에 도달 못 함 → 매 야간 동일 재측정.
- 조치: 드라이버 규칙(MODEL_SIG = `DS_BASE:최신 ckpt 내부 최신 mtime`)대로 `logs/cop_measured.marker` = `episodes_rs232:1789243031` 기록(옛 값 `episodes_s1:1786060554` 백업).
- 검증: 드라이버 재실행 → `STAGE=완료/유지  데이터 episodes_rs232 100ep · 최종 성공률=0.875 (목표 0.50)`, 2초 종료, `logs/cop_rollout.log` mtime 불변(재측정 없음).

### 3. 로드맵 W2 완료 항목 반영
- `[v] RS232 ACT 학습` — 9/13 04:57 42epoch 완주(wall 32.5h), ckpt epoch_0009~0041, 마커 승격.
- `[v] RS232 4-seed 측정` — epoch_0041 4seed×10: **부분성공 0.875(35/40) · 완전분리 0.75(30/40)**, 9/13·14·15 동일 재현. 완료 기준(부분성공 50%) 충족.
- `dashboard/build.py --json`: **target_progress 0.722 → 0.750** (time_elapsed 0.908, 시연까지 D-45).

## 관찰 / 이슈
- **churn 재발 방지 조치 적용(사용자 승인, 9/16 15:46)**: `~/.hermes/scripts/cop_sim_env.py` 드라이버 호출 `timeout=300 → 600`(백업 `.bak.20260916-timeout`, py_compile OK), hermes `cron.script_timeout_seconds 1800 → 3600`(백업 `config.yaml.bak.20260916`). 검증: `_get_script_timeout()` = **3600**(config 캐시는 파일 서명 기반이라 게이트웨이 재시작 불필요), `cop_sim_env.py --pipeline-only` 0.62s `STAGE=완료/유지 0.875`. 예산: 드라이버 600 + Claude 위임 1800 = 2400 < 3600.
- **메일 4일 연속 실패**: 07:00 잡 SMTP 535 BadCredentials(9/12~9/16) — Gmail 앱 비밀번호 재발급 필요(사용자 조치). 대시보드 데이터 빌드·git pull 자체는 정상.
- 측정 완전분리 0.75 는 9/11 근단 기하 상한 추정(≈37.5%)을 상회 — 정책이 근단 약세 배치도 상당수 처리.

## 다음 단계
- W2 잔여: RS232 DR 합성 + 재학습(드라이버 새 사이클 트리거 시) — 드라이버 timeout 반영 완료라 측정까지 한 실행에서 진행 가능.
- 측정기에 rollout 별 플러그 초기좌표 덤프 추가 → 근단(x<0.19) 실패 배치 분해(9/13 이월).
- 실기 실측 수신 시: 흔들기 모델 재검토, closeup 카메라(손목 장착) 정합.
