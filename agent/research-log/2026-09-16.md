# 시뮬 진척 — 2026-09-16 (수요일)

## 오늘 진행 단계
Phase 4 - W2 - 운영 점검: hermes 경고 마커 삭제 + **야간 재측정 churn(9/13~15) 근본원인 확정·해소** + 완료 항목 로드맵 반영.

## 실행 테스트 결과

### 1. hermes fleet_restart_pending 마커 삭제 (사용자 지시)
- 삭제 전: 9/7 04:01 생성, `expected_sha=2d7799275046ef5cd48f4a11dbff9c49cddee9ee` = 현재 hermes-agent HEAD(게이트웨이 pid 23312 가 같은 코드로 9/11 10:41 재시작) → **stale 확정**.
- 백업 후 삭제. 검증: `_pending_fleet_restart_needed()` **False**, `_receipt_reports_stale_runtime()` **False**(영수증 fleet state=current·sha=HEAD), `hermes cron list` 경고 출력 없음.

### 2. 야간 재측정 churn 근본원인 (9/13·14·15 3일 연속)
- `hermes cron runs 76b3cd4eb4fc`: 3일 모두 **exit 2 / top_level_crash — 드라이버가 300초 timeout**. 23:30 잡(cop_sim_test.py)은 정상 → 문서·커밋은 있었으나 **23:00 잡의 Claude 단계는 3일 미실행**(로드맵 미갱신 원인).
- 기전: `~/.hermes/scripts/cop_sim_env.py` 가 드라이버를 `timeout=300` 으로 호출. RS232 4-seed 측정은 **79.5s/seed × 4 ≈ 318s + 모델 로드** → 측정 산출물은 쓰이지만 드라이버가 죽어 stage 5 의 `cop_measured.marker` 기록에 도달 못 함 → 매 야간 동일 재측정.
- 조치: 드라이버 규칙(MODEL_SIG = `DS_BASE:최신 ckpt 내부 최신 mtime`)대로 `logs/cop_measured.marker` = `episodes_rs232:1789243031` 기록(옛 값 `episodes_s1:1786060554` 백업).
- 검증: 드라이버 재실행 → `STAGE=완료/유지  데이터 episodes_rs232 100ep · 최종 성공률=0.875 (목표 0.50)`, 2초 종료, `logs/cop_rollout.log` mtime 불변(재측정 없음).

### 3. 로드맵 W2 완료 항목 반영
- `[v] RS232 ACT 학습` — 9/13 04:57 42epoch 완주(wall 32.5h), ckpt epoch_0009~0041, 마커 승격.
- `[v] RS232 4-seed 측정` — epoch_0041 4seed×10: **부분성공 0.875(35/40) · 완전분리 0.75(30/40)**, 9/13·14·15 동일 재현. 완료 기준(부분성공 50%) 충족.
- `dashboard/build.py --json`: **target_progress 0.722 → 0.750** (time_elapsed 0.908, 시연까지 D-45).

## 관찰 / 이슈
- **churn 재발 방지 조치 적용(사용자 승인, 9/16 15:46)**: `~/.hermes/scripts/cop_sim_env.py` 드라이버 호출 `timeout=300 → 600`(백업 `.bak.20260916-timeout`, py_compile OK), hermes `cron.script_timeout_seconds 1800 → 3600`(백업 `config.yaml.bak.20260916`). 검증: `_get_script_timeout()` = **3600**(config 캐시는 파일 서명 기반이라 게이트웨이 재시작 불필요), `cop_sim_env.py --pipeline-only` 0.62s `STAGE=완료/유지 0.875`. 예산: 드라이버 600 + Claude 위임 1800 = 2400 < 3600.
- **메일 4일 연속 실패**: 07:00 잡 SMTP 535 BadCredentials(9/12~9/16) — Gmail 앱 비밀번호 재발급 필요(사용자 조치). 대시보드 데이터 빌드·git pull 자체는 정상.
- 측정 완전분리 0.75 는 9/11 근단 기하 상한 추정(≈37.5%)을 상회 — 정책이 근단 약세 배치도 상당수 처리.

## 다음 단계
- W2 잔여: RS232 DR 합성 + 재학습(드라이버 새 사이클 트리거 시) — 드라이버 timeout 반영 완료라 측정까지 한 실행에서 진행 가능.
- 측정기에 rollout 별 플러그 초기좌표 덤프 추가 → 근단(x<0.19) 실패 배치 분해(9/13 이월).
- 실기 실측 수신 시: 흔들기 모델 재검토, closeup 카메라(손목 장착) 정합.

## 오후 — RS232 케이블 출구 실기 정합 + 측정 판정 보강

### 1. 케이블 출구 정정 (커밋 8fa5adb)
- 사용자 지적: 실기는 케이블이 후드 **뒷면(커넥터 축)** 으로 빠지는데 시뮬은 옆(-y) 45mm 라 집게가 잡는 자리와 겹쳐 보인다.
- 실기 영상(`assets/reports/realtrack/rt_closeup.mp4`) 프레임 확인 → 후드 뒤로 곧게 빠진 뒤 HHT 단말기로 상승. 씬 XML `rs232_cable` 을 `fromto -0.031 0 0 → -0.076 0 0.008` 로, 3D 뷰어 2곳(오버뷰 히어로·3D 리플레이)도 축 방향으로 회전·재배치.
- 동작 영향 없음 확인: 케이블은 contype/conaffinity=0 시각 전용. 같은 시드 67000~67004 를 이전 씬/새 씬에서 각각 실행 → 5/5 성공·전부 1차 시도로 동일.

### 2. 재수집 + 재학습 (렌더 관측이 바뀌므로 필수)
- 옛(옆 케이블) 데이터셋 → `data/episodes.bak-rs232-sidecable-20260916` 백업. 같은 시드(20260911)로 재수집: 100/100, 시도 119, **yield 84%**, 16,093프레임, 에피소드 158~169.
- 점검: 동작 명령·관절값 **100% 실기 한계 안**, 저장 배치 구간 3/24/30/43, features 동일.
- 드라이버 STAGE=학습시작 → pid 48360, `--epochs 42 --no-resume`, dataset_root=`data/episodes_rs232`. 완료 예상 9/18 새벽.

### 3. 측정 판정 보강 (목표치·임계 불변)
- 확인된 결함: 측정기의 부분성공 판정이 집게 파지를 확인하지 않아, 닫은 집게로 후드를 누르고 끄는 비핀치 동작이 부분성공으로 잡혔다(점검 6회 중 2회).
- 조치: `render_act_rollout_rs232.py` 에 "임계를 넘는 그 스텝에 고정 jaw·가동 jaw 모두 후드 접촉" 조건 추가. 판정 임계(2.95/5.9mm)와 로드맵 완료 기준(부분성공 50%)은 **그대로**. 이 조건은 수치를 낮추기만 하므로 기준 완화가 아니다.
- 비교 가능성: 옛 방식 수치를 `success_rate_nopinch_legacy` / `full_rate_nopinch_legacy` 와 per-rollout `success_nopinch` · `pinch_steps` 로 병기(1사이클).
- 스모크(기존 ckpt, 1 rollout × 40프레임): 새 필드 정상 출력, 드라이버가 읽는 `success_rate` 는 마지막 키 유지.

## 다음 단계
- 재학습 완료(9/18 새벽) → 4-seed 측정 → 새 기준/옛 기준 수치 병기 보고 → 사이트 갱신.
- 실기 실측 대기: HHT 플러그 분리력·잭스크류 체결 여부, closeup 카메라(손목 장착) 자세, 서보 강성.

## 야간 (23:00) — RS232 ACT 재학습 진행 상태 문서화

### 드라이버 STAGE 결과 (재실행 아님, 관측만)
- STAGE=**학습중**. pid **48360** alive, state `Us`(세션 분리 = 04:04 killer 회피), 시작 17:13.
- `train_act.py --epochs 42` → `checkpoints/act_rs232_sim`. 라이브 **epoch 7/42** step 900 loss **0.0431**(l1 0.0205 kl 0.0023) 정상 수렴, log mtime 23:01.

### 디스크 검증
- 데이터셋 `data/episodes_rs232` = 100ep/16,093f (낮 재수집본, 케이블 출구 정정 반영).
- 체크포인트 epoch_0009~0041 (Sep 12~13) = 직전 완료 run(정정 前). 새 run 은 epoch 7 → 신규 ckpt 0개(첫 저장 epoch 9 미도달) = 정상.
- 마커: trained_on=`episodes_rs232:1789126035`(정정 前 완료)·pending=`episodes_rs232:1789243031`(학습중·대기)·measured=`episodes_rs232:1789243031`(낮에 기록한 현 MODEL_SIG).
- rollout 산출물 mtime 9/15 23:06 불변 = 야간 측정 미실행(STAGE=학습중, 설계대로). baseline 부분성공 0.875/완전분리 0.75 무손상.

### 헬스체크
- `sim_camera_verification.py` 1회 → **PASS** (top/closeup 30프레임, 학습과 동시 실행 정상).

### 관찰
- **churn(9/13~15) 종료 확인**: 낮에 timeout 600/3600 + measured 마커 기록 → 오늘밤 STAGE=학습중은 churn 이 아니라 낮 재학습의 정상 전진.
- **신규 모델 측정 누락 위험 없음**: MODEL_SIG = `DS_BASE:최신 ckpt mtime` 라 재학습이 epoch_0009+ 저장 시 mtime 갱신 → measured(옛 1789243031)와 불일치 → 드라이버가 epoch_0041 new 자동 재측정. (dataset-sig 기반 아님 → measured==pending 이어도 스킵 안 됨.)
- [의도적 skip] `sim_pick_place.py`/`sim_data_collector.py` 미실행 — Phase 0 레거시 grasp, RS232 무관.
- [자가치유] 없음(에러 없음, 학습 정상 수렴).

### 다음 단계
- 학습 완주(잔여 35epoch, ETA ≈ 9/18 새벽 KST) → 새 ckpt mtime → MODEL_SIG 갱신 → 드라이버 epoch_0041 new 측정(핀치 판정 + `*_nopinch_legacy` 병기).
- W2 잔여: RS232 DR 합성 + 재학습 (로드맵 854, 재학습 완주 후 드라이버 새 사이클).
- 상세: `research/simulation/2026-09-16_rs232-act-retrain-inflight.md`.

