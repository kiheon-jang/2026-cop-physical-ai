# 시뮬 진척 — 2026-07-03 (금요일)

## 오늘 진행 단계
Phase 2 - W1 - **DR 축별(per-axis) ablation**: 조명/마찰/카메라노이즈를 각각 단독 적용해
어느 섭동축이 grasp 를 흔드는지 분리 측정. (7/2 는 3축 aggregate 만 측정했음.)

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(01:00): **STAGE=완료/유지 · 데이터 50ep · 최종 성공률=0.7 (목표 0.90)**.
  새 사이클 미트리거(COP_TARGET_EP 미상향·DR 마커 미설정) → 파이프라인 수집/학습/측정 재실행 없음.
- 3자 정합: `data/episodes_cl` 50ep/3350frame · `rollout_summary.json` 7/10(불변) ·
  `logs/cop_measured.marker`/`cop_trained_on.marker` 존재.

## 실행 테스트 결과
- **코드(surgical)**: `sim_domain_randomization.py` `randomize_scene(axes=None)` 부분집합 인자 추가(하위호환,
  상수 `DR_AXES`) + `render_act_rollout.py` `--dr-axes` 플래그(부분집합 → `rollout_summary_dr_<axes>.json` 격리).
- **축별 ablation** (동일 seed 42, N=10, epoch_0099, 비파괴):

  | 조건 | 성공률 | median lift | 실패 rollout |
  |---|---|---|---|
  | DR-off (운영 대조군) | 0.70 (7/10) | 43.7mm | 2, 5, 8 |
  | light-only | 0.70 (7/10) | 43.8mm | 2, 5, 8 |
  | friction-only | 0.70 (7/10) | 43.4mm | 2, 5, 8 |
  | camera-noise-only | 0.70 (7/10) | 43.7mm | 2, 5, 8 |
  | 3축 aggregate (7/2) | 0.80 (8/10) | 44.1mm | 2, 8 |

- **세 축 각각 단독 → 성공률·실패집합 baseline 과 완전 동일(0.70, {2,5,8})** = 지배적 Sim2Real 섭동축 없음.
- 7/2 aggregate 의 +1(rollout 5)은 3축 동시에서만 → 임계값(40mm) 부근 marginal 노이즈(축별로는 5 여전히 실패).
- **결론: closed-loop 정책은 조명·마찰·카메라노이즈 각각에 독립적으로 강건.** 남은 실패는 섭동이 아니라
  특정 큐브배치 대응력(모방격차) → 데이터 증대로 접근할 문제.
- 상세: `research/simulation/2026-07-03_phase2-w1-dr-axis-ablation.md` (Obsidian 미러 완료).

## 관찰 / 이슈
- 큐브 초기위치 rng 는 DR rng 와 별도 스트림 → 축 선택과 무관하게 큐브 배치 동일(공정 비교 성립). 확인됨.
- 산출물은 신규 `rollout_summary_dr_{light,friction,camera}.json` 3개뿐 — 운영 `rollout_summary.json` 및
  aggregate `rollout_summary_dr.json` 불변, `data/episodes_cl` 무손상. 드라이버 파이프라인 재실행 없음.
- [자가치유] 없음 — 드라이버 STAGE 정상, 마커·산출물 3자 정합, 운영 데이터/측정치 불변, 에러 없음.

## 다음 단계
- **W1 본격 잔여**(변함없음): `sim_data_collector.py --dr` 로 DR 50ep 합성(`data/episodes_cl_dr`) → ACT 재학습 →
  DR-학습 모델 일반화 검증 (드라이버 사이클, 수 시간, COP_TARGET_EP 상향/DR 마커 트리거 시).
- 남은 구조적 실패({2,5,8})는 해당 큐브배치 커버하는 데이터 증대로 접근 — 드라이버 다음 사이클 후보.
- **W2**: zero-shot 실기 추론 → 격차 측정 (외부의존: 장기헌 Orin/실기 SSH 수신 시). 축별 강건성 균일 →
  실기 격차 크면 원인은 시각·물리 섭동보다 기구/캘리브레이션 쪽으로 좁혀짐.

## 보고용 증거 후보
- [x] DR 축별 ablation: light/friction/camera 각각 0.70 동등(실패집합 불변) → "지배적 Sim2Real 섭동축 없음,
  정책 축별 강건" → 7월 보고서 [Sim2Real 준비/robustness] 보강.

---

## 야간 회차(23:00) — 드라이버 STAGE 문서화 + 무결성 재확인
> 01:00 회차가 DR 축별 ablation 을 완료·문서화·미러·커밋(3c73867)했다.
> 본 23:00 회차는 드라이버 STAGE 결과 재확인 + 무결성 검증(코드/파이프라인 재실행 없음).

### 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): **STAGE=완료/유지 · 데이터 50ep · 최종 성공률=0.7 (목표 0.90)**.
  성공률 미달이나 새 사이클 미트리거(COP_TARGET_EP 미상향·DR 마커 미설정) → 파이프라인 수집/학습/측정 재실행 없음.

### 무결성 재확인 (.venv 직접 검증)
| 산출물 | 값 | 상태 |
|---|---|---|
| `data/episodes_cl/meta/info.json` | 50 episodes / 3350 frames | 무손상 ✓ |
| `rollout_summary.json` (운영 대조군) | success_rate 0.70, median 43.7mm, 실패 {2,5,8} | 불변 ✓ |
| `rollout_summary_dr_light.json` | 0.70, median 43.8mm, 실패 {2,5,8} | 01:00 산물 격리 ✓ |
| `rollout_summary_dr_friction.json` | 0.70, median 43.4mm, 실패 {2,5,8} | 01:00 산물 격리 ✓ |
| `rollout_summary_dr_camera.json` | 0.70, median 43.7mm, 실패 {2,5,8} | 01:00 산물 격리 ✓ |
| `logs/cop_measured.marker` / `cop_trained_on.marker` | 둘 다 존재 | 3자 정합 ✓ |
| `SO-ARM100` (nested git) | `.gitmodules` 없음 → main repo add 제외 | 규칙 준수 ✓ |

- 세 축 DR 요약 모두 운영 baseline 과 **성공률·실패집합 완전 일치({2,5,8})** — 재검증으로 ablation 결론
  (지배 섭동축 없음·정책 축별 강건) 재확정. 운영 summary 및 aggregate `rollout_summary_dr.json` 불변.

### 관찰 / 이슈 (23:00)
- 오늘 산출물(DR 축별 3종)은 01:00 회차에서 이미 커밋 완료 → 23:00 회차는 신규 sim 코드작업 없음(정상).
  W1 본격 잔여(DR 50ep 합성→재학습, 수 시간)는 드라이버 사이클 담당이라 야간 문서화 회차에서 미트리거.
- 추적 안 된 `docs/01_overview/daily-reports/2026-07-03.html`(일일 보고 산물) 감지 → 본 커밋에 포함.
- [자가치유] 없음 — 드라이버 STAGE 정상, 마커·산출물 3자 정합, 운영 데이터셋/측정치 불변, 에러 없음.

---

## 야간 회차(23:30) — 우선순위 스크립트 실제 실행 + 회귀 메트릭
> 01:00/23:00 회차는 DR 축별 ablation 문서화·무결성 검증이 중심이었다(코드 재실행 없음).
> 본 23:30 metrics 회차는 우선순위 4종 스크립트를 **실제 실행**해 회귀 baseline 을 재확인한다.

### 실행 테스트 결과 (.venv 직접 실행)

#### 스크립트: sim_headless_6dof_video.py
- 반복: 1회 (결정론적)
- 결과: **PASS** — 6관절(shoulder_pan/lift, elbow_flex, wrist_flex/roll, gripper) 정현파 구동,
  2501 프레임 렌더 → `research/simulation/video/sim_6dof_animation.mp4` 저장.

#### 스크립트: sim_camera_verification.py
- 반복: 1회 (결정론적)
- 결과: **PASS** — 오버헤드(640×480)+그리퍼(320×240) 2대 동시 오프스크린 캡처, 각 30프레임.

#### 스크립트: sim_pick_place.py (open-loop baseline)
- 반복: 2회 — 두 회차 **완전 동일**(결정론적, 무작위화 없음)
- 메트릭:
  - 성공률: **0/2 = 0%** (approach_ok=false, lift_ok=false) — open-loop 알려진 기준선 불변
  - min_approach_dist: **322.8mm** (임계 5mm), max_lift: 0mm (임계 50mm), exit=1
- 해석: open-loop expert 는 큐브에 ~32cm 못 미침(6/23 근본원인 그대로). closed-loop(70%) 대비 확인용 대조군.

#### 스크립트: sim_data_collector.py (closed-loop expert 스모크)
- 반복: 2 에피소드 (운영 `data/episodes_cl` 보호 위해 **throwaway root** `_smoke_datacoll_20260703` 로 격리 실행 후 삭제)
  - ⚠️ 이 스크립트는 대상 root 를 `shutil.rmtree` 후 재생성 → 기본 root(`data/episodes`) 실행 금지. 명시 `--root` 필수.
- 메트릭: **성공 2/2, yield 100%**, lift 43.2 / 43.7mm, grasp 재시도 각 1회, 프레임 67/ep.
- 해석: closed-loop 자동수집 expert 정상 가동(수집 6/25 사이클과 동일 수준). 스모크 후 throwaway 즉시 제거.

### 회귀 요약
| 스크립트 | 반복 | 성공률 | 상태 |
|---|---|---|---|
| sim_headless_6dof_video | 1 | — (렌더 2501f) | PASS ✓ |
| sim_camera_verification | 1 | — (2cam×30f) | PASS ✓ |
| sim_pick_place (open-loop) | 2 | 0% | 기준선 불변 ✓ |
| sim_data_collector (CL 스모크) | 2ep | 100% yield | 정상 ✓ |

### 무결성 재확인 (실행 후)
- `data/episodes_cl/meta/info.json` = 50 episodes / 3350 frames **불변** ✓
- `rollout_summary.json` success_rate 0.70 **불변** ✓
- throwaway `_smoke_datacoll_20260703` 삭제 완료, 운영 `data/episodes`·`episodes_cl` 무손상.
- 렌더 산출물(mp4/png)은 스크립트 하드코딩 경로 `/Users/markmini/Documents/dev/...` 로 저장 → git repo(`/Volumes/MARK_DATA/...`) 밖. repo 트래킹 영향 없음.

### 관찰 / 이슈 (23:30)
- 우선순위 4종 전부 headless 정상. `sim_viewer_6dof.py` 는 규칙대로 skip.
- [자가치유] 없음 — 4종 모두 에러 없이 완주, 운영 데이터/측정치 불변.
- [주의 기록] `sim_data_collector.py` 기본 root(`data/episodes`)는 실행 시 rmtree 대상 → 회귀 테스트는 반드시 `--root` 로 격리(본 회차 준수).
</content>
