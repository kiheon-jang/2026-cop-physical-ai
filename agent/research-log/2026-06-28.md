# 시뮬 진척 — 2026-06-28 (일요일)

## 오늘 진행 단계

Phase 1 - W4 - **closed-loop 자동수집 1사이클 완료/유지** (PHASE_ROADMAP L162, 6/22~6/30 범위).
야간 전진은 결정론적 드라이버 `scripts/cop_pipeline_advance.sh` 가 처리. 01:00 **STAGE=완료/유지** (데이터 50ep · 최종 성공률 0.7 · 목표 0.90).
본 회차 = STAGE 결과 검증·문서화·보고 (파이프라인 재실행 없음).

## 실행 테스트 결과

- **드라이버 STAGE=완료/유지**: 마커 정합 → 재학습/재측정 없이 유지 (설계대로).
  - `logs/cop_trained_on.marker`=1782287560 (mtime 6/24 17:12) / `logs/cop_measured.marker`=1782134830 (mtime 6/25 23:00) → 현 체크포인트와 정합.
- **산출물 디스크 검증**:
  - `data/episodes_cl`: 50 ep / 3350 frame / robot=so101 ✓
  - `checkpoints/act/epoch_0099/model.safetensors`: mtime 6/25 02:17, 336MB (closed-loop 신선) ✓
  - `rollout_summary.json`: 10 rollout 중 **7 성공 = 0.70**, median lift **43.7mm**, threshold 40mm ✓
    (실패 3건 = rollout 2/5/8, max_lift 3.7~6.9mm / 성공 7건 43.7~45.6mm → 이분 분포)
- **환경 무결성 재확인** (재학습 아님):
  - `.venv/bin/python3` → mujoco **3.8.0** import ✓ (Apple Silicon ARM64)
  - `SO-ARM100/Simulation/SO101/scene_grasp_pads.xml` 존재 ✓
  - git 서브모듈 없음 → main repo 직접 커밋 안전 ✓ / git status clean (어제 일일 리포트 HTML 커밋 완료)

## 실행 테스트 결과 (23:30 sim-test 에이전트 — 실제 스크립트 구동)

> 01:00 항목은 디스크/마커 정합 검증. 아래는 우선순위 4종 스크립트를 **실제로 N회 실행**한 결과.

### sim_headless_6dof_video.py
- 반복: 5회 — **성공률 1.00 (5/5)**
- 메트릭: wall 9.21s 평균(9.09~9.46), child maxrss ~1117MB. 2501프레임 6-DoF 애니메이션 mp4 정상 저장.

### sim_camera_verification.py
- 반복: 5회 — **성공률 1.00 (5/5)**
- 메트릭: wall 0.62s 평균(0.59~0.71). 두 카메라 30프레임 동시 캡처·저장 정상.

### sim_pick_place.py (레거시 open-loop)
- 반복: 5회 — **성공률 0.00 (0/5)** — 스크립트는 rc=0 정상 종료, task만 실패(설계상 기준선).
- 메트릭: wall 10.47s 평균, child maxrss ~2940MB. min_approach_dist 0.323m, max_lift 0.0mm.
- 해석: 고정 관절 open-loop expert = 문서화된 **0% 기준선**(closed-loop 대비). 회귀 아님.

### sim_data_collector.py (closed-loop) — 스모크
- 운영 데이터셋 `data/episodes_cl` 무손상 위해 **스크래치 루트(`data/_smoke_collector`, 3ep)** 로 구동 후 삭제.
- 결과: **성공 3/3, yield 100%**, lift 43.3~43.8mm, frames 67/ep, robot=so101, info.json 정합 ✓.
- 해석: closed-loop 수집기 정상 작동(기본 `--root data/episodes` 는 대상 삭제·재생성이므로 운영 루트 미지정). 

## 관찰 / 이슈

- **홀딩 상태가 정상 동작**: 마커가 현 체크포인트와 일치 → 드라이버 STAGE=완료/유지. 신규 사이클은 드라이버의 마커 리셋 / `COP_TARGET_EP` 상향 시에만 트리거 → 본 회차 수동 트리거하지 않음.
- **실행 테스트 4종 회귀 없음**: headless/camera 100%, collector yield 100%, pick_place 0%(기준선 일치). 6/27 기록과 동일 패턴.
- **성공률 간극(70% < 90%)**: ACT 모방격차 + sim 데이터 50ep 한계. 90%+ 는 Sim2Real 진입 조건이라 데이터 증대(50→200ep)/씬 다양화가 자연스러운 후속.
- **rollout 분포 관찰**: 성공/실패가 ~44mm vs ~0mm 이분 분포 → 간헐적 그립 실패 형태의 모방격차.
- [자가치유] 없음 — 드라이버 STAGE 정상, 산출물·마커 정합, 운영 데이터셋 무손상, 에러 없음.

## 다음 단계

- Phase 1 W4 dated 항목 사실상 종료. 남은 것 = PHASE_ROADMAP L168 "학습 모델 Orin Nano 배포" → 외부 의존(장기헌 SSH, external-dependencies 우선순위2) 미수신으로 이연. `[ ]` 유지.
- 7월 Phase 2(Sim2Real) 진입 전 sim 성공률 70%→90%+ 향상(데이터 증대/씬 다양화) — 다음 사이클은 드라이버가 마커 리셋/타깃 상향 시 자동 전진.

## 보고용 증거 후보
- [x] closed-loop rollout 70%(7/10, median lift 43.7mm) vs open-loop 0% → 6월 보고서 [2.사전학습]
- [x] sim 환경 무결성 재확인(mujoco 3.8.0 + 마커·산출물 정합) → 환경 안정성 근거 [2.시뮬환경]
