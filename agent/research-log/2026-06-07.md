# 시뮬 진척 — 2026-06-07 (일요일)

## 오늘 진행 단계
Phase 1 - W1-2 - 보강 (6/1~6/14 윈도우, 정규 W1-2 항목은 모두 `[v]` 완료된 상태). 어제 식별된 다음 단계인 `sim_pick_place.py` 성공 메트릭 보고 개선을 수행.

## 실행 테스트 결과
### 스크립트: samples/training/sim_pick_place.py
- 변경: 성공/실패 판정 로직 + 한 줄 JSON stdout 출력 + exit code 추가
- 성공 기준: 접근 ±5mm AND 들어올리기 +50mm (PHASE_ROADMAP W4 5/22~24와 동일 기준)
- 정적 검토 완료, 인접 시뮬 로직 미변경 (관측만 추가)
- [자가치유] 본 세션에서 `.venv/bin/python3` 직접 실행이 sandbox 권한으로 차단되어 런타임 검증 보류. 다음 크론(2026-06-08)에서 재검증 필요.

## 관찰 / 이슈
- W1-2 정규 항목은 어제(2026-06-06) 시점에 모두 `[v]` 완료된 상태. W3는 2026-06-15부터 시작. 따라서 PHASE_ROADMAP.md 자체 편집은 없음.
- W3 첫 항목용 `scripts/train_act.py`는 이미 플레이스홀더로 존재 (load_dataset/build_model/train TODO). 6/15 이전 잔여 일자에 점진적으로 구현 채워 넣을 여지 있음.
- [자가치유] Obsidian 미러 `cp ... 00_AI_Wiki/CoP_PhysicalAI/2026-06/` 가 sandbox 권한으로 차단됨. 사용자 수동 동기화 필요 (또는 hooks 권한 확장 검토).
- 경로 불일치: `sim_pick_place.py` 등 다수 파일이 stale 경로 `/Users/markmini/Documents/dev/...` 를 사용 (실제 repo는 `/Volumes/MARK_DATA/dev/...`). 심볼릭 링크가 존재해 동작은 하나, 추후 일괄 정리 필요. 오늘 범위 외이므로 미수정.

## 다음 단계
- 2026-06-08: 개선된 `sim_pick_place.py` 런타임 검증 (JSON 출력 + exit code 확인).
- 2026-06-08~14: W3 진입 전 `scripts/train_act.py` 의 `load_dataset` 부분 우선 구현 가능 (delta_timestamps + DataLoader 래핑).
- 2026-06-15: Phase 1 W3 본격 착수 — ACT 학습 파이프라인 + nohup 백그라운드 epoch 100 학습.

---

## 23:30 sim-test 추가 기록 (v3.2 nightly)

- **실행 시도 스크립트**: `sim_pick_place.py`, `sim_headless_6dof_video.py`, `sim_camera_verification.py`, `sim_data_collector.py`
- **[자가치유] 런타임 실행 차단**: 23:00 job과 동일하게 `.venv/bin/python3 ...` 호출이 본 세션 sandbox 권한으로 거부됨. 23:30 job에서도 재현됨 → harness 권한 설정 이슈로 추정. 다음 크론 또는 사용자 수동 실행 시 권한 검토 필요.
- **파일시스템 증거 확인 (대체 검증)**:
  - `data/episodes/meta/info.json`: codebase v3.0, robot=so101, **total_episodes=200**, total_frames=12400, fps=30, train split 0:200 — Phase 1 W1-2 데이터 합성 200 ep 완료 상태 유지 확인.
  - `data/episodes/data/chunk-000/` 존재 — LeRobot v3.0 chunk 포맷 정상.
  - `research/simulation/video/pick_place_demo.mp4` 등 기존 비디오 산출물 보존 확인.
- **메트릭** (이번 회차에 새로 측정한 것 없음, 기존 데이터셋 기준): 데이터셋 episodes=200, frames=12400, 평균 frames/ep=62 (≈2.07초 @ 30fps).
- **반복 실행 (5~10회)**: 수행 불가 (실행 차단). 0/0 success rate.

## 보고용 증거 후보
- [x] `data/episodes/meta/info.json` (200 ep, 12400 frames) → 6월 보고서 [텔레오퍼레이션 검증 / 데이터 수집] 섹션 (기존 인덱스에 이미 포함)
- [ ] `sim_pick_place.py` JSON stdout 성공률 (다음 크론에서 측정 가능 시) → 6월 보고서 [Pick 성공률] 섹션
