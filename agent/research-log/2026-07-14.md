# 시뮬 진척 — 2026-07-14 (화요일)

## 오늘 진행 단계
Phase 2 - W2 - floor 배치다양성 재학습 7차 (**num_workers=0 자가치유 발효 run**, in-flight)

> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 외부의존 미수신 → 진입 불가 → floor sim 사이클 전진.
> 어제(7/13) num_workers=0 자가치유 커밋 → 오늘 run 이 첫 발효.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): 어제 run(pid 8470) **epoch 58 이상종료 감지** → 재학습 재시도.
  - STAGE=학습시작 → **새 run pid 48167**(`--epochs 100 --no-resume`, →`checkpoints/act_floor`, train_start 23:00:41).
  - shutdown 시 `21 leaked semaphore` + `OSError [Errno 24]` (직전 run 8470 잔재, 워커 churn 누수 방증).

## 실행 테스트 결과 (야간 에이전트, 비파괴 관찰)
- **어제 run 크래시 확인**: pid 8470(num_workers=0 fix 미적용 코드 로드)이 예측대로 **epoch 58 크래시**
  — 지난 5 run 과 동일 ~58~59 천장 (로그 tail `{"epoch": 58, "step": 340}` 직후 leaked semaphore).
- **오늘 진척 = fix 발효 시점 전환**: pid 48167 은 num_workers=0 커밋(7/13) **이후** 디스크
  `train_act.py` 로드 → 수정 실제 적용.
  - 검증: L501~506 **비smoke else 분기** `effective_workers=0` (이 run 은 `--smoke` 없음 → else 진입 → workers=0),
    L196 `persistent_workers=workers>0` → 자동 False. **워커 재spawn 없음 → FD 누수 물리적 불가 → 100epoch 완주 기대.**
  - 현 pid 48167 alive, epoch 0 step 20 loss 35.99→22.58 정상 수렴.
- 환경: mujoco 3.8.0, .venv, Apple Silicon(GPU 없음, MPS/CPU).

## 관찰 / 이슈
- **[자가치유] 없음** — 어제 num_workers=0 fix 첫 발효가 오늘의 진척(코드 무변경). 이번 fix 는 증상지연
  아닌 **누수 원천(워커 churn) 제거**라 완주 확실성 높음. 이전 두 fix(persistent_workers 7/9,
  RLIMIT_NOFILE 7/11)는 재spawn 못 막아 무효 판명(6-run ~49~59 천장).
- **무결성 격리 유지**: target=`episodes_floor` · trained_on=`episodes_cl_dr:1783181837`(직전 승격값·불변) ·
  pending=`episodes_floor:1783324998`(대기·미승격) · measured=`episodes_cl_dr:1783346557`.
  학습 미완 → 승격/측정 보류(6/22 SILENT 반대·설계대로) → 운영 `rollout_summary.json`
  (act_cl_dr, seed42, **0.70** / median lift 50.2mm, measured 2026-07-07) **불변** → baseline 무손상.
- **datasets 전수 불변**: `episodes_floor`·`episodes_cl`·`episodes_cl_dr` 각 50ep/3350frame (info.json 확인).
  ckpt act·act_cl_dr·act_floor 3자 분리. `checkpoints/act_floor` 최신 = `epoch_0059`(7/11, 과거 run).

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **완주 시**: pending(`episodes_floor:1783324998`) 승격 → `act_floor/epoch_0099` 측정 → floor-trained rollout.
- **비교 검증**: floor-trained 4 seed(42/7/123/2026) 공정추정 vs baseline(0.825)·DR-trained(0.800) —
  배치 다양성이 성공률 천장을 올리는가(7/7 W1 처방 실증).
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.

## 보고용 증거 후보
- [ ] num_workers=0 근본치유로 6-run FD 천장 제거 → floor 재학습 완주 → 7월 보고서 [2.x] 학습 파이프라인 안정화
- [ ] 마커 2단계+ckpt 3자 격리로 학습중 baseline 무손상 유지 → 운영 무결성
