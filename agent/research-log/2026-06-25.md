# 시뮬 진척 — 2026-06-25 (목요일)

## 오늘 진행 단계

Phase 1 - W4 - **closed-loop 자동수집 1사이클 완주** (PHASE_ROADMAP L162, 6/22~6/30 범위 첫 `[ ]`).
야간 전진은 결정론적 드라이버 `scripts/cop_pipeline_advance.sh` 가 처리. 01:00 STAGE=**학습중**.
본 회차는 STAGE 결과를 검증·문서화·보고(파이프라인 재실행 없음).

## 실행 테스트 결과

- **학습 진행중**: ACT 재학습 pid **20078** alive. `epoch 85/100`, loss 0.00556(l1 0.00445, kld 0.000110),
  MPS, loss 단조감소 정상. 로그 mtime 01:00:50 활성. 잔여 15 epoch, ETA ~02:20.
- **데이터셋 정합 ✓**: run2 step 수 ~360–419/epoch (=`episodes_cl` 3350frame÷8≈419) → closed-loop 데이터 사용 확정.
  로그 L16421 config dump(dataset_root=`data/episodes`, 1550 step/epoch)는 run1(6/22 open-loop) 종료 요약 — 현 학습과 무관.
- **체크포인트 저장 ✓**: closed-loop run2 가 epoch_0009~0079 저장. `epoch_0079/model.safetensors` mtime 00:26(최신).
  epoch_0099 는 6/22 open-loop(run2 미도달). 디렉터리 mtime(6/21~22)은 save_pretrained 덮어쓰기 특성 — **파일 mtime 기준 정상**.
- **측정 대기**: `rollout_summary.json` = epoch_0009/2rollout/0%/6.4mm (6/24 17:30 스모크, open-loop baseline).
  closed-loop 유효 측정은 학습 완료 후 드라이버가 자동 수행.

## 관찰 / 이슈

- **관측 함정 2건(실제 결함 아님, 문서로 명확화)**:
  1. checkpoint **디렉터리** mtime 이 6/21~22 로 보여 "신규 저장 안 됨"으로 오해 가능 → 실은 파일(model.safetensors) mtime 00:26 으로 신선. `save_pretrained` 가 디렉터리 mtime 미갱신.
  2. 단일 `act_train.log` 에 run1(open-loop 200ep)·run2(closed-loop 50ep)가 append 되어 config dump 의 dataset_root=`data/episodes` 가 현 학습으로 오인될 소지 → step/epoch 규모로 run2=episodes_cl 확정.
- **측정-체크포인트 정합**: 학습 완료 시 epoch_0099 가 closed-loop 로 덮여 file-mtime 최신이 되어 find_latest_checkpoint 가 올바른 모델을 잡음 → 드라이버 설계대로 동작 예정.
- [자가치유] 없음 — 수집✓·학습 정상·산출물 무결·데이터셋/체크포인트 정합, 에러 없음.

## 다음 단계

- **2026-06-25 ~02:20 이후**: 학습 완료 → 다음 cron 회차 드라이버 측정 스테이지가 closed-loop 모델 10-rollout 측정 → `rollout_summary.json` 갱신. 유효 Pick 성공률 확보 시 PHASE_ROADMAP L162 `[v]` + Phase 1 W4 완료 기준(🔄) 닫기.
- **외부 의존**: Orin Nano SSH(장기헌, external-dependencies.md) 미수신 → 배포 단계 이연 유지.

## 보고용 증거 후보
- [ ] closed-loop ACT 재학습 loss 곡선(open-loop final 0.0012 대비 50ep closed-loop 수렴) → 6월 보고서 [2.사전학습]
- [x] (학습 완료 후) closed-loop rollout 성공률 **70%** vs open-loop `sim_pick_place.py` 0% 대비 → 6월 보고서 [2.사전학습]

---

# 추가 회차 — 23:00 STAGE=측정 (1사이클 완주)

01:00 시점 "학습중"이던 closed-loop 1사이클이 **완주**됨. 드라이버 STAGE=측정 결과를 문서화·보고.

## 실행 테스트 결과 (완주)

- **수집 ✓**: closed-loop expert 50/50 (시도 55, yield 91%) → `data/episodes_cl` 3350 frame.
- **학습 ✓**: ACT 100 epoch 완료(`act_train.log` "학습 100 epoch 완료"), final loss **0.004564**(l1 0.00377/kld 7.98e-5),
  wall ≈9.1h, pid 20078 종료. `epoch_0099/model.safetensors` mtime **02:17** = closed-loop 신선(타임스탬프로 확정,
  어제 예측대로 학습완료 시 epoch_0099 가 closed-loop 로 덮여 file-mtime 최신이 됨).
- **측정 ✓**: `rollout_summary.json`(mtime 23:00) = 10 rollout 중 **7 성공 = 70%**, median lift **43.7mm**,
  scene_grasp_pads, threshold 40mm. 영상 `inference_epoch_0099_20260625.mp4`. 실패 3건(rollout 2/5/8) 전부 lift<7mm.

→ **open-loop 0% → closed-loop 70%** end-to-end 입증. 상세: `research/simulation/2026-06-25_closed-loop-cycle1-complete.md`.

## 관찰 / 이슈 (완주)

- 01:00 회차가 명시한 "관측 함정 2건"·"측정-체크포인트 정합" 예측이 그대로 적중(epoch_0099 file-mtime 02:17 신선,
  find_latest_checkpoint 가 올바른 closed-loop 모델 선택). 드라이버 설계대로 동작.
- **정책 70% < expert 88%/75% (ACT 모방격차)**; **sim 90%+ 목표(Sim2Real 조건)와 간극** → 데이터증대/씬다양화 후속 여지.
- [자가치유] 없음 — 전 구간 정상, 산출물 무결, 에러 없음. 신선도는 타임스탬프로 검증 완료.

## 다음 단계 (갱신)

- PHASE_ROADMAP L162 `[v]` 클로즈 + Phase 1 W4 완료 기준 🔄→✅(70%). 남은 Phase 1 항목 = L166 Orin Nano 배포뿐
  → 외부 의존(장기헌 SSH) 미수신으로 이연.
- 7월 Phase 2(Sim2Real) 진입 전 sim 성공률 향상이 자연스러운 후속.

---

# 추가 회차 — 23:30 sim-test (스크립트 실행 검증)

> sim-test 에이전트(v3.2)가 23:00 산출물 위에서 **오늘의 sim 스크립트를 실제 실행**해 무결성·성능 측정.
> 파이프라인 재실행 없음. 1사이클 결과는 23:00 회차가 이미 문서화 → 본 회차는 산출물 디스크 검증 + 스크립트 회귀 실행.

## 실행 테스트 결과

### 스크립트: sim_headless_6dof_video.py
- 반복: 2회 (결정론적, 동일 산출)
- 결과: 성공 2/2 (성공률 1.0)
- 메트릭:
  - 생성 프레임: 2501 frame
  - 실행 시간: ~9.3s/run (user 7.28s, 89% CPU, single-process)
  - 산출: `research/simulation/video/sim_6dof_animation.mp4` 저장 정상

### 스크립트: sim_camera_verification.py
- 반복: 3회
- 결과: 성공 3/3 (성공률 1.0)
- 메트릭:
  - 캡처: 30 frame × 2 cam(overhead 640×480 + gripper 320×240) = 60 img/run
  - 실행 시간: 0.58~0.69s/run (offscreen Renderer, viewer 미호출 → headless 안전)

### 스크립트: sim_pick_place.py (open-loop baseline)
- 반복: 2회 (결정론적, 동일 산출)
- 결과: 성공 0/2 (성공률 **0.0** — 문서화된 open-loop 근본결함 재현)
- 메트릭:
  - min_approach_dist: 0.3228m (큐브에 ~32cm 못 미침), max_lift: 0.0m
  - approach_ok=false, lift_ok=false → 6/22~23 규명한 단일조 호 스윕 결함과 일치
  - **대조**: closed-loop 정책 70%(7/10) vs open-loop 0% — end-to-end 격차 재확인

### 스크립트: sim_data_collector.py (closed-loop, 스모크)
- 반복: 2 에피소드 (`--root /tmp/cop_smoke_episodes --episodes 2`, 본 데이터 미손상)
- 결과: 성공 2/2 (yield **100%**), lift 43.5mm, grasp 재시도 1회
- 메트릭: closed-loop expert 수집기 정상 가동 확인

## 산출물 디스크 검증 (1사이클)
- `data/episodes_cl/meta/info.json`: 50 ep / 3350 frame / 30fps / so101 ✓ (23:00 문서와 일치)
- `rollout_summary.json`: 10 rollout, 7 success, rate **0.70**, median lift **43.7mm**, epoch_0099, cpu ✓

## 관찰 / 이슈
- 모든 우선 타깃 스크립트 정상 실행. 환경: mujoco 3.8.0, .venv, Apple Silicon(메모리만, GPU 없음).
- **하드코딩 경로 관측(결함 아님)**: 스크립트 다수가 `/Users/markmini/Documents/dev/2026-cop-physical-ai`(구 경로)를
  하드코딩하나 해당 경로가 실존(별도 사본)하여 실행 성공. 작업 디렉터리는 `/Volumes/MARK_DATA/dev/...`.
  산출 영상은 구 경로 video/ 로 저장됨 — 향후 경로 통일 시 정리 후보.
- [자가치유] 없음 — 23:00 산출물 전부 존재, 스크립트 전부 통과, 산출물 디스크 정합.

## 다음 단계
- L162 `[v]` 클로즈 상태 유지. 남은 Phase 1 = L166 Orin Nano 배포(외부 의존 이연).
- 7월 Phase 2 진입 전 sim 성공률 향상(50→200ep 데이터 증대/씬 다양화).

## 보고용 증거 후보
- [x] closed-loop rollout 70%(7/10) vs open-loop sim_pick_place.py 0% 실측 대조 → 6월 보고서 [2.사전학습]
- [x] sim 스크립트 회귀 통과(6dof/camera/collector 100%) → 환경 안정성 근거 [2.시뮬환경]
