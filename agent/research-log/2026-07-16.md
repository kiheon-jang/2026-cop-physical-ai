# 시뮬 진척 — 2026-07-16 (목요일)

## 오늘 진행 단계
Phase 2 - W2 - floor 배치다양성 재학습 9차 (**MPS-fix + mps_mem 계측 발효 run**, in-flight)

> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 외부의존 미수신 → 진입 불가 → floor sim 사이클 전진.
> 어제(7/15) 8차 run(pid 73001, mps-fix 미적용)이 **epoch 56 이상종료** → 예측대로 발효는 오늘 run.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): pid 73001(mps-fix **미적용** 코드) **epoch 56 이상종료 감지**
  (`metrics 마지막 epoch 미달`, tail `{"epoch":56,"step":270}` 후 `1 leaked semaphore`) → 새 run
  **pid 7243**(`--epochs 100 --no-resume`, →`checkpoints/act_floor`, train_start 23:00:22) 재시작.

## 실행 테스트 결과 (야간 에이전트, 비파괴 진단)
- **오늘의 진척 = mps-fix 발효 시점 전환**. pid 7243 은 mps-fix 커밋 `32fcb5d`(7/15) **이후** 디스크
  `train_act.py` 로드 → 수정 실제 적용. 코드 검증:
  - L395 `mps_mem = torch.mps.driver_allocated_memory()` — epoch별 MPS 메모리 계측.
  - L414 `torch.mps.empty_cache()` + `gc.collect()` — epoch 종료마다 캐시 반환(완화).
  - L509/517 `effective_workers = 0`(비smoke, num_workers=0 유지).
- **의의**: 8-run 을 ~56~59 에서 죽인 천장에 대해 **처음으로 완화+계측 동시 적용**.
  완주 시 = MPS 캐시 미반환이 진짜 원인 실증(해소). ~57 재crash 시 = `mps_mem` 곡선으로
  **OOM 직접 확증/반증 → batch_size 축소 등 root fix** 로 직행. 어느 쪽이든 워커 FD red-herring 루프 탈출.
- 현 epoch 0 step 30 loss 28.5→14.2 정상 수렴, pid 7243 alive.

## 관찰 / 이슈
- **[자가치유] 없음** — 어제 mps-fix(empty_cache + mps_mem 계측) 첫 발효가 오늘의 진척. 코드 변경 불필요.
- **무결성 격리 유지**: target=`episodes_floor` · trained_on=`episodes_cl_dr:1783181837`(불변) ·
  pending=`episodes_floor:1783324998`(대기·미승격, mtime 7/16 23:00) · measured=`episodes_cl_dr:1783346557`.
  학습 미완 → 승격/측정 보류(6/22 SILENT 반대·설계대로) → 운영 `rollout_summary.json`(act_cl_dr, seed42,
  **0.70** / median lift 50.2mm, 2026-07-07) **불변** → baseline 무손상.
- **datasets 전수 불변**: `episodes_floor`·`episodes_cl`·`episodes_cl_dr` 각 50ep/3350frame.
  ckpt act·act_cl_dr·act_floor 3자 분리. `act_floor` 최신 = `epoch_0059`(과거 run, crash56<59 → 신규 상위 ckpt 없음).

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **완주 시**: pending 승격 → `act_floor/epoch_0099` 측정 → floor-trained rollout →
  4-seed(42/7/123/2026) 공정추정 vs baseline 0.825 / DR-trained 0.800(배치 다양성이 천장 올리는가).
- **재crash 시**: 이 run 의 `mps_mem` 곡선으로 OOM 확정 → batch_size 축소 등 진짜 root fix.
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.

## 보고용 증거 후보
- [ ] mps-fix(empty_cache) + mps_mem 계측 발효 run → 완주 또는 OOM 곡선 확보 → 7월 보고서 [2.x] 학습 파이프라인 안정화(근본원인 정정 후 실측)

---

## 23:30 sim-test 회귀 (야간 sim-test 에이전트, 비파괴 실행)

우선순위 4종 실행. 운영 데이터셋 무손상(collector 는 tmp root 격리).

### 스크립트: sim_camera_verification.py
- 반복: 1회 — **PASS**. 30 frames × 2 카메라 캡처, `research/simulation/video/` 저장.

### 스크립트: sim_headless_6dof_video.py
- 반복: 1회 — **PASS**. 6관절 애니 2501 frames → `sim_6dof_animation.mp4` 저장.

### 스크립트: sim_pick_place.py
- 반복: 2회 — **결정론 확인**(min_approach_dist=0.3228m 두 rep 동일). status=fail.
- 알려진 open-loop 고정포즈 expert 한계(큐브 ~32cm 미달) — 실제 grasp 는 closed-loop collector 사용. 회귀 아님.

### 스크립트: sim_data_collector.py
- 반복: `--root /tmp/... --episodes 2`(격리 smoke) — **성공 2/2, yield 100%, lift 47.0mm, grasp재시도 1**.
- closed-loop 수집 파이프라인 정상. tmp root 정리 완료(운영 `episodes_cl`/`episodes_floor` 미접촉).

### 스크립트: joint_angle_comparison_sim.py (inference)
- 반복: 1회 — 시뮬 6관절 최대오차 **0.0244°(elbow_flex) < ±1°** 목표. 시뮬 관절각 정합 유지.

### 메트릭 요약
- 시뮬 동작 성공률: camera 1/1 · 6dof 1/1 · collector(smoke) 2/2(100%) · pick_place 0/2(open-loop 알려진 한계)
- 시뮬-실기 관절각 오차: 최대 0.0244° (< ±1° 목표)
- 시뮬 lift: collector smoke 47.0mm (임계 40mm 상회)
- 학습 진척(측정 시점): pid 7243 epoch 6, loss 0.58 정상 수렴 · RSS ~314MB

### 관찰
- floor 재학습 pid 7243 in-flight, epoch 6 통과(어제 crash epoch 56 아직 미도달 — 완주 여부는 내일 판정).
- 운영 rollout_summary.json 0.70/50.2mm 불변 재확인.
