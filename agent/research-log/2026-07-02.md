# 시뮬 진척 — 2026-07-02 (목요일)

## 오늘 진행 단계
Phase 2 - W1 - **DR 연결의 "측정" 절반**: DR on/off rollout 프록시 비교로 closed-loop 정책의
DR 강건성 측정. (DR 50ep 합성→재학습은 드라이버 사이클 담당.)

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(01:00): **STAGE=완료/유지 · 50ep · 최종 성공률=0.7 (목표 0.90)**.
- 3자 정합 확인: `data/episodes_cl` 50ep/3350frame · `rollout_summary.json` 7/10=0.70(mtime 6/25) ·
  `logs/cop_measured.marker`/`cop_trained_on.marker` 존재. 새 사이클 미트리거 → 파이프라인 재실행 없음.

## 실행 테스트 결과
- **DR on/off 프록시 측정** (동일 seed 42, N=10, epoch_0099, 비파괴):
  ```
  .venv/bin/python3 scripts/render_act_rollout.py --checkpoint checkpoints/act/epoch_0099 --rollouts 10 --seed 42 --dr
  ```
  | 조건 | 성공률 | median lift | 실패 rollout |
  |---|---|---|---|
  | DR-off (운영 대조군) | 0.70 (7/10) | 43.7mm | 2,5,8 |
  | DR-on (신규) | **0.80 (8/10)** | 44.1mm | 2,8 |
  - DR-on 이 +1 성공(노이즈 범위) → **성능 동등** → 추론-시점 조명/마찰/카메라노이즈 섭동이 grasp 미붕괴.
  - **결론: closed-loop 정책이 시각·물리 섭동에 강건 → 긍정적 Sim2Real 신호.**
  - DR-off 기준선은 **재실행 안 함**(운영 7/10 을 대조군으로 사용) — 동일 seed 로 DR-on 만 신규 실행.

## 관찰 / 이슈
- 실패 rollout 2·8 은 두 조건 공통(seed 공통) → 특정 초기 큐브 배치의 구조적 실패이지 DR 유발 아님.
- 오늘 프록시는 **추론-시점** 강건성만 측정. DR 데이터로 **재학습**하는 본격 검증(일반화·격차 축소)은
  드라이버 사이클(50ep 합성→재학습, 수 시간) 담당 — 오늘 STAGE=완료/유지라 미트리거.
- [자가치유] 없음 — 드라이버 STAGE 정상, 마커·산출물 3자 정합, 운영 `rollout_summary.json` 7/10 불변,
  `data/episodes_cl` 50ep 무손상, 변경 파일은 `rollout_summary_dr.json` 단 하나, 서브모듈 무변경, 에러 없음.

## 다음 단계
- **W1 본격 잔여**: `sim_data_collector.py --dr` 로 DR 50ep 합성(`data/episodes_cl_dr`) → ACT 재학습 →
  DR-학습 모델 vs 기준 성공률 비교 (드라이버 사이클, COP_TARGET_EP 상향 또는 DR 마커 트리거 시).
- **W2**: zero-shot 실기 추론 → 격차 측정 (외부의존: 장기헌 Orin/실기 SSH 수신 시). 미수신 시 DR 학습 on/off 비교가 프록시.

## 보고용 증거 후보
- [x] DR on/off 프록시 측정: 정책 강건성 확인(0.70→0.80 동등, 섭동 무붕괴) → 7월 보고서 [Sim2Real 준비/robustness]

---

## 야간 회차(23:00) — 드라이버 STAGE 문서화 + 무결성 재확인
> 01:04 회차가 DR on/off 프록시 측정을 완료·문서화·미러·커밋(de1eca3)했다.
> 본 23:00 회차는 드라이버 STAGE 결과 재확인 + 무결성 검증(코드/파이프라인 재실행 없음).

### 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): **STAGE=완료/유지 · 데이터 50ep · 최종 성공률=0.7 (목표 0.90)**.
  성공률 미달이나 새 사이클 미트리거(COP_TARGET_EP 미상향·DR 마커 미설정) → 파이프라인 재실행 없음.

### 무결성 재확인 (.venv 직접 검증)
| 산출물 | 값 | 상태 |
|---|---|---|
| `data/episodes_cl/meta/info.json` | 50 episodes / 3350 frames | 무손상 ✓ |
| `rollout_summary.json` (운영 대조군) | success_rate 0.70, median lift 43.7mm | 불변 ✓ |
| `rollout_summary_dr.json` (DR 프록시) | success_rate 0.80, dr=true | 01:04 산물 격리 유지 ✓ |
| `logs/cop_trained_on.marker` / `cop_measured.marker` | 둘 다 존재 | 3자 정합 ✓ |
| `SO-ARM100` (nested git) | .gitmodules 매핑 없음 → main repo add 제외 | 규칙 준수 ✓ |

### 관찰 / 이슈 (23:00)
- 오늘 산출물(DR 프록시)은 01:04 회차에서 이미 커밋 완료 → 23:00 회차는 신규 sim 코드작업 없음(정상).
  W1 본격 잔여(DR 50ep 합성→재학습, 수 시간)는 드라이버 사이클 담당이라 야간 문서화 회차에서 미트리거.
- [자가치유] 없음 — 드라이버 STAGE 정상, 마커·산출물 3자 정합, 운영 데이터셋/측정치 불변, 에러 없음.
