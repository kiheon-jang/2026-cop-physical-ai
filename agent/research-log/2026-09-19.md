# 시뮬 진척 — 2026-09-19 (토요일)

## 오늘 진행 단계
Phase 4 - W2 - **RS232 DR 수집 배선 + DR 데이터셋 합성** (로드맵 866행 `[ ]` 의 합성 절반).
드라이버 STAGE=완료/유지(`episodes_rs232` 100ep · 성공률 0.600, 목표 0.50 충족) — 재실행 없음.

## 실행 테스트 결과 (23:30 sim-test 잡)
### 우선순위 렌더 헬스 4종 — 전부 PASS
- **sim_headless_6dof_video.py**: 2501 frames 생성·mp4 저장 OK (9.4s). 렌더 파이프라인 정상.
- **sim_camera_verification.py**: top/closeup 듀얼 30f 동시 캡처 OK (0.66s).
- **sim_pick_place.py** (레거시 무회귀): `fail` min_approach 0.323m·lift 0.0m — 고정포즈 레거시 expert(9/17~18과 동일값) → **무회귀 확인**(실 grasp 경로는 closed-loop collector).
- **sim_data_collector.py** 스모크 2ep(throwaway root, 삭제): 성공 **2/2 yield 100%**, lift 43.4mm, LeRobot v3 저장 OK (28s).

### RS232 DR 배선 (23:00 잡 산출)
- **RS232 수집기에 DR 배선 신규**(`sim_rs232_unplug_collector.py`): S1 수집기와 동일 계약으로 3곳 이식
  (`COP_COLLECT_DR=1` env-gate · `record()` 카메라노이즈 · reset 루프 restore→randomize→mj_setConst).
  환경치수 불변(조명/마찰/카메라만 섭동·복원), 기본 off → 드라이버 nominal 파이프라인 불변.
- **DR 스모크 2ep**(throwaway root, 실행 후 삭제): 성공 2/2, yield 100%, `[DR 수집]` 배너 출력, LeRobot v3 저장 정상.
- **DR 실인가 단위검증**: friction 무작위 변동·camera_noise_std 5.27·`restore_baseline` 후 friction `np.allclose` 원복 = 비파괴/비누적 확정.
- **DR 데이터셋 합성**(`data/episodes_rs232_dr`, 100ep): **23:00 잡이 6/100 에서 중단**(부모 claude 프로세스 종료 시 자식 수집기 SIGKILL, `21 leaked semaphore`) → 23:30 잡이 **detached(setsid) 재기동으로 완주 처리 중**(아래 자가치유).

## 관찰 / 이슈
- RS232 수집기는 그동안 DR 미지원이라 DR 데이터셋 자체를 만들 수단이 없었다 → 배선이 이번 사이클의 실제 blocker 해소.
- 별도 데이터셋 루트라 운영 `episodes_rs232`·`checkpoints/act_rs232_sim`·마커·`rollout_summary_rs232.json`(0.600) 전부 불변(무결성 격리).
- Phase 2 W1 실증상 DR 축은 sim 성공 천장을 못 올림(baseline 0.825 ↔ DR-trained 0.800) → RS232 정합 환경에서 실측 확증이 항목의 목적.
- **[자가치유] DR 합성 재기동**: 23:00 잡의 100ep 합성이 6/100 에서 부모 프로세스 종료에 딸려 죽음(child-of-agent 킬, 07/09~13 floor 재학습과 동일 부모-사망 패턴). `os.fork()`+`os.setsid()` 로 **새 세션에 detached 재기동**(PID 29014, `COP_COLLECT_DR=1`, root 존재 시 자동 wipe→클린 100ep) → 내 잡 종료와 무관하게 완주. 격리: 별도 루트라 운영 `episodes_rs232`(100ep)·`rollout_summary_rs232.json`(0.600) 불변. **완주 확인은 내일 잡이 `data/episodes_rs232_dr/meta/info.json` total_episodes=100 으로 검증**.
- 미결(외부): Gmail 앱 비밀번호 재발급(07:00 SMTP 535, 9/12~ 지속) — 사용자 조치 대기.

## 다음 단계
- 로드맵 866 재학습 절반: 드라이버 새 사이클 조건(`logs/cop_dataset_target`→`episodes_rs232_dr` 전환 / 마커 삭제) 세팅 시
  드라이버가 DR 42epoch 재학습 → DR-trained 4-seed 측정 → nominal 0.600 과 공정 비교. 에이전트는 준비 완료를 표면화(직접 전환 안 함).
- DP 비교(869)·실기 검증(867)은 후속 phase/외부 의존 보류.
