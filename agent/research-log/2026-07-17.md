# 시뮬 진척 — 2026-07-17 (금요일)

## 오늘 진행 단계
Phase 2 - W2 - floor 배치다양성 재학습 **10차** (**mps_mem 판독 = GPU OOM 반증 + RSS 프로브 자가치유**)

> W2 dated 실기 스텝(zero-shot)은 Orin/실기 SSH 외부의존 미수신 → 진입 불가 → floor sim 사이클 전진.

## 결정론적 드라이버 결과 (재실행 없음)
- `cop_pipeline_advance.sh`(23:00): **9차 run(pid 7243, mps-fix 발효 코드) epoch 58 이상종료 감지**
  (`metrics 마지막 epoch 미달`, tail `{"epoch":58,"step":10}` 후 `1 leaked semaphore`, traceback 없음)
  → 새 run **pid 26783**(`--epochs 100 --no-resume`, →`checkpoints/act_floor`, 23:00:34) 재시작.
  현 epoch 0 step 350 loss 3.79 정상 수렴, alive.

## 실행 테스트 결과 (야간 에이전트, 비파괴 진단)
- **9-run 만의 결착 = GPU OOM 가설 반증.** mps-fix 실적용 9차 run 의 `mps_mem` 곡선 판독:
  epoch 0 5.72GB → epoch 7 6.65GB 1회 상승 → **epoch 8~57 완전 평탄(6.65~6.67GB, 누수 전무)**.
  6.65GB = 16GB 통합메모리의 42% → **GPU OOM 물리적 불가**. 7/15 OOM 가설 반증(FD 가설은 이미 반증됨).
- **crash 시그니처 종합**: crash epoch ~57(9-run 천장) · mps_mem 평탄 · elapsed_sec 평탄(~311s, slowdown 없음)
  · traceback 없음 + leaked semaphore = **외부 SIGKILL 급사**. epoch57 done=04:03:54 → crash ~04:04.
  (고정 23:00 시작 탓 epoch~57 ↔ 벽시계~04:00 교락.)
- **남은 유일 미측정 변수 = 프로세스 RSS(통합메모리)** — mps_mem 은 GPU 할당자만 계측. jetsam(통합메모리
  압박 SIGKILL)이면 GPU 평탄이어도 RSS 는 오른다.

### 우선순위 5종 회귀 테스트 (23:30, 학습과 병행 비파괴)
- **sim_camera_verification.py** — PASS. 30프레임 양 카메라 동기 캡처, `research/simulation/video/` 저장.
- **sim_headless_6dof_video.py** — PASS. 6관절 순차 애니 2501프레임 → `sim_6dof_animation.mp4`.
- **sim_pick_place.py** — 결정론 fail, min_approach_dist=**0.3228m**(어제와 동일). fixed-pose open-loop expert
  씬 특성(closed-loop 파이프라인과 무관) → 회귀 baseline 불변. 결정론이라 1회=N회.
- **sim_data_collector.py** — 2/2 성공, **yield 100%**, lift **42.2mm**(closed-loop, ≥40mm 필터 통과).
- **joint_angle_comparison_sim.py** — 최대 관절각 오차 **0.0244°**(elbow 45.0244 vs 45) < 1° 목표 PASS.
  시뮬-실기 매핑 정합 유지.

## 관찰 / 이슈
- **[자가치유] RSS 프로브 추가** (`scripts/train_act.py`, surgical, 다음 run 발효):
  ① 모듈 top `import resource`(지역 import 뿐이라 epoch 루프 NameError 방지) ② `epoch_metric` 에
  `rss_bytes`(getrusage RUSAGE_SELF, macOS=bytes) 1필드 ③ 반증된 OOM 주석 2곳 정정. 검증 `ast OK`·
  샘플 17MB(bytes 확인). 학습로직 무변경. **판독**: 다음 crash 시 RSS 평탄=외부/시각연동, RSS 상승=jetsam.
  발효는 다음 run(pid 26783 은 23:00:34 편집前 로드 → 오늘밤 ~57 재crash 예상 → 내일 재시작이 계측코드 로드).
- **무결성 격리 유지**: target=`episodes_floor`·trained_on=`episodes_cl_dr:1783181837`(불변)·
  pending=`episodes_floor`(대기·미승격)·measured=`episodes_cl_dr:1783346557`. 학습 미완→승격/측정 보류→
  운영 `rollout_summary.json`(act_cl_dr seed42 **0.70**/median lift 50.2mm, 7/7) 불변→baseline 무손상.
- datasets `episodes_floor`·`episodes_cl`·`episodes_cl_dr` 각 50ep/3350frame 불변.
  `act_floor` 최신 ckpt=`epoch_0059`(과거 run, crash56<59→신규 상위 ckpt 없음).
- sandbox: `log show`(jetsam 확인) 권한 차단 — external-dependencies.md v3.2 allowlist 블로커 지속.

## 다음 단계 (드라이버 담당 — 재실행 금지)
- **완주 시**: pending 승격 → `act_floor/epoch_0099` 측정 → floor rollout → 4-seed 공정추정 vs
  baseline 0.825 / DR-trained 0.800 (배치 다양성이 성공률 천장을 올리는가).
- **재crash 시**: RSS 곡선으로 jetsam vs 외부 시각연동 판정 → root fix(교락 해제/resume/epoch 하향/batch 축소).
  **오늘 OOM 반증 = 9-run 증상추적 루프 탈출의 첫 결착.**
- 실기 track: Orin/실기 SSH 수신 시 W2 zero-shot 재개 — external-dependencies.md 미수신 지속 감시.

## 보고용 증거 후보
- [ ] mps_mem 평탄 판독 → GPU OOM 반증 + RSS 프로브 → 7월 보고서 [2.x] 학습 파이프라인 안정화(근본원인 재규명 실측)
- [ ] 우선순위 5종 회귀 PASS(camera·6dof·collector 2/2 42.2mm·관절각 0.0244°<1°) → 7월 보고서 [2.x] 시뮬 환경 안정성 회귀
